<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>nl.bzk.algemeenbrp</groupId>
        <artifactId>alg</artifactId>
        <version>145.3</version>
        <relativePath>../../algemeen/alg</relativePath>
    </parent>

    <groupId>nl.bzk.brp</groupId>
    <artifactId>distributie-docker</artifactId>
    <packaging>pom</packaging>
    <name>Distributie - Docker - Root POM</name>

    <properties>
        <docker.image.name>${project.artifactId}</docker.image.name>
        <docker.image.version>latest</docker.image.version>
        <docker.registry />
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Base images -->
            <dependency>
                <groupId>nl.bzk.brp</groupId>
                <artifactId>distributie-docker-basis-alpine</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>nl.bzk.brp</groupId>
                <artifactId>distributie-docker-basis-java</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>nl.bzk.brp</groupId>
                <artifactId>distributie-docker-basis-java-runtime</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>nl.bzk.brp</groupId>
                <artifactId>distributie-docker-basis-postgres</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>nl.bzk.brp</groupId>
                <artifactId>distributie-docker-basis-postgresclient</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>nl.bzk.brp</groupId>
                <artifactId>distributie-docker-basis-tomcat</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>nl.bzk.brp</groupId>
                <artifactId>distributie-docker-basis-tomcat-runtime</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>

            <!-- Logging -->
            <dependency>
                <groupId>nl.bzk.brp</groupId>
                <artifactId>distributie-docker-logging-elasticsearch</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>nl.bzk.brp</groupId>
                <artifactId>distributie-docker-logging-kibana</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>nl.bzk.brp</groupId>
                <artifactId>distributie-docker-logging-logstash</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <id>integration-test</id>
                        <phase>none</phase>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <id>jar-test-jar</id>
                        <phase>none</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>docker</id>
            <properties>
                <jenkins.user>${settings.servers.server.brpcentral.username}</jenkins.user>
                <jenkins.password>${settings.servers.server.brpcentral.password}</jenkins.password>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>docker-assembly</id>
                                <phase>none</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                                <configuration>
                                    <finalName>docker</finalName>
                                    <appendAssemblyId>false</appendAssemblyId>
                                    <attach>false</attach>
                                    <descriptors>
                                        <descriptor>src/main/assembly/docker.xml</descriptor>
                                    </descriptors>
                                    <filters>
                                        <filter>src/main/assembly/docker.properties</filter>
                                    </filters>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>docker-build</id>
                                <phase>none</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <workingDirectory>target/docker</workingDirectory>
                                    <executable>docker</executable>
                                    <commandlineArgs>build -q -t ${docker.registry}${docker.image.name}:${docker.image.version} .</commandlineArgs>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>docker-deploy</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>docker-deploy</id>
                                <phase>none</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <workingDirectory>target/docker</workingDirectory>
                                    <executable>docker</executable>
                                    <commandlineArgs>push ${docker.registry}${docker.image.name}:${docker.image.version}</commandlineArgs>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>docker-release</id>
            <properties>
                <docker.image.version>release-${project.version}</docker.image.version>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>docker-tag</id>
                                <phase>none</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <workingDirectory>target/docker</workingDirectory>
                                    <executable>docker</executable>
                                    <commandlineArgs>tag ${docker.registry}${docker.image.name}:${docker.image.version}
                                        ${docker.image.name}:${docker.image.version}
                                    </commandlineArgs>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
