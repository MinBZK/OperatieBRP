<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>nl.bzk.brp.bmr</groupId>
        <artifactId>parent</artifactId>
        <version>0.0.6</version>
    </parent>
    <artifactId>domein</artifactId>
    <name>Uit het BMR gegenereerde artifacts - Domein module</name>
    <url>http://maven.apache.org</url>
    <properties>
        <bmr.resources.directory>${project.build.directory}/generated-resources/bmr</bmr.resources.directory>
        <jdbc.url>jdbc:postgresql://localhost/brp</jdbc.url>
        <jdbc.username>brp</jdbc.username>
        <jdbc.password>brp</jdbc.password>
    </properties>

    <profiles>
        <profile>
            <id>Jenkins-ci-build</id>
            <activation>
                <property>
                    <name>ci.build</name>
                </property>
            </activation>
            <properties>
                <jdbc.url>jdbc:postgresql://fac-db05.modernodam.nl/brp</jdbc.url>
                <jdbc.username>jenkins</jdbc.username>
                <jdbc.password>95thvef288ckf43re</jdbc.password>
            </properties>
        </profile>
    </profiles>

    <build>
        <resources>
            <!-- Artifacts gegenereerd uit het BMR met de bmr-maven-plugin. -->
            <resource>
                <directory>${bmr.resources.directory}</directory>
                <targetPath>db</targetPath>
            </resource>
            <!-- Ook gegenereerd uit het BMR, maar op de "oude" manier en ingecheckt in Subversion -->
            <resource>
                <directory>${basedir}/src/main/resources</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>nl.bzk.brp.generator</groupId>
                <artifactId>bmr-maven-plugin</artifactId>
                <configuration>
                    <modelUri>classpath:/model/brp.bmrxmi</modelUri>
                    <domein>BRP</domein>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>nl.bzk.brp.bmr</groupId>
                        <artifactId>brp-model</artifactId>
                        <version>${project.version}</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>genereer-schema</id>
                        <configuration>
                            <generatoren>
                                <item>schema</item>
                                <item>schema-data-logischeElementen</item>
                            </generatoren>
                            <outputDirectory>${bmr.resources.directory}</outputDirectory>
                        </configuration>
                        <goals>
                            <goal>genereer</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>genereer-domein</id>
                        <configuration>
                            <generatoren>
                                <item>domein</item>
                            </generatoren>
                        </configuration>
                        <goals>
                            <goal>genereer</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Test gegenereerde SQL artefacten -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>sql-maven-plugin</artifactId>
                <version>1.5</version>
                <dependencies>
                    <dependency>
                        <groupId>postgresql</groupId>
                        <artifactId>postgresql</artifactId>
                        <version>9.1-901.jdbc4</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <driver>org.postgresql.Driver</driver>
                    <url>${jdbc.url}</url>
                    <username>${jdbc.username}</username>
                    <password>${jdbc.password}</password>
                </configuration>
                <executions>
                    <execution>
                        <id>test-sql-scripts</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>execute</goal>
                        </goals>
                        <configuration>
                            <srcFiles>
                                <srcFile>${bmr.resources.directory}/brp.sql</srcFile>
                                <srcFile>${bmr.resources.directory}/brp-stamgegevensStatisch.sql</srcFile>
                                <srcFile>${bmr.resources.directory}/brp-logischeElementGegevens.sql</srcFile>
                            </srcFiles>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.hibernate.javax.persistence</groupId>
            <artifactId>hibernate-jpa-2.0-api</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-codec</groupId>
            <artifactId>commons-codec</artifactId>
        </dependency>
        <dependency>
            <groupId>javax.validation</groupId>
            <artifactId>validation-api</artifactId>
            <version>1.0.0.GA</version>
        </dependency>
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-validator</artifactId>
            <version>4.1.0.Final</version>
        </dependency>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context-support</artifactId>
        </dependency>
    </dependencies>
</project>
