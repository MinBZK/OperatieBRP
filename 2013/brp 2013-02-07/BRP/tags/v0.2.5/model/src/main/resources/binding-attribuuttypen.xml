<binding package="nl.bzk.brp.model.attribuuttype">

    <include path="classpath:/binding-basistypen.xml" precompiled="false"/>

    <!-- Attribuuttype Organisatienaam -->
    <mapping abstract="true" type-name="Organisatienaam" class="nl.bzk.brp.model.attribuuttype.Organisatienaam">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Applicatienaam -->
    <mapping abstract="true" type-name="Applicatienaam" class="nl.bzk.brp.model.attribuuttype.Applicatienaam">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Sleutelwaardetekst -->
    <mapping abstract="true" type-name="Sleutelwaardetekst" class="nl.bzk.brp.model.attribuuttype.Sleutelwaardetekst">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype AangeverAdreshoudingCode -->
    <mapping abstract="true" type-name="AangeverAdreshoudingCode" class="nl.bzk.brp.model.attribuuttype.AangeverAdreshoudingCode">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype AdellijkeTitelCode -->
    <mapping abstract="true" type-name="AdellijkeTitelCode" class="nl.bzk.brp.model.attribuuttype.AdellijkeTitelCode">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Administratienummer -->
    <mapping abstract="true" type-name="Administratienummer" class="nl.bzk.brp.model.attribuuttype.Administratienummer">
        <structure map-as="long"/>
    </mapping>

    <!-- Attribuuttype Adresregel -->
    <mapping abstract="true" type-name="Adresregel" class="nl.bzk.brp.model.attribuuttype.Adresregel">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Adresseerbaarobject -->
    <mapping abstract="true" type-name="Adresseerbaarobject" class="nl.bzk.brp.model.attribuuttype.Adresseerbaarobject">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype AfgekorteNaamOpenbareRuimte -->
    <mapping abstract="true" type-name="AfgekorteNaamOpenbareRuimte" class="nl.bzk.brp.model.attribuuttype.AfgekorteNaamOpenbareRuimte">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Aktenummer -->
    <mapping abstract="true" type-name="Aktenummer" class="nl.bzk.brp.model.attribuuttype.Aktenummer">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype AutoriteitVanAfgifteReisdocumentCode -->
    <mapping abstract="true" type-name="AutoriteitVanAfgifteReisdocumentCode" class="nl.bzk.brp.model.attribuuttype.AutoriteitVanAfgifteReisdocumentCode">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype BuitenlandsePlaats -->
    <mapping abstract="true" type-name="BuitenlandsePlaats" class="nl.bzk.brp.model.attribuuttype.BuitenlandsePlaats">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype BuitenlandseRegio -->
    <mapping abstract="true" type-name="BuitenlandseRegio" class="nl.bzk.brp.model.attribuuttype.BuitenlandseRegio">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Burgerservicenummer -->
    <mapping abstract="true" type-name="Burgerservicenummer" class="nl.bzk.brp.model.attribuuttype.Burgerservicenummer">
        <value style="text" field="waarde" type="java.lang.String"/>
    </mapping>

    <!-- Attribuuttype Datum -->
    <mapping abstract="true" type-name="Datum" class="nl.bzk.brp.model.attribuuttype.Datum">
        <structure map-as="integer"/>
    </mapping>

    <!-- Attribuuttype DatumTijd -->
    <mapping abstract="true" type-name="DatumTijd" class="nl.bzk.brp.model.attribuuttype.DatumTijd">
        <structure map-as="datumTijd"/>
    </mapping>

    <!-- Attribuuttype DocumentIdentificatie -->
    <mapping abstract="true" type-name="DocumentIdentificatie" class="nl.bzk.brp.model.attribuuttype.DocumentIdentificatie">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype DocumentNummer -->
    <mapping abstract="true" type-name="DocumentNummer" class="nl.bzk.brp.model.attribuuttype.DocumentNummer">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype DocumentOmschrijving -->
    <mapping abstract="true" type-name="DocumentOmschrijving" class="nl.bzk.brp.model.attribuuttype.DocumentOmschrijving">
        <structure map-as="tekst"/>
    </mapping>


    <!-- Attribuuttype Gemeentecode -->
    <mapping abstract="true" type-name="Gemeentecode" class="nl.bzk.brp.model.attribuuttype.Gemeentecode">
        <structure map-as="code"/>
    </mapping>

    <!-- Attribuuttype Gemeentedeel -->
    <mapping abstract="true" type-name="Gemeentedeel" class="nl.bzk.brp.model.attribuuttype.Gemeentedeel">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Geslachtsnaamcomponent -->
    <mapping abstract="true" type-name="Geslachtsnaamcomponent" class="nl.bzk.brp.model.attribuuttype.Geslachtsnaamcomponent">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Huisletter -->
    <mapping abstract="true" type-name="Huisletter" class="nl.bzk.brp.model.attribuuttype.Huisletter">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Huisnummer -->
    <mapping abstract="true" type-name="Huisnummer" class="nl.bzk.brp.model.attribuuttype.Huisnummer">
        <structure map-as="integer"/>
    </mapping>

    <!-- Attribuuttype Huisnummertoevoeging -->
    <mapping abstract="true" type-name="Huisnummertoevoeging" class="nl.bzk.brp.model.attribuuttype.Huisnummertoevoeging">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype IdentificatiecodeNummeraanduiding -->
    <mapping abstract="true" type-name="IdentificatiecodeNummeraanduiding" class="nl.bzk.brp.model.attribuuttype.IdentificatiecodeNummeraanduiding">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype ISO3166_1Alpha2 -->
    <mapping abstract="true" type-name="ISO3166_1Alpha2" class="nl.bzk.brp.model.attribuuttype.ISO31661Alpha2">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Landcode -->
    <mapping abstract="true" type-name="Landcode" class="nl.bzk.brp.model.attribuuttype.Landcode">
        <structure map-as="code"/>
    </mapping>

    <!-- Attribuuttype LengteInCm -->
    <mapping abstract="true" type-name="LengteInCm" class="nl.bzk.brp.model.attribuuttype.LengteInCm">
        <structure map-as="short"/>
    </mapping>

    <!-- Attribuuttype LocatieOmschrijving -->
    <mapping abstract="true" type-name="LocatieOmschrijving" class="nl.bzk.brp.model.attribuuttype.LocatieOmschrijving">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype LocatieTovAdres -->
    <mapping abstract="true" type-name="LocatieTovAdres" class="nl.bzk.brp.model.attribuuttype.LocatieTovAdres">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Naam -->
    <mapping abstract="true" type-name="Naam" class="nl.bzk.brp.model.attribuuttype.Naam">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype NaamOpenbareRuimte -->
    <mapping abstract="true" type-name="NaamOpenbareRuimte" class="nl.bzk.brp.model.attribuuttype.NaamOpenbareRuimte">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Nationaliteitcode -->
    <mapping abstract="true" type-name="Nationaliteitcode" class="nl.bzk.brp.model.attribuuttype.Nationaliteitcode">
        <structure map-as="code"/>
    </mapping>

    <!-- Attribuuttype Omschrijving -->
    <mapping abstract="true" type-name="Omschrijving" class="nl.bzk.brp.model.attribuuttype.Omschrijving">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Ontleningstoelichting -->
    <mapping abstract="true" type-name="Ontleningstoelichting" class="nl.bzk.brp.model.attribuuttype.Ontleningstoelichting">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype PlaatsCode -->
    <mapping abstract="true" type-name="PlaatsCode" class="nl.bzk.brp.model.attribuuttype.PlaatsCode">
        <structure map-as="code"/>
    </mapping>

    <!-- Attribuuttype Postcode -->
    <mapping abstract="true" type-name="Postcode" class="nl.bzk.brp.model.attribuuttype.Postcode">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype PredikaatCode -->
    <mapping abstract="true" type-name="PredikaatCode" class="nl.bzk.brp.model.attribuuttype.PredikaatCode">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype RedenBeeindigingRelatieCode -->
    <mapping abstract="true" type-name="RedenBeeindigingRelatieCode" class="nl.bzk.brp.model.attribuuttype.RedenBeeindigingRelatieCode">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype RedenVerkrijgingNaam -->
    <mapping abstract="true" type-name="RedenVerkrijgingNaam" class="nl.bzk.brp.model.attribuuttype.RedenVerkrijgingNaam">
        <structure map-as="tekst"/>
    </mapping>

        <!-- Attribuuttype RedenVerkrijgingCode -->
    <mapping abstract="true" type-name="RedenVerkrijgingCode" class="nl.bzk.brp.model.attribuuttype.RedenVerkrijgingCode">
        <structure map-as="kortecode"/>
    </mapping>

    <!-- Attribuuttype RedenVerliesNaam -->
    <mapping abstract="true" type-name="RedenVerliesNaam" class="nl.bzk.brp.model.attribuuttype.RedenVerliesNaam">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype RedenVervallenReisdocumentCode -->
    <mapping abstract="true" type-name="RedenVervallenReisdocumentCode" class="nl.bzk.brp.model.attribuuttype.RedenVervallenReisdocumentCode">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype RedenWijzigingAdresCode -->
    <mapping abstract="true" type-name="RedenWijzigingAdresCode" class="nl.bzk.brp.model.attribuuttype.RedenWijzigingAdresCode">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype ReisdocumentNummer -->
    <mapping abstract="true" type-name="ReisdocumentNummer" class="nl.bzk.brp.model.attribuuttype.ReisdocumentNummer">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Scheidingsteken -->
    <mapping abstract="true" type-name="Scheidingsteken" class="nl.bzk.brp.model.attribuuttype.Scheidingsteken">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Volgnummer -->
    <mapping abstract="true" type-name="Volgnummer" class="nl.bzk.brp.model.attribuuttype.Volgnummer">
        <structure map-as="integer"/>
    </mapping>

    <!-- Attribuuttype Voornaam -->
    <mapping abstract="true" type-name="Voornaam" class="nl.bzk.brp.model.attribuuttype.Voornaam">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Voorvoegsel -->
    <mapping abstract="true" type-name="Voorvoegsel" class="nl.bzk.brp.model.attribuuttype.Voorvoegsel">
        <structure map-as="tekst"/>
    </mapping>

    <!-- Attribuuttype Versienummer -->
    <mapping abstract="true" type-name="Versienummer" class="nl.bzk.brp.model.attribuuttype.Versienummer">
        <structure map-as="long"/>
    </mapping>

</binding>