// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Wijzegebruikgeslnaam;
import nl.bzk.brp.model.data.kern.WijzegebruikgeslnaamDataOnDemand;
import nl.bzk.brp.model.data.kern.WijzegebruikgeslnaamIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect WijzegebruikgeslnaamIntegrationTest_Roo_IntegrationTest {
    
    declare @type: WijzegebruikgeslnaamIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: WijzegebruikgeslnaamIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: WijzegebruikgeslnaamIntegrationTest: @Transactional;
    
    @Autowired
    private WijzegebruikgeslnaamDataOnDemand WijzegebruikgeslnaamIntegrationTest.dod;
    
    @Test
    public void WijzegebruikgeslnaamIntegrationTest.testCountWijzegebruikgeslnaams() {
        Assert.assertNotNull("Data on demand for 'Wijzegebruikgeslnaam' failed to initialize correctly", dod.getRandomWijzegebruikgeslnaam());
        long count = Wijzegebruikgeslnaam.countWijzegebruikgeslnaams();
        Assert.assertTrue("Counter for 'Wijzegebruikgeslnaam' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void WijzegebruikgeslnaamIntegrationTest.testFindWijzegebruikgeslnaam() {
        Wijzegebruikgeslnaam obj = dod.getRandomWijzegebruikgeslnaam();
        Assert.assertNotNull("Data on demand for 'Wijzegebruikgeslnaam' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Wijzegebruikgeslnaam' failed to provide an identifier", id);
        obj = Wijzegebruikgeslnaam.findWijzegebruikgeslnaam(id);
        Assert.assertNotNull("Find method for 'Wijzegebruikgeslnaam' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Wijzegebruikgeslnaam' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void WijzegebruikgeslnaamIntegrationTest.testFindAllWijzegebruikgeslnaams() {
        Assert.assertNotNull("Data on demand for 'Wijzegebruikgeslnaam' failed to initialize correctly", dod.getRandomWijzegebruikgeslnaam());
        long count = Wijzegebruikgeslnaam.countWijzegebruikgeslnaams();
        Assert.assertTrue("Too expensive to perform a find all test for 'Wijzegebruikgeslnaam', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Wijzegebruikgeslnaam> result = Wijzegebruikgeslnaam.findAllWijzegebruikgeslnaams();
        Assert.assertNotNull("Find all method for 'Wijzegebruikgeslnaam' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Wijzegebruikgeslnaam' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void WijzegebruikgeslnaamIntegrationTest.testFindWijzegebruikgeslnaamEntries() {
        Assert.assertNotNull("Data on demand for 'Wijzegebruikgeslnaam' failed to initialize correctly", dod.getRandomWijzegebruikgeslnaam());
        long count = Wijzegebruikgeslnaam.countWijzegebruikgeslnaams();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Wijzegebruikgeslnaam> result = Wijzegebruikgeslnaam.findWijzegebruikgeslnaamEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Wijzegebruikgeslnaam' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Wijzegebruikgeslnaam' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void WijzegebruikgeslnaamIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Wijzegebruikgeslnaam' failed to initialize correctly", dod.getRandomWijzegebruikgeslnaam());
        Wijzegebruikgeslnaam obj = dod.getNewTransientWijzegebruikgeslnaam(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Wijzegebruikgeslnaam' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Wijzegebruikgeslnaam' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Wijzegebruikgeslnaam' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void WijzegebruikgeslnaamIntegrationTest.testRemove() {
        Wijzegebruikgeslnaam obj = dod.getRandomWijzegebruikgeslnaam();
        Assert.assertNotNull("Data on demand for 'Wijzegebruikgeslnaam' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Wijzegebruikgeslnaam' failed to provide an identifier", id);
        obj = Wijzegebruikgeslnaam.findWijzegebruikgeslnaam(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Wijzegebruikgeslnaam' with identifier '" + id + "'", Wijzegebruikgeslnaam.findWijzegebruikgeslnaam(id));
    }
    
}
