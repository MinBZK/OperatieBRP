// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.autaut.Protocolleringsniveau;
import nl.bzk.brp.model.data.autaut.ProtocolleringsniveauDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect ProtocolleringsniveauDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ProtocolleringsniveauDataOnDemand: @Component;
    
    private Random ProtocolleringsniveauDataOnDemand.rnd = new SecureRandom();
    
    private List<Protocolleringsniveau> ProtocolleringsniveauDataOnDemand.data;
    
    public Protocolleringsniveau ProtocolleringsniveauDataOnDemand.getNewTransientProtocolleringsniveau(int index) {
        Protocolleringsniveau obj = new Protocolleringsniveau();
        setCode(obj, index);
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setNaam(obj, index);
        setOms(obj, index);
        return obj;
    }
    
    public void ProtocolleringsniveauDataOnDemand.setCode(Protocolleringsniveau obj, int index) {
        Short code = new Integer(index).shortValue();
        obj.setCode(code);
    }
    
    public void ProtocolleringsniveauDataOnDemand.setDataanvgel(Protocolleringsniveau obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void ProtocolleringsniveauDataOnDemand.setDateindegel(Protocolleringsniveau obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void ProtocolleringsniveauDataOnDemand.setNaam(Protocolleringsniveau obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public void ProtocolleringsniveauDataOnDemand.setOms(Protocolleringsniveau obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = new Random().nextInt(10) + oms.substring(1, 250);
        }
        obj.setOms(oms);
    }
    
    public Protocolleringsniveau ProtocolleringsniveauDataOnDemand.getSpecificProtocolleringsniveau(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Protocolleringsniveau obj = data.get(index);
        Short id = obj.getId();
        return Protocolleringsniveau.findProtocolleringsniveau(id);
    }
    
    public Protocolleringsniveau ProtocolleringsniveauDataOnDemand.getRandomProtocolleringsniveau() {
        init();
        Protocolleringsniveau obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Protocolleringsniveau.findProtocolleringsniveau(id);
    }
    
    public boolean ProtocolleringsniveauDataOnDemand.modifyProtocolleringsniveau(Protocolleringsniveau obj) {
        return false;
    }
    
    public void ProtocolleringsniveauDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Protocolleringsniveau.findProtocolleringsniveauEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Protocolleringsniveau' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Protocolleringsniveau>();
        for (int i = 0; i < 10; i++) {
            Protocolleringsniveau obj = getNewTransientProtocolleringsniveau(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
