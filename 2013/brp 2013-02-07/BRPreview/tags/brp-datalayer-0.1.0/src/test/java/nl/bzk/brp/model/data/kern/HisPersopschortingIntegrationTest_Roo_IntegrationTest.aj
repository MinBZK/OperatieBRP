// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.HisPersopschorting;
import nl.bzk.brp.model.data.kern.HisPersopschortingDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPersopschortingIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisPersopschortingIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisPersopschortingIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisPersopschortingIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisPersopschortingIntegrationTest: @Transactional;
    
    @Autowired
    private HisPersopschortingDataOnDemand HisPersopschortingIntegrationTest.dod;
    
    @Test
    public void HisPersopschortingIntegrationTest.testCountHisPersopschortings() {
        Assert.assertNotNull("Data on demand for 'HisPersopschorting' failed to initialize correctly", dod.getRandomHisPersopschorting());
        long count = HisPersopschorting.countHisPersopschortings();
        Assert.assertTrue("Counter for 'HisPersopschorting' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisPersopschortingIntegrationTest.testFindHisPersopschorting() {
        HisPersopschorting obj = dod.getRandomHisPersopschorting();
        Assert.assertNotNull("Data on demand for 'HisPersopschorting' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisPersopschorting' failed to provide an identifier", id);
        obj = HisPersopschorting.findHisPersopschorting(id);
        Assert.assertNotNull("Find method for 'HisPersopschorting' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisPersopschorting' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisPersopschortingIntegrationTest.testFindAllHisPersopschortings() {
        Assert.assertNotNull("Data on demand for 'HisPersopschorting' failed to initialize correctly", dod.getRandomHisPersopschorting());
        long count = HisPersopschorting.countHisPersopschortings();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisPersopschorting', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisPersopschorting> result = HisPersopschorting.findAllHisPersopschortings();
        Assert.assertNotNull("Find all method for 'HisPersopschorting' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisPersopschorting' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisPersopschortingIntegrationTest.testFindHisPersopschortingEntries() {
        Assert.assertNotNull("Data on demand for 'HisPersopschorting' failed to initialize correctly", dod.getRandomHisPersopschorting());
        long count = HisPersopschorting.countHisPersopschortings();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisPersopschorting> result = HisPersopschorting.findHisPersopschortingEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisPersopschorting' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisPersopschorting' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisPersopschortingIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisPersopschorting' failed to initialize correctly", dod.getRandomHisPersopschorting());
        HisPersopschorting obj = dod.getNewTransientHisPersopschorting(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisPersopschorting' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisPersopschorting' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisPersopschorting' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisPersopschortingIntegrationTest.testRemove() {
        HisPersopschorting obj = dod.getRandomHisPersopschorting();
        Assert.assertNotNull("Data on demand for 'HisPersopschorting' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisPersopschorting' failed to provide an identifier", id);
        obj = HisPersopschorting.findHisPersopschorting(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisPersopschorting' with identifier '" + id + "'", HisPersopschorting.findHisPersopschorting(id));
    }
    
}
