// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Element;
import nl.bzk.brp.model.data.kern.ElementDataOnDemand;
import nl.bzk.brp.model.data.kern.ElementIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ElementIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ElementIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ElementIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ElementIntegrationTest: @Transactional;
    
    @Autowired
    private ElementDataOnDemand ElementIntegrationTest.dod;
    
    @Test
    public void ElementIntegrationTest.testCountElements() {
        Assert.assertNotNull("Data on demand for 'Element' failed to initialize correctly", dod.getRandomElement());
        long count = Element.countElements();
        Assert.assertTrue("Counter for 'Element' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ElementIntegrationTest.testFindElement() {
        Element obj = dod.getRandomElement();
        Assert.assertNotNull("Data on demand for 'Element' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Element' failed to provide an identifier", id);
        obj = Element.findElement(id);
        Assert.assertNotNull("Find method for 'Element' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Element' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ElementIntegrationTest.testFindAllElements() {
        Assert.assertNotNull("Data on demand for 'Element' failed to initialize correctly", dod.getRandomElement());
        long count = Element.countElements();
        Assert.assertTrue("Too expensive to perform a find all test for 'Element', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Element> result = Element.findAllElements();
        Assert.assertNotNull("Find all method for 'Element' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Element' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ElementIntegrationTest.testFindElementEntries() {
        Assert.assertNotNull("Data on demand for 'Element' failed to initialize correctly", dod.getRandomElement());
        long count = Element.countElements();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Element> result = Element.findElementEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Element' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Element' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ElementIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Element' failed to initialize correctly", dod.getRandomElement());
        Element obj = dod.getNewTransientElement(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Element' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Element' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Element' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ElementIntegrationTest.testRemove() {
        Element obj = dod.getRandomElement();
        Assert.assertNotNull("Data on demand for 'Element' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Element' failed to provide an identifier", id);
        obj = Element.findElement(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Element' with identifier '" + id + "'", Element.findElement(id));
    }
    
}
