// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Verdrag;
import nl.bzk.brp.model.data.kern.VerdragDataOnDemand;
import nl.bzk.brp.model.data.kern.VerdragIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect VerdragIntegrationTest_Roo_IntegrationTest {
    
    declare @type: VerdragIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: VerdragIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: VerdragIntegrationTest: @Transactional;
    
    @Autowired
    private VerdragDataOnDemand VerdragIntegrationTest.dod;
    
    @Test
    public void VerdragIntegrationTest.testCountVerdrags() {
        Assert.assertNotNull("Data on demand for 'Verdrag' failed to initialize correctly", dod.getRandomVerdrag());
        long count = Verdrag.countVerdrags();
        Assert.assertTrue("Counter for 'Verdrag' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void VerdragIntegrationTest.testFindVerdrag() {
        Verdrag obj = dod.getRandomVerdrag();
        Assert.assertNotNull("Data on demand for 'Verdrag' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Verdrag' failed to provide an identifier", id);
        obj = Verdrag.findVerdrag(id);
        Assert.assertNotNull("Find method for 'Verdrag' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Verdrag' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void VerdragIntegrationTest.testFindAllVerdrags() {
        Assert.assertNotNull("Data on demand for 'Verdrag' failed to initialize correctly", dod.getRandomVerdrag());
        long count = Verdrag.countVerdrags();
        Assert.assertTrue("Too expensive to perform a find all test for 'Verdrag', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Verdrag> result = Verdrag.findAllVerdrags();
        Assert.assertNotNull("Find all method for 'Verdrag' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Verdrag' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void VerdragIntegrationTest.testFindVerdragEntries() {
        Assert.assertNotNull("Data on demand for 'Verdrag' failed to initialize correctly", dod.getRandomVerdrag());
        long count = Verdrag.countVerdrags();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Verdrag> result = Verdrag.findVerdragEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Verdrag' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Verdrag' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void VerdragIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Verdrag' failed to initialize correctly", dod.getRandomVerdrag());
        Verdrag obj = dod.getNewTransientVerdrag(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Verdrag' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Verdrag' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Verdrag' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void VerdragIntegrationTest.testRemove() {
        Verdrag obj = dod.getRandomVerdrag();
        Assert.assertNotNull("Data on demand for 'Verdrag' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Verdrag' failed to provide an identifier", id);
        obj = Verdrag.findVerdrag(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Verdrag' with identifier '" + id + "'", Verdrag.findVerdrag(id));
    }
    
}
