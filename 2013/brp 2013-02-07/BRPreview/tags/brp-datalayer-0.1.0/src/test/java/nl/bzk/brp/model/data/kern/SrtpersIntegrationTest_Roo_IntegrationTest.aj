// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Srtpers;
import nl.bzk.brp.model.data.kern.SrtpersDataOnDemand;
import nl.bzk.brp.model.data.kern.SrtpersIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SrtpersIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SrtpersIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SrtpersIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SrtpersIntegrationTest: @Transactional;
    
    @Autowired
    private SrtpersDataOnDemand SrtpersIntegrationTest.dod;
    
    @Test
    public void SrtpersIntegrationTest.testCountSrtperses() {
        Assert.assertNotNull("Data on demand for 'Srtpers' failed to initialize correctly", dod.getRandomSrtpers());
        long count = Srtpers.countSrtperses();
        Assert.assertTrue("Counter for 'Srtpers' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SrtpersIntegrationTest.testFindSrtpers() {
        Srtpers obj = dod.getRandomSrtpers();
        Assert.assertNotNull("Data on demand for 'Srtpers' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtpers' failed to provide an identifier", id);
        obj = Srtpers.findSrtpers(id);
        Assert.assertNotNull("Find method for 'Srtpers' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Srtpers' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SrtpersIntegrationTest.testFindAllSrtperses() {
        Assert.assertNotNull("Data on demand for 'Srtpers' failed to initialize correctly", dod.getRandomSrtpers());
        long count = Srtpers.countSrtperses();
        Assert.assertTrue("Too expensive to perform a find all test for 'Srtpers', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Srtpers> result = Srtpers.findAllSrtperses();
        Assert.assertNotNull("Find all method for 'Srtpers' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Srtpers' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SrtpersIntegrationTest.testFindSrtpersEntries() {
        Assert.assertNotNull("Data on demand for 'Srtpers' failed to initialize correctly", dod.getRandomSrtpers());
        long count = Srtpers.countSrtperses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Srtpers> result = Srtpers.findSrtpersEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Srtpers' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Srtpers' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SrtpersIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Srtpers' failed to initialize correctly", dod.getRandomSrtpers());
        Srtpers obj = dod.getNewTransientSrtpers(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Srtpers' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Srtpers' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Srtpers' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SrtpersIntegrationTest.testRemove() {
        Srtpers obj = dod.getRandomSrtpers();
        Assert.assertNotNull("Data on demand for 'Srtpers' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtpers' failed to provide an identifier", id);
        obj = Srtpers.findSrtpers(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Srtpers' with identifier '" + id + "'", Srtpers.findSrtpers(id));
    }
    
}
