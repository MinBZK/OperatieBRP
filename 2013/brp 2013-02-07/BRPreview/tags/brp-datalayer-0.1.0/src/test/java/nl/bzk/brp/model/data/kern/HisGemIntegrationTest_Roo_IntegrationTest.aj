// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.HisGem;
import nl.bzk.brp.model.data.kern.HisGemDataOnDemand;
import nl.bzk.brp.model.data.kern.HisGemIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisGemIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisGemIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisGemIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisGemIntegrationTest: @Transactional;
    
    @Autowired
    private HisGemDataOnDemand HisGemIntegrationTest.dod;
    
    @Test
    public void HisGemIntegrationTest.testCountHisGems() {
        Assert.assertNotNull("Data on demand for 'HisGem' failed to initialize correctly", dod.getRandomHisGem());
        long count = HisGem.countHisGems();
        Assert.assertTrue("Counter for 'HisGem' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisGemIntegrationTest.testFindHisGem() {
        HisGem obj = dod.getRandomHisGem();
        Assert.assertNotNull("Data on demand for 'HisGem' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisGem' failed to provide an identifier", id);
        obj = HisGem.findHisGem(id);
        Assert.assertNotNull("Find method for 'HisGem' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisGem' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisGemIntegrationTest.testFindAllHisGems() {
        Assert.assertNotNull("Data on demand for 'HisGem' failed to initialize correctly", dod.getRandomHisGem());
        long count = HisGem.countHisGems();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisGem', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisGem> result = HisGem.findAllHisGems();
        Assert.assertNotNull("Find all method for 'HisGem' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisGem' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisGemIntegrationTest.testFindHisGemEntries() {
        Assert.assertNotNull("Data on demand for 'HisGem' failed to initialize correctly", dod.getRandomHisGem());
        long count = HisGem.countHisGems();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisGem> result = HisGem.findHisGemEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisGem' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisGem' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisGemIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisGem' failed to initialize correctly", dod.getRandomHisGem());
        HisGem obj = dod.getNewTransientHisGem(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisGem' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisGem' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisGem' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisGemIntegrationTest.testRemove() {
        HisGem obj = dod.getRandomHisGem();
        Assert.assertNotNull("Data on demand for 'HisGem' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisGem' failed to provide an identifier", id);
        obj = HisGem.findHisGem(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisGem' with identifier '" + id + "'", HisGem.findHisGem(id));
    }
    
}
