// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Wijzegebruikgeslnaam;
import nl.bzk.brp.model.data.kern.WijzegebruikgeslnaamDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect WijzegebruikgeslnaamDataOnDemand_Roo_DataOnDemand {
    
    declare @type: WijzegebruikgeslnaamDataOnDemand: @Component;
    
    private Random WijzegebruikgeslnaamDataOnDemand.rnd = new SecureRandom();
    
    private List<Wijzegebruikgeslnaam> WijzegebruikgeslnaamDataOnDemand.data;
    
    public Wijzegebruikgeslnaam WijzegebruikgeslnaamDataOnDemand.getNewTransientWijzegebruikgeslnaam(int index) {
        Wijzegebruikgeslnaam obj = new Wijzegebruikgeslnaam();
        setCode(obj, index);
        setNaam(obj, index);
        setOms(obj, index);
        return obj;
    }
    
    public void WijzegebruikgeslnaamDataOnDemand.setCode(Wijzegebruikgeslnaam obj, int index) {
        String code = String.valueOf(index);
        if (code.length() > 1) {
            code = new Random().nextInt(10) + code.substring(1, 1);
        }
        obj.setCode(code);
    }
    
    public void WijzegebruikgeslnaamDataOnDemand.setNaam(Wijzegebruikgeslnaam obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public void WijzegebruikgeslnaamDataOnDemand.setOms(Wijzegebruikgeslnaam obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = oms.substring(0, 250);
        }
        obj.setOms(oms);
    }
    
    public Wijzegebruikgeslnaam WijzegebruikgeslnaamDataOnDemand.getSpecificWijzegebruikgeslnaam(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Wijzegebruikgeslnaam obj = data.get(index);
        Short id = obj.getId();
        return Wijzegebruikgeslnaam.findWijzegebruikgeslnaam(id);
    }
    
    public Wijzegebruikgeslnaam WijzegebruikgeslnaamDataOnDemand.getRandomWijzegebruikgeslnaam() {
        init();
        Wijzegebruikgeslnaam obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Wijzegebruikgeslnaam.findWijzegebruikgeslnaam(id);
    }
    
    public boolean WijzegebruikgeslnaamDataOnDemand.modifyWijzegebruikgeslnaam(Wijzegebruikgeslnaam obj) {
        return false;
    }
    
    public void WijzegebruikgeslnaamDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Wijzegebruikgeslnaam.findWijzegebruikgeslnaamEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Wijzegebruikgeslnaam' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Wijzegebruikgeslnaam>();
        for (int i = 0; i < 10; i++) {
            Wijzegebruikgeslnaam obj = getNewTransientWijzegebruikgeslnaam(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
