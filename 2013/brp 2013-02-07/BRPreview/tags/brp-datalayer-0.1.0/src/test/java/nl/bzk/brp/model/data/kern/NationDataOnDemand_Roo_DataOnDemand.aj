// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Nation;
import nl.bzk.brp.model.data.kern.NationDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect NationDataOnDemand_Roo_DataOnDemand {
    
    declare @type: NationDataOnDemand: @Component;
    
    private Random NationDataOnDemand.rnd = new SecureRandom();
    
    private List<Nation> NationDataOnDemand.data;
    
    public Nation NationDataOnDemand.getNewTransientNation(int index) {
        Nation obj = new Nation();
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setNaam(obj, index);
        setNationcode(obj, index);
        return obj;
    }
    
    public void NationDataOnDemand.setDataanvgel(Nation obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void NationDataOnDemand.setDateindegel(Nation obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void NationDataOnDemand.setNaam(Nation obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public void NationDataOnDemand.setNationcode(Nation obj, int index) {
        String nationcode = "na_" + index;
        if (nationcode.length() > 4) {
            nationcode = nationcode.substring(0, 4);
        }
        obj.setNationcode(nationcode);
    }
    
    public Nation NationDataOnDemand.getSpecificNation(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Nation obj = data.get(index);
        Integer id = obj.getId();
        return Nation.findNation(id);
    }
    
    public Nation NationDataOnDemand.getRandomNation() {
        init();
        Nation obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Nation.findNation(id);
    }
    
    public boolean NationDataOnDemand.modifyNation(Nation obj) {
        return false;
    }
    
    public void NationDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Nation.findNationEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Nation' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Nation>();
        for (int i = 0; i < 10; i++) {
            Nation obj = getNewTransientNation(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
