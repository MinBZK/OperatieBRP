// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Categoriesrtactie;
import nl.bzk.brp.model.data.kern.CategoriesrtactieDataOnDemand;
import nl.bzk.brp.model.data.kern.CategoriesrtactieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategoriesrtactieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategoriesrtactieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategoriesrtactieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategoriesrtactieIntegrationTest: @Transactional;
    
    @Autowired
    private CategoriesrtactieDataOnDemand CategoriesrtactieIntegrationTest.dod;
    
    @Test
    public void CategoriesrtactieIntegrationTest.testCountCategoriesrtacties() {
        Assert.assertNotNull("Data on demand for 'Categoriesrtactie' failed to initialize correctly", dod.getRandomCategoriesrtactie());
        long count = Categoriesrtactie.countCategoriesrtacties();
        Assert.assertTrue("Counter for 'Categoriesrtactie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategoriesrtactieIntegrationTest.testFindCategoriesrtactie() {
        Categoriesrtactie obj = dod.getRandomCategoriesrtactie();
        Assert.assertNotNull("Data on demand for 'Categoriesrtactie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Categoriesrtactie' failed to provide an identifier", id);
        obj = Categoriesrtactie.findCategoriesrtactie(id);
        Assert.assertNotNull("Find method for 'Categoriesrtactie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Categoriesrtactie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategoriesrtactieIntegrationTest.testFindAllCategoriesrtacties() {
        Assert.assertNotNull("Data on demand for 'Categoriesrtactie' failed to initialize correctly", dod.getRandomCategoriesrtactie());
        long count = Categoriesrtactie.countCategoriesrtacties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Categoriesrtactie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Categoriesrtactie> result = Categoriesrtactie.findAllCategoriesrtacties();
        Assert.assertNotNull("Find all method for 'Categoriesrtactie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Categoriesrtactie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategoriesrtactieIntegrationTest.testFindCategoriesrtactieEntries() {
        Assert.assertNotNull("Data on demand for 'Categoriesrtactie' failed to initialize correctly", dod.getRandomCategoriesrtactie());
        long count = Categoriesrtactie.countCategoriesrtacties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Categoriesrtactie> result = Categoriesrtactie.findCategoriesrtactieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Categoriesrtactie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Categoriesrtactie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategoriesrtactieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Categoriesrtactie' failed to initialize correctly", dod.getRandomCategoriesrtactie());
        Categoriesrtactie obj = dod.getNewTransientCategoriesrtactie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Categoriesrtactie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Categoriesrtactie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Categoriesrtactie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CategoriesrtactieIntegrationTest.testRemove() {
        Categoriesrtactie obj = dod.getRandomCategoriesrtactie();
        Assert.assertNotNull("Data on demand for 'Categoriesrtactie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Categoriesrtactie' failed to provide an identifier", id);
        obj = Categoriesrtactie.findCategoriesrtactie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Categoriesrtactie' with identifier '" + id + "'", Categoriesrtactie.findCategoriesrtactie(id));
    }
    
}
