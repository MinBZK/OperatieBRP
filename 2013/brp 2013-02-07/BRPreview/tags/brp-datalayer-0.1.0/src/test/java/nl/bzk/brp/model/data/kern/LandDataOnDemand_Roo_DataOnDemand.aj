// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Land;
import nl.bzk.brp.model.data.kern.LandDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect LandDataOnDemand_Roo_DataOnDemand {
    
    declare @type: LandDataOnDemand: @Component;
    
    private Random LandDataOnDemand.rnd = new SecureRandom();
    
    private List<Land> LandDataOnDemand.data;
    
    public Land LandDataOnDemand.getNewTransientLand(int index) {
        Land obj = new Land();
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setIso31661alpha2(obj, index);
        setLandcode(obj, index);
        setNaam(obj, index);
        return obj;
    }
    
    public void LandDataOnDemand.setDataanvgel(Land obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void LandDataOnDemand.setDateindegel(Land obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void LandDataOnDemand.setIso31661alpha2(Land obj, int index) {
        String iso31661alpha2 = "i" + index;
        if (iso31661alpha2.length() > 2) {
            iso31661alpha2 = iso31661alpha2.substring(0, 2);
        }
        obj.setIso31661alpha2(iso31661alpha2);
    }
    
    public void LandDataOnDemand.setLandcode(Land obj, int index) {
        String landcode = "la_" + index;
        if (landcode.length() > 4) {
            landcode = landcode.substring(0, 4);
        }
        obj.setLandcode(landcode);
    }
    
    public void LandDataOnDemand.setNaam(Land obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public Land LandDataOnDemand.getSpecificLand(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Land obj = data.get(index);
        Integer id = obj.getId();
        return Land.findLand(id);
    }
    
    public Land LandDataOnDemand.getRandomLand() {
        init();
        Land obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Land.findLand(id);
    }
    
    public boolean LandDataOnDemand.modifyLand(Land obj) {
        return false;
    }
    
    public void LandDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Land.findLandEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Land' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Land>();
        for (int i = 0; i < 10; i++) {
            Land obj = getNewTransientLand(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
