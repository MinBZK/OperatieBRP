// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Actie;
import nl.bzk.brp.model.data.kern.ActieDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPersimmigratie;
import nl.bzk.brp.model.data.kern.HisPersimmigratieDataOnDemand;
import nl.bzk.brp.model.data.kern.Land;
import nl.bzk.brp.model.data.kern.LandDataOnDemand;
import nl.bzk.brp.model.data.kern.Pers;
import nl.bzk.brp.model.data.kern.PersDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect HisPersimmigratieDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HisPersimmigratieDataOnDemand: @Component;
    
    private Random HisPersimmigratieDataOnDemand.rnd = new SecureRandom();
    
    private List<HisPersimmigratie> HisPersimmigratieDataOnDemand.data;
    
    @Autowired
    private ActieDataOnDemand HisPersimmigratieDataOnDemand.actieDataOnDemand;
    
    @Autowired
    private LandDataOnDemand HisPersimmigratieDataOnDemand.landDataOnDemand;
    
    @Autowired
    private PersDataOnDemand HisPersimmigratieDataOnDemand.persDataOnDemand;
    
    public HisPersimmigratie HisPersimmigratieDataOnDemand.getNewTransientHisPersimmigratie(int index) {
        HisPersimmigratie obj = new HisPersimmigratie();
        setActieaanpgel(obj, index);
        setActieinh(obj, index);
        setActieverval(obj, index);
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setDatvestiginginnederland(obj, index);
        setLandvanwaargevestigd(obj, index);
        setPers(obj, index);
        setTsreg(obj, index);
        setTsverval(obj, index);
        return obj;
    }
    
    public void HisPersimmigratieDataOnDemand.setActieaanpgel(HisPersimmigratie obj, int index) {
        Actie actieaanpgel = actieDataOnDemand.getRandomActie();
        obj.setActieaanpgel(actieaanpgel);
    }
    
    public void HisPersimmigratieDataOnDemand.setActieinh(HisPersimmigratie obj, int index) {
        Actie actieinh = actieDataOnDemand.getRandomActie();
        obj.setActieinh(actieinh);
    }
    
    public void HisPersimmigratieDataOnDemand.setActieverval(HisPersimmigratie obj, int index) {
        Actie actieverval = actieDataOnDemand.getRandomActie();
        obj.setActieverval(actieverval);
    }
    
    public void HisPersimmigratieDataOnDemand.setDataanvgel(HisPersimmigratie obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void HisPersimmigratieDataOnDemand.setDateindegel(HisPersimmigratie obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void HisPersimmigratieDataOnDemand.setDatvestiginginnederland(HisPersimmigratie obj, int index) {
        BigDecimal datvestiginginnederland = BigDecimal.valueOf(index);
        obj.setDatvestiginginnederland(datvestiginginnederland);
    }
    
    public void HisPersimmigratieDataOnDemand.setLandvanwaargevestigd(HisPersimmigratie obj, int index) {
        Land landvanwaargevestigd = landDataOnDemand.getRandomLand();
        obj.setLandvanwaargevestigd(landvanwaargevestigd);
    }
    
    public void HisPersimmigratieDataOnDemand.setPers(HisPersimmigratie obj, int index) {
        Pers pers = persDataOnDemand.getRandomPers();
        obj.setPers(pers);
    }
    
    public void HisPersimmigratieDataOnDemand.setTsreg(HisPersimmigratie obj, int index) {
        Date tsreg = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsreg(tsreg);
    }
    
    public void HisPersimmigratieDataOnDemand.setTsverval(HisPersimmigratie obj, int index) {
        Date tsverval = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsverval(tsverval);
    }
    
    public HisPersimmigratie HisPersimmigratieDataOnDemand.getSpecificHisPersimmigratie(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        HisPersimmigratie obj = data.get(index);
        Long id = obj.getId();
        return HisPersimmigratie.findHisPersimmigratie(id);
    }
    
    public HisPersimmigratie HisPersimmigratieDataOnDemand.getRandomHisPersimmigratie() {
        init();
        HisPersimmigratie obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return HisPersimmigratie.findHisPersimmigratie(id);
    }
    
    public boolean HisPersimmigratieDataOnDemand.modifyHisPersimmigratie(HisPersimmigratie obj) {
        return false;
    }
    
    public void HisPersimmigratieDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = HisPersimmigratie.findHisPersimmigratieEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'HisPersimmigratie' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<HisPersimmigratie>();
        for (int i = 0; i < 10; i++) {
            HisPersimmigratie obj = getNewTransientHisPersimmigratie(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
