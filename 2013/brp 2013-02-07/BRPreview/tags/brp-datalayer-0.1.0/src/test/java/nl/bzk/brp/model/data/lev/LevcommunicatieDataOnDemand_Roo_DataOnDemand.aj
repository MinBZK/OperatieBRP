// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.lev;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.lev.Lev;
import nl.bzk.brp.model.data.lev.LevDataOnDemand;
import nl.bzk.brp.model.data.lev.Levcommunicatie;
import nl.bzk.brp.model.data.lev.LevcommunicatieDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect LevcommunicatieDataOnDemand_Roo_DataOnDemand {
    
    declare @type: LevcommunicatieDataOnDemand: @Component;
    
    private Random LevcommunicatieDataOnDemand.rnd = new SecureRandom();
    
    private List<Levcommunicatie> LevcommunicatieDataOnDemand.data;
    
    @Autowired
    private LevDataOnDemand LevcommunicatieDataOnDemand.levDataOnDemand;
    
    public Levcommunicatie LevcommunicatieDataOnDemand.getNewTransientLevcommunicatie(int index) {
        Levcommunicatie obj = new Levcommunicatie();
        setLev(obj, index);
        return obj;
    }
    
    public void LevcommunicatieDataOnDemand.setLev(Levcommunicatie obj, int index) {
        Lev lev = levDataOnDemand.getRandomLev();
        obj.setLev(lev);
    }
    
    public Levcommunicatie LevcommunicatieDataOnDemand.getSpecificLevcommunicatie(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Levcommunicatie obj = data.get(index);
        Long id = obj.getId();
        return Levcommunicatie.findLevcommunicatie(id);
    }
    
    public Levcommunicatie LevcommunicatieDataOnDemand.getRandomLevcommunicatie() {
        init();
        Levcommunicatie obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Levcommunicatie.findLevcommunicatie(id);
    }
    
    public boolean LevcommunicatieDataOnDemand.modifyLevcommunicatie(Levcommunicatie obj) {
        return false;
    }
    
    public void LevcommunicatieDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Levcommunicatie.findLevcommunicatieEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Levcommunicatie' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Levcommunicatie>();
        for (int i = 0; i < 10; i++) {
            Levcommunicatie obj = getNewTransientLevcommunicatie(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
