// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Predikaat;
import nl.bzk.brp.model.data.kern.PredikaatDataOnDemand;
import nl.bzk.brp.model.data.kern.PredikaatIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PredikaatIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PredikaatIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PredikaatIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PredikaatIntegrationTest: @Transactional;
    
    @Autowired
    private PredikaatDataOnDemand PredikaatIntegrationTest.dod;
    
    @Test
    public void PredikaatIntegrationTest.testCountPredikaats() {
        Assert.assertNotNull("Data on demand for 'Predikaat' failed to initialize correctly", dod.getRandomPredikaat());
        long count = Predikaat.countPredikaats();
        Assert.assertTrue("Counter for 'Predikaat' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PredikaatIntegrationTest.testFindPredikaat() {
        Predikaat obj = dod.getRandomPredikaat();
        Assert.assertNotNull("Data on demand for 'Predikaat' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Predikaat' failed to provide an identifier", id);
        obj = Predikaat.findPredikaat(id);
        Assert.assertNotNull("Find method for 'Predikaat' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Predikaat' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PredikaatIntegrationTest.testFindAllPredikaats() {
        Assert.assertNotNull("Data on demand for 'Predikaat' failed to initialize correctly", dod.getRandomPredikaat());
        long count = Predikaat.countPredikaats();
        Assert.assertTrue("Too expensive to perform a find all test for 'Predikaat', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Predikaat> result = Predikaat.findAllPredikaats();
        Assert.assertNotNull("Find all method for 'Predikaat' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Predikaat' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PredikaatIntegrationTest.testFindPredikaatEntries() {
        Assert.assertNotNull("Data on demand for 'Predikaat' failed to initialize correctly", dod.getRandomPredikaat());
        long count = Predikaat.countPredikaats();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Predikaat> result = Predikaat.findPredikaatEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Predikaat' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Predikaat' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PredikaatIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Predikaat' failed to initialize correctly", dod.getRandomPredikaat());
        Predikaat obj = dod.getNewTransientPredikaat(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Predikaat' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Predikaat' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Predikaat' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PredikaatIntegrationTest.testRemove() {
        Predikaat obj = dod.getRandomPredikaat();
        Assert.assertNotNull("Data on demand for 'Predikaat' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Predikaat' failed to provide an identifier", id);
        obj = Predikaat.findPredikaat(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Predikaat' with identifier '" + id + "'", Predikaat.findPredikaat(id));
    }
    
}
