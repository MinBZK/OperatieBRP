// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.HisPersbijhverantwoordelijk;
import nl.bzk.brp.model.data.kern.HisPersbijhverantwoordelijkDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPersbijhverantwoordelijkIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisPersbijhverantwoordelijkIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisPersbijhverantwoordelijkIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisPersbijhverantwoordelijkIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisPersbijhverantwoordelijkIntegrationTest: @Transactional;
    
    @Autowired
    private HisPersbijhverantwoordelijkDataOnDemand HisPersbijhverantwoordelijkIntegrationTest.dod;
    
    @Test
    public void HisPersbijhverantwoordelijkIntegrationTest.testCountHisPersbijhverantwoordelijks() {
        Assert.assertNotNull("Data on demand for 'HisPersbijhverantwoordelijk' failed to initialize correctly", dod.getRandomHisPersbijhverantwoordelijk());
        long count = HisPersbijhverantwoordelijk.countHisPersbijhverantwoordelijks();
        Assert.assertTrue("Counter for 'HisPersbijhverantwoordelijk' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisPersbijhverantwoordelijkIntegrationTest.testFindHisPersbijhverantwoordelijk() {
        HisPersbijhverantwoordelijk obj = dod.getRandomHisPersbijhverantwoordelijk();
        Assert.assertNotNull("Data on demand for 'HisPersbijhverantwoordelijk' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisPersbijhverantwoordelijk' failed to provide an identifier", id);
        obj = HisPersbijhverantwoordelijk.findHisPersbijhverantwoordelijk(id);
        Assert.assertNotNull("Find method for 'HisPersbijhverantwoordelijk' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisPersbijhverantwoordelijk' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisPersbijhverantwoordelijkIntegrationTest.testFindAllHisPersbijhverantwoordelijks() {
        Assert.assertNotNull("Data on demand for 'HisPersbijhverantwoordelijk' failed to initialize correctly", dod.getRandomHisPersbijhverantwoordelijk());
        long count = HisPersbijhverantwoordelijk.countHisPersbijhverantwoordelijks();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisPersbijhverantwoordelijk', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisPersbijhverantwoordelijk> result = HisPersbijhverantwoordelijk.findAllHisPersbijhverantwoordelijks();
        Assert.assertNotNull("Find all method for 'HisPersbijhverantwoordelijk' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisPersbijhverantwoordelijk' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisPersbijhverantwoordelijkIntegrationTest.testFindHisPersbijhverantwoordelijkEntries() {
        Assert.assertNotNull("Data on demand for 'HisPersbijhverantwoordelijk' failed to initialize correctly", dod.getRandomHisPersbijhverantwoordelijk());
        long count = HisPersbijhverantwoordelijk.countHisPersbijhverantwoordelijks();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisPersbijhverantwoordelijk> result = HisPersbijhverantwoordelijk.findHisPersbijhverantwoordelijkEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisPersbijhverantwoordelijk' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisPersbijhverantwoordelijk' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisPersbijhverantwoordelijkIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisPersbijhverantwoordelijk' failed to initialize correctly", dod.getRandomHisPersbijhverantwoordelijk());
        HisPersbijhverantwoordelijk obj = dod.getNewTransientHisPersbijhverantwoordelijk(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisPersbijhverantwoordelijk' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisPersbijhverantwoordelijk' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisPersbijhverantwoordelijk' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisPersbijhverantwoordelijkIntegrationTest.testRemove() {
        HisPersbijhverantwoordelijk obj = dod.getRandomHisPersbijhverantwoordelijk();
        Assert.assertNotNull("Data on demand for 'HisPersbijhverantwoordelijk' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisPersbijhverantwoordelijk' failed to provide an identifier", id);
        obj = HisPersbijhverantwoordelijk.findHisPersbijhverantwoordelijk(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisPersbijhverantwoordelijk' with identifier '" + id + "'", HisPersbijhverantwoordelijk.findHisPersbijhverantwoordelijk(id));
    }
    
}
