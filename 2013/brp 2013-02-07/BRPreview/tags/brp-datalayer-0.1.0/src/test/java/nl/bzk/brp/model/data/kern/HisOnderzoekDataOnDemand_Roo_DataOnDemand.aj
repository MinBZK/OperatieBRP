// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Actie;
import nl.bzk.brp.model.data.kern.ActieDataOnDemand;
import nl.bzk.brp.model.data.kern.HisOnderzoek;
import nl.bzk.brp.model.data.kern.HisOnderzoekDataOnDemand;
import nl.bzk.brp.model.data.kern.Onderzoek;
import nl.bzk.brp.model.data.kern.OnderzoekDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect HisOnderzoekDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HisOnderzoekDataOnDemand: @Component;
    
    private Random HisOnderzoekDataOnDemand.rnd = new SecureRandom();
    
    private List<HisOnderzoek> HisOnderzoekDataOnDemand.data;
    
    @Autowired
    private ActieDataOnDemand HisOnderzoekDataOnDemand.actieDataOnDemand;
    
    @Autowired
    private OnderzoekDataOnDemand HisOnderzoekDataOnDemand.onderzoekDataOnDemand;
    
    public HisOnderzoek HisOnderzoekDataOnDemand.getNewTransientHisOnderzoek(int index) {
        HisOnderzoek obj = new HisOnderzoek();
        setActieinh(obj, index);
        setActieverval(obj, index);
        setDatbegin(obj, index);
        setDateinde(obj, index);
        setOms(obj, index);
        setOnderzoek(obj, index);
        setTsreg(obj, index);
        setTsverval(obj, index);
        return obj;
    }
    
    public void HisOnderzoekDataOnDemand.setActieinh(HisOnderzoek obj, int index) {
        Actie actieinh = actieDataOnDemand.getRandomActie();
        obj.setActieinh(actieinh);
    }
    
    public void HisOnderzoekDataOnDemand.setActieverval(HisOnderzoek obj, int index) {
        Actie actieverval = actieDataOnDemand.getRandomActie();
        obj.setActieverval(actieverval);
    }
    
    public void HisOnderzoekDataOnDemand.setDatbegin(HisOnderzoek obj, int index) {
        BigDecimal datbegin = BigDecimal.valueOf(index);
        obj.setDatbegin(datbegin);
    }
    
    public void HisOnderzoekDataOnDemand.setDateinde(HisOnderzoek obj, int index) {
        BigDecimal dateinde = BigDecimal.valueOf(index);
        obj.setDateinde(dateinde);
    }
    
    public void HisOnderzoekDataOnDemand.setOms(HisOnderzoek obj, int index) {
        String oms = "oms_" + index;
        obj.setOms(oms);
    }
    
    public void HisOnderzoekDataOnDemand.setOnderzoek(HisOnderzoek obj, int index) {
        Onderzoek onderzoek = onderzoekDataOnDemand.getRandomOnderzoek();
        obj.setOnderzoek(onderzoek);
    }
    
    public void HisOnderzoekDataOnDemand.setTsreg(HisOnderzoek obj, int index) {
        Date tsreg = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsreg(tsreg);
    }
    
    public void HisOnderzoekDataOnDemand.setTsverval(HisOnderzoek obj, int index) {
        Date tsverval = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsverval(tsverval);
    }
    
    public HisOnderzoek HisOnderzoekDataOnDemand.getSpecificHisOnderzoek(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        HisOnderzoek obj = data.get(index);
        Long id = obj.getId();
        return HisOnderzoek.findHisOnderzoek(id);
    }
    
    public HisOnderzoek HisOnderzoekDataOnDemand.getRandomHisOnderzoek() {
        init();
        HisOnderzoek obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return HisOnderzoek.findHisOnderzoek(id);
    }
    
    public boolean HisOnderzoekDataOnDemand.modifyHisOnderzoek(HisOnderzoek obj) {
        return false;
    }
    
    public void HisOnderzoekDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = HisOnderzoek.findHisOnderzoekEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'HisOnderzoek' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<HisOnderzoek>();
        for (int i = 0; i < 10; i++) {
            HisOnderzoek obj = getNewTransientHisOnderzoek(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
