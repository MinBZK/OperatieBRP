// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Srtactie;
import nl.bzk.brp.model.data.kern.SrtactieDataOnDemand;
import nl.bzk.brp.model.data.kern.SrtactieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SrtactieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SrtactieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SrtactieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SrtactieIntegrationTest: @Transactional;
    
    @Autowired
    private SrtactieDataOnDemand SrtactieIntegrationTest.dod;
    
    @Test
    public void SrtactieIntegrationTest.testCountSrtacties() {
        Assert.assertNotNull("Data on demand for 'Srtactie' failed to initialize correctly", dod.getRandomSrtactie());
        long count = Srtactie.countSrtacties();
        Assert.assertTrue("Counter for 'Srtactie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SrtactieIntegrationTest.testFindSrtactie() {
        Srtactie obj = dod.getRandomSrtactie();
        Assert.assertNotNull("Data on demand for 'Srtactie' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtactie' failed to provide an identifier", id);
        obj = Srtactie.findSrtactie(id);
        Assert.assertNotNull("Find method for 'Srtactie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Srtactie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SrtactieIntegrationTest.testFindAllSrtacties() {
        Assert.assertNotNull("Data on demand for 'Srtactie' failed to initialize correctly", dod.getRandomSrtactie());
        long count = Srtactie.countSrtacties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Srtactie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Srtactie> result = Srtactie.findAllSrtacties();
        Assert.assertNotNull("Find all method for 'Srtactie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Srtactie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SrtactieIntegrationTest.testFindSrtactieEntries() {
        Assert.assertNotNull("Data on demand for 'Srtactie' failed to initialize correctly", dod.getRandomSrtactie());
        long count = Srtactie.countSrtacties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Srtactie> result = Srtactie.findSrtactieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Srtactie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Srtactie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SrtactieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Srtactie' failed to initialize correctly", dod.getRandomSrtactie());
        Srtactie obj = dod.getNewTransientSrtactie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Srtactie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Srtactie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Srtactie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SrtactieIntegrationTest.testRemove() {
        Srtactie obj = dod.getRandomSrtactie();
        Assert.assertNotNull("Data on demand for 'Srtactie' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtactie' failed to provide an identifier", id);
        obj = Srtactie.findSrtactie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Srtactie' with identifier '" + id + "'", Srtactie.findSrtactie(id));
    }
    
}
