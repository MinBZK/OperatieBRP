// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.HisPersverificatie;
import nl.bzk.brp.model.data.kern.HisPersverificatieDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPersverificatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisPersverificatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisPersverificatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisPersverificatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisPersverificatieIntegrationTest: @Transactional;
    
    @Autowired
    private HisPersverificatieDataOnDemand HisPersverificatieIntegrationTest.dod;
    
    @Test
    public void HisPersverificatieIntegrationTest.testCountHisPersverificaties() {
        Assert.assertNotNull("Data on demand for 'HisPersverificatie' failed to initialize correctly", dod.getRandomHisPersverificatie());
        long count = HisPersverificatie.countHisPersverificaties();
        Assert.assertTrue("Counter for 'HisPersverificatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisPersverificatieIntegrationTest.testFindHisPersverificatie() {
        HisPersverificatie obj = dod.getRandomHisPersverificatie();
        Assert.assertNotNull("Data on demand for 'HisPersverificatie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisPersverificatie' failed to provide an identifier", id);
        obj = HisPersverificatie.findHisPersverificatie(id);
        Assert.assertNotNull("Find method for 'HisPersverificatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisPersverificatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisPersverificatieIntegrationTest.testFindAllHisPersverificaties() {
        Assert.assertNotNull("Data on demand for 'HisPersverificatie' failed to initialize correctly", dod.getRandomHisPersverificatie());
        long count = HisPersverificatie.countHisPersverificaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisPersverificatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisPersverificatie> result = HisPersverificatie.findAllHisPersverificaties();
        Assert.assertNotNull("Find all method for 'HisPersverificatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisPersverificatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisPersverificatieIntegrationTest.testFindHisPersverificatieEntries() {
        Assert.assertNotNull("Data on demand for 'HisPersverificatie' failed to initialize correctly", dod.getRandomHisPersverificatie());
        long count = HisPersverificatie.countHisPersverificaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisPersverificatie> result = HisPersverificatie.findHisPersverificatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisPersverificatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisPersverificatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisPersverificatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisPersverificatie' failed to initialize correctly", dod.getRandomHisPersverificatie());
        HisPersverificatie obj = dod.getNewTransientHisPersverificatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisPersverificatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisPersverificatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisPersverificatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisPersverificatieIntegrationTest.testRemove() {
        HisPersverificatie obj = dod.getRandomHisPersverificatie();
        Assert.assertNotNull("Data on demand for 'HisPersverificatie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisPersverificatie' failed to provide an identifier", id);
        obj = HisPersverificatie.findHisPersverificatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisPersverificatie' with identifier '" + id + "'", HisPersverificatie.findHisPersverificatie(id));
    }
    
}
