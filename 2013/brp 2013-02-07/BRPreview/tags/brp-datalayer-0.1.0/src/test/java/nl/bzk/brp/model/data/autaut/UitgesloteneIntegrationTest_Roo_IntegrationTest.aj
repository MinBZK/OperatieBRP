// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.Uitgeslotene;
import nl.bzk.brp.model.data.autaut.UitgesloteneDataOnDemand;
import nl.bzk.brp.model.data.autaut.UitgesloteneIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UitgesloteneIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UitgesloteneIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UitgesloteneIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UitgesloteneIntegrationTest: @Transactional;
    
    @Autowired
    private UitgesloteneDataOnDemand UitgesloteneIntegrationTest.dod;
    
    @Test
    public void UitgesloteneIntegrationTest.testCountUitgeslotenes() {
        Assert.assertNotNull("Data on demand for 'Uitgeslotene' failed to initialize correctly", dod.getRandomUitgeslotene());
        long count = Uitgeslotene.countUitgeslotenes();
        Assert.assertTrue("Counter for 'Uitgeslotene' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UitgesloteneIntegrationTest.testFindUitgeslotene() {
        Uitgeslotene obj = dod.getRandomUitgeslotene();
        Assert.assertNotNull("Data on demand for 'Uitgeslotene' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Uitgeslotene' failed to provide an identifier", id);
        obj = Uitgeslotene.findUitgeslotene(id);
        Assert.assertNotNull("Find method for 'Uitgeslotene' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Uitgeslotene' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UitgesloteneIntegrationTest.testFindAllUitgeslotenes() {
        Assert.assertNotNull("Data on demand for 'Uitgeslotene' failed to initialize correctly", dod.getRandomUitgeslotene());
        long count = Uitgeslotene.countUitgeslotenes();
        Assert.assertTrue("Too expensive to perform a find all test for 'Uitgeslotene', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Uitgeslotene> result = Uitgeslotene.findAllUitgeslotenes();
        Assert.assertNotNull("Find all method for 'Uitgeslotene' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Uitgeslotene' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UitgesloteneIntegrationTest.testFindUitgesloteneEntries() {
        Assert.assertNotNull("Data on demand for 'Uitgeslotene' failed to initialize correctly", dod.getRandomUitgeslotene());
        long count = Uitgeslotene.countUitgeslotenes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Uitgeslotene> result = Uitgeslotene.findUitgesloteneEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Uitgeslotene' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Uitgeslotene' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UitgesloteneIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Uitgeslotene' failed to initialize correctly", dod.getRandomUitgeslotene());
        Uitgeslotene obj = dod.getNewTransientUitgeslotene(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Uitgeslotene' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Uitgeslotene' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Uitgeslotene' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UitgesloteneIntegrationTest.testRemove() {
        Uitgeslotene obj = dod.getRandomUitgeslotene();
        Assert.assertNotNull("Data on demand for 'Uitgeslotene' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Uitgeslotene' failed to provide an identifier", id);
        obj = Uitgeslotene.findUitgeslotene(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Uitgeslotene' with identifier '" + id + "'", Uitgeslotene.findUitgeslotene(id));
    }
    
}
