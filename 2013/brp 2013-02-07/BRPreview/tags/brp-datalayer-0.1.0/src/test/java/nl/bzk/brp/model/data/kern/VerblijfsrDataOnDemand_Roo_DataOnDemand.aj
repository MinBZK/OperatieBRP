// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Verblijfsr;
import nl.bzk.brp.model.data.kern.VerblijfsrDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect VerblijfsrDataOnDemand_Roo_DataOnDemand {
    
    declare @type: VerblijfsrDataOnDemand: @Component;
    
    private Random VerblijfsrDataOnDemand.rnd = new SecureRandom();
    
    private List<Verblijfsr> VerblijfsrDataOnDemand.data;
    
    public Verblijfsr VerblijfsrDataOnDemand.getNewTransientVerblijfsr(int index) {
        Verblijfsr obj = new Verblijfsr();
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setOms(obj, index);
        return obj;
    }
    
    public void VerblijfsrDataOnDemand.setDataanvgel(Verblijfsr obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void VerblijfsrDataOnDemand.setDateindegel(Verblijfsr obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void VerblijfsrDataOnDemand.setOms(Verblijfsr obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = new Random().nextInt(10) + oms.substring(1, 250);
        }
        obj.setOms(oms);
    }
    
    public Verblijfsr VerblijfsrDataOnDemand.getSpecificVerblijfsr(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Verblijfsr obj = data.get(index);
        Integer id = obj.getId();
        return Verblijfsr.findVerblijfsr(id);
    }
    
    public Verblijfsr VerblijfsrDataOnDemand.getRandomVerblijfsr() {
        init();
        Verblijfsr obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Verblijfsr.findVerblijfsr(id);
    }
    
    public boolean VerblijfsrDataOnDemand.modifyVerblijfsr(Verblijfsr obj) {
        return false;
    }
    
    public void VerblijfsrDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Verblijfsr.findVerblijfsrEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Verblijfsr' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Verblijfsr>();
        for (int i = 0; i < 10; i++) {
            Verblijfsr obj = getNewTransientVerblijfsr(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
