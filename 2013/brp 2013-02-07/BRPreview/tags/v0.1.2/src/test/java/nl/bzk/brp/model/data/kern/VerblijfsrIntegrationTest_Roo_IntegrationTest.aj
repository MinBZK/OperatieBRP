// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Verblijfsr;
import nl.bzk.brp.model.data.kern.VerblijfsrDataOnDemand;
import nl.bzk.brp.model.data.kern.VerblijfsrIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect VerblijfsrIntegrationTest_Roo_IntegrationTest {
    
    declare @type: VerblijfsrIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: VerblijfsrIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: VerblijfsrIntegrationTest: @Transactional;
    
    @Autowired
    private VerblijfsrDataOnDemand VerblijfsrIntegrationTest.dod;
    
    @Test
    public void VerblijfsrIntegrationTest.testCountVerblijfsrs() {
        Assert.assertNotNull("Data on demand for 'Verblijfsr' failed to initialize correctly", dod.getRandomVerblijfsr());
        long count = Verblijfsr.countVerblijfsrs();
        Assert.assertTrue("Counter for 'Verblijfsr' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void VerblijfsrIntegrationTest.testFindVerblijfsr() {
        Verblijfsr obj = dod.getRandomVerblijfsr();
        Assert.assertNotNull("Data on demand for 'Verblijfsr' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Verblijfsr' failed to provide an identifier", id);
        obj = Verblijfsr.findVerblijfsr(id);
        Assert.assertNotNull("Find method for 'Verblijfsr' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Verblijfsr' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void VerblijfsrIntegrationTest.testFindAllVerblijfsrs() {
        Assert.assertNotNull("Data on demand for 'Verblijfsr' failed to initialize correctly", dod.getRandomVerblijfsr());
        long count = Verblijfsr.countVerblijfsrs();
        Assert.assertTrue("Too expensive to perform a find all test for 'Verblijfsr', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Verblijfsr> result = Verblijfsr.findAllVerblijfsrs();
        Assert.assertNotNull("Find all method for 'Verblijfsr' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Verblijfsr' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void VerblijfsrIntegrationTest.testFindVerblijfsrEntries() {
        Assert.assertNotNull("Data on demand for 'Verblijfsr' failed to initialize correctly", dod.getRandomVerblijfsr());
        long count = Verblijfsr.countVerblijfsrs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Verblijfsr> result = Verblijfsr.findVerblijfsrEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Verblijfsr' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Verblijfsr' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void VerblijfsrIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Verblijfsr' failed to initialize correctly", dod.getRandomVerblijfsr());
        Verblijfsr obj = dod.getNewTransientVerblijfsr(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Verblijfsr' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Verblijfsr' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Verblijfsr' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void VerblijfsrIntegrationTest.testRemove() {
        Verblijfsr obj = dod.getRandomVerblijfsr();
        Assert.assertNotNull("Data on demand for 'Verblijfsr' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Verblijfsr' failed to provide an identifier", id);
        obj = Verblijfsr.findVerblijfsr(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Verblijfsr' with identifier '" + id + "'", Verblijfsr.findVerblijfsr(id));
    }
    
}
