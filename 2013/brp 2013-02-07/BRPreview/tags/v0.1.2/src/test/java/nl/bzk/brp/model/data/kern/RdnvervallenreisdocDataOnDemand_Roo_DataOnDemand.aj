// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Rdnvervallenreisdoc;
import nl.bzk.brp.model.data.kern.RdnvervallenreisdocDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect RdnvervallenreisdocDataOnDemand_Roo_DataOnDemand {
    
    declare @type: RdnvervallenreisdocDataOnDemand: @Component;
    
    private Random RdnvervallenreisdocDataOnDemand.rnd = new SecureRandom();
    
    private List<Rdnvervallenreisdoc> RdnvervallenreisdocDataOnDemand.data;
    
    public Rdnvervallenreisdoc RdnvervallenreisdocDataOnDemand.getNewTransientRdnvervallenreisdoc(int index) {
        Rdnvervallenreisdoc obj = new Rdnvervallenreisdoc();
        setCode(obj, index);
        setNaam(obj, index);
        return obj;
    }
    
    public void RdnvervallenreisdocDataOnDemand.setCode(Rdnvervallenreisdoc obj, int index) {
        String code = String.valueOf(index);
        if (code.length() > 1) {
            code = new Random().nextInt(10) + code.substring(1, 1);
        }
        obj.setCode(code);
    }
    
    public void RdnvervallenreisdocDataOnDemand.setNaam(Rdnvervallenreisdoc obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public Rdnvervallenreisdoc RdnvervallenreisdocDataOnDemand.getSpecificRdnvervallenreisdoc(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Rdnvervallenreisdoc obj = data.get(index);
        Short id = obj.getId();
        return Rdnvervallenreisdoc.findRdnvervallenreisdoc(id);
    }
    
    public Rdnvervallenreisdoc RdnvervallenreisdocDataOnDemand.getRandomRdnvervallenreisdoc() {
        init();
        Rdnvervallenreisdoc obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Rdnvervallenreisdoc.findRdnvervallenreisdoc(id);
    }
    
    public boolean RdnvervallenreisdocDataOnDemand.modifyRdnvervallenreisdoc(Rdnvervallenreisdoc obj) {
        return false;
    }
    
    public void RdnvervallenreisdocDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Rdnvervallenreisdoc.findRdnvervallenreisdocEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Rdnvervallenreisdoc' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Rdnvervallenreisdoc>();
        for (int i = 0; i < 10; i++) {
            Rdnvervallenreisdoc obj = getNewTransientRdnvervallenreisdoc(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
