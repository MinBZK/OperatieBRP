// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.Certificaat;
import nl.bzk.brp.model.data.autaut.CertificaatDataOnDemand;
import nl.bzk.brp.model.data.autaut.CertificaatIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CertificaatIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CertificaatIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CertificaatIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CertificaatIntegrationTest: @Transactional;
    
    @Autowired
    private CertificaatDataOnDemand CertificaatIntegrationTest.dod;
    
    @Test
    public void CertificaatIntegrationTest.testCountCertificaats() {
        Assert.assertNotNull("Data on demand for 'Certificaat' failed to initialize correctly", dod.getRandomCertificaat());
        long count = Certificaat.countCertificaats();
        Assert.assertTrue("Counter for 'Certificaat' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CertificaatIntegrationTest.testFindCertificaat() {
        Certificaat obj = dod.getRandomCertificaat();
        Assert.assertNotNull("Data on demand for 'Certificaat' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Certificaat' failed to provide an identifier", id);
        obj = Certificaat.findCertificaat(id);
        Assert.assertNotNull("Find method for 'Certificaat' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Certificaat' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CertificaatIntegrationTest.testFindAllCertificaats() {
        Assert.assertNotNull("Data on demand for 'Certificaat' failed to initialize correctly", dod.getRandomCertificaat());
        long count = Certificaat.countCertificaats();
        Assert.assertTrue("Too expensive to perform a find all test for 'Certificaat', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Certificaat> result = Certificaat.findAllCertificaats();
        Assert.assertNotNull("Find all method for 'Certificaat' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Certificaat' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CertificaatIntegrationTest.testFindCertificaatEntries() {
        Assert.assertNotNull("Data on demand for 'Certificaat' failed to initialize correctly", dod.getRandomCertificaat());
        long count = Certificaat.countCertificaats();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Certificaat> result = Certificaat.findCertificaatEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Certificaat' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Certificaat' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CertificaatIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Certificaat' failed to initialize correctly", dod.getRandomCertificaat());
        Certificaat obj = dod.getNewTransientCertificaat(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Certificaat' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Certificaat' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Certificaat' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CertificaatIntegrationTest.testRemove() {
        Certificaat obj = dod.getRandomCertificaat();
        Assert.assertNotNull("Data on demand for 'Certificaat' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Certificaat' failed to provide an identifier", id);
        obj = Certificaat.findCertificaat(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Certificaat' with identifier '" + id + "'", Certificaat.findCertificaat(id));
    }
    
}
