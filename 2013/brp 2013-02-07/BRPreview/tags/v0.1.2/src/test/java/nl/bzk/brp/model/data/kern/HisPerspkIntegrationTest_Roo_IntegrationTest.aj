// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.HisPerspk;
import nl.bzk.brp.model.data.kern.HisPerspkDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPerspkIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisPerspkIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisPerspkIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisPerspkIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisPerspkIntegrationTest: @Transactional;
    
    @Autowired
    private HisPerspkDataOnDemand HisPerspkIntegrationTest.dod;
    
    @Test
    public void HisPerspkIntegrationTest.testCountHisPerspks() {
        Assert.assertNotNull("Data on demand for 'HisPerspk' failed to initialize correctly", dod.getRandomHisPerspk());
        long count = HisPerspk.countHisPerspks();
        Assert.assertTrue("Counter for 'HisPerspk' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisPerspkIntegrationTest.testFindHisPerspk() {
        HisPerspk obj = dod.getRandomHisPerspk();
        Assert.assertNotNull("Data on demand for 'HisPerspk' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisPerspk' failed to provide an identifier", id);
        obj = HisPerspk.findHisPerspk(id);
        Assert.assertNotNull("Find method for 'HisPerspk' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisPerspk' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisPerspkIntegrationTest.testFindAllHisPerspks() {
        Assert.assertNotNull("Data on demand for 'HisPerspk' failed to initialize correctly", dod.getRandomHisPerspk());
        long count = HisPerspk.countHisPerspks();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisPerspk', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisPerspk> result = HisPerspk.findAllHisPerspks();
        Assert.assertNotNull("Find all method for 'HisPerspk' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisPerspk' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisPerspkIntegrationTest.testFindHisPerspkEntries() {
        Assert.assertNotNull("Data on demand for 'HisPerspk' failed to initialize correctly", dod.getRandomHisPerspk());
        long count = HisPerspk.countHisPerspks();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisPerspk> result = HisPerspk.findHisPerspkEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisPerspk' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisPerspk' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisPerspkIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisPerspk' failed to initialize correctly", dod.getRandomHisPerspk());
        HisPerspk obj = dod.getNewTransientHisPerspk(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisPerspk' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisPerspk' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisPerspk' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisPerspkIntegrationTest.testRemove() {
        HisPerspk obj = dod.getRandomHisPerspk();
        Assert.assertNotNull("Data on demand for 'HisPerspk' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisPerspk' failed to provide an identifier", id);
        obj = HisPerspk.findHisPerspk(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisPerspk' with identifier '" + id + "'", HisPerspk.findHisPerspk(id));
    }
    
}
