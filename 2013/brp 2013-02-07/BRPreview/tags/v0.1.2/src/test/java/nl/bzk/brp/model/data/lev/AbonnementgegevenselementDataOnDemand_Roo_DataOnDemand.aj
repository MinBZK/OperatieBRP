// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.lev;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Dbobject;
import nl.bzk.brp.model.data.kern.DbobjectDataOnDemand;
import nl.bzk.brp.model.data.lev.Abonnement;
import nl.bzk.brp.model.data.lev.AbonnementDataOnDemand;
import nl.bzk.brp.model.data.lev.Abonnementgegevenselement;
import nl.bzk.brp.model.data.lev.AbonnementgegevenselementDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect AbonnementgegevenselementDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AbonnementgegevenselementDataOnDemand: @Component;
    
    private Random AbonnementgegevenselementDataOnDemand.rnd = new SecureRandom();
    
    private List<Abonnementgegevenselement> AbonnementgegevenselementDataOnDemand.data;
    
    @Autowired
    private AbonnementDataOnDemand AbonnementgegevenselementDataOnDemand.abonnementDataOnDemand;
    
    @Autowired
    private DbobjectDataOnDemand AbonnementgegevenselementDataOnDemand.dbobjectDataOnDemand;
    
    public Abonnementgegevenselement AbonnementgegevenselementDataOnDemand.getNewTransientAbonnementgegevenselement(int index) {
        Abonnementgegevenselement obj = new Abonnementgegevenselement();
        setAbonnement(obj, index);
        setGegevenselement(obj, index);
        return obj;
    }
    
    public void AbonnementgegevenselementDataOnDemand.setAbonnement(Abonnementgegevenselement obj, int index) {
        Abonnement abonnement = abonnementDataOnDemand.getRandomAbonnement();
        obj.setAbonnement(abonnement);
    }
    
    public void AbonnementgegevenselementDataOnDemand.setGegevenselement(Abonnementgegevenselement obj, int index) {
        Dbobject gegevenselement = dbobjectDataOnDemand.getRandomDbobject();
        obj.setGegevenselement(gegevenselement);
    }
    
    public Abonnementgegevenselement AbonnementgegevenselementDataOnDemand.getSpecificAbonnementgegevenselement(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Abonnementgegevenselement obj = data.get(index);
        Integer id = obj.getId();
        return Abonnementgegevenselement.findAbonnementgegevenselement(id);
    }
    
    public Abonnementgegevenselement AbonnementgegevenselementDataOnDemand.getRandomAbonnementgegevenselement() {
        init();
        Abonnementgegevenselement obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Abonnementgegevenselement.findAbonnementgegevenselement(id);
    }
    
    public boolean AbonnementgegevenselementDataOnDemand.modifyAbonnementgegevenselement(Abonnementgegevenselement obj) {
        return false;
    }
    
    public void AbonnementgegevenselementDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Abonnementgegevenselement.findAbonnementgegevenselementEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Abonnementgegevenselement' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Abonnementgegevenselement>();
        for (int i = 0; i < 10; i++) {
            Abonnementgegevenselement obj = getNewTransientAbonnementgegevenselement(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
