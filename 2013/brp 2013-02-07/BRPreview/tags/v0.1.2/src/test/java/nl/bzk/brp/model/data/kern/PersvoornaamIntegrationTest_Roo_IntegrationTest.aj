// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Persvoornaam;
import nl.bzk.brp.model.data.kern.PersvoornaamDataOnDemand;
import nl.bzk.brp.model.data.kern.PersvoornaamIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PersvoornaamIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersvoornaamIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersvoornaamIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersvoornaamIntegrationTest: @Transactional;
    
    @Autowired
    private PersvoornaamDataOnDemand PersvoornaamIntegrationTest.dod;
    
    @Test
    public void PersvoornaamIntegrationTest.testCountPersvoornaams() {
        Assert.assertNotNull("Data on demand for 'Persvoornaam' failed to initialize correctly", dod.getRandomPersvoornaam());
        long count = Persvoornaam.countPersvoornaams();
        Assert.assertTrue("Counter for 'Persvoornaam' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersvoornaamIntegrationTest.testFindPersvoornaam() {
        Persvoornaam obj = dod.getRandomPersvoornaam();
        Assert.assertNotNull("Data on demand for 'Persvoornaam' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persvoornaam' failed to provide an identifier", id);
        obj = Persvoornaam.findPersvoornaam(id);
        Assert.assertNotNull("Find method for 'Persvoornaam' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Persvoornaam' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersvoornaamIntegrationTest.testFindAllPersvoornaams() {
        Assert.assertNotNull("Data on demand for 'Persvoornaam' failed to initialize correctly", dod.getRandomPersvoornaam());
        long count = Persvoornaam.countPersvoornaams();
        Assert.assertTrue("Too expensive to perform a find all test for 'Persvoornaam', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Persvoornaam> result = Persvoornaam.findAllPersvoornaams();
        Assert.assertNotNull("Find all method for 'Persvoornaam' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Persvoornaam' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersvoornaamIntegrationTest.testFindPersvoornaamEntries() {
        Assert.assertNotNull("Data on demand for 'Persvoornaam' failed to initialize correctly", dod.getRandomPersvoornaam());
        long count = Persvoornaam.countPersvoornaams();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Persvoornaam> result = Persvoornaam.findPersvoornaamEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Persvoornaam' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Persvoornaam' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersvoornaamIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Persvoornaam' failed to initialize correctly", dod.getRandomPersvoornaam());
        Persvoornaam obj = dod.getNewTransientPersvoornaam(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Persvoornaam' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Persvoornaam' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Persvoornaam' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersvoornaamIntegrationTest.testRemove() {
        Persvoornaam obj = dod.getRandomPersvoornaam();
        Assert.assertNotNull("Data on demand for 'Persvoornaam' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persvoornaam' failed to provide an identifier", id);
        obj = Persvoornaam.findPersvoornaam(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Persvoornaam' with identifier '" + id + "'", Persvoornaam.findPersvoornaam(id));
    }
    
}
