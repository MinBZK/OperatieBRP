// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Functieadres;
import nl.bzk.brp.model.data.kern.FunctieadresDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect FunctieadresDataOnDemand_Roo_DataOnDemand {
    
    declare @type: FunctieadresDataOnDemand: @Component;
    
    private Random FunctieadresDataOnDemand.rnd = new SecureRandom();
    
    private List<Functieadres> FunctieadresDataOnDemand.data;
    
    public Functieadres FunctieadresDataOnDemand.getNewTransientFunctieadres(int index) {
        Functieadres obj = new Functieadres();
        setCode(obj, index);
        setNaam(obj, index);
        return obj;
    }
    
    public void FunctieadresDataOnDemand.setCode(Functieadres obj, int index) {
        String code = String.valueOf(index);
        if (code.length() > 1) {
            code = new Random().nextInt(10) + code.substring(1, 1);
        }
        obj.setCode(code);
    }
    
    public void FunctieadresDataOnDemand.setNaam(Functieadres obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public Functieadres FunctieadresDataOnDemand.getSpecificFunctieadres(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Functieadres obj = data.get(index);
        Short id = obj.getId();
        return Functieadres.findFunctieadres(id);
    }
    
    public Functieadres FunctieadresDataOnDemand.getRandomFunctieadres() {
        init();
        Functieadres obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Functieadres.findFunctieadres(id);
    }
    
    public boolean FunctieadresDataOnDemand.modifyFunctieadres(Functieadres obj) {
        return false;
    }
    
    public void FunctieadresDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Functieadres.findFunctieadresEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Functieadres' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Functieadres>();
        for (int i = 0; i < 10; i++) {
            Functieadres obj = getNewTransientFunctieadres(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
