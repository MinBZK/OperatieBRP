// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Actie;
import nl.bzk.brp.model.data.kern.ActieDataOnDemand;
import nl.bzk.brp.model.data.kern.Adellijketitel;
import nl.bzk.brp.model.data.kern.AdellijketitelDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPersgeslnaamcomp;
import nl.bzk.brp.model.data.kern.HisPersgeslnaamcompDataOnDemand;
import nl.bzk.brp.model.data.kern.Persgeslnaamcomp;
import nl.bzk.brp.model.data.kern.PersgeslnaamcompDataOnDemand;
import nl.bzk.brp.model.data.kern.Predikaat;
import nl.bzk.brp.model.data.kern.PredikaatDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect HisPersgeslnaamcompDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HisPersgeslnaamcompDataOnDemand: @Component;
    
    private Random HisPersgeslnaamcompDataOnDemand.rnd = new SecureRandom();
    
    private List<HisPersgeslnaamcomp> HisPersgeslnaamcompDataOnDemand.data;
    
    @Autowired
    private ActieDataOnDemand HisPersgeslnaamcompDataOnDemand.actieDataOnDemand;
    
    @Autowired
    private AdellijketitelDataOnDemand HisPersgeslnaamcompDataOnDemand.adellijketitelDataOnDemand;
    
    @Autowired
    private PersgeslnaamcompDataOnDemand HisPersgeslnaamcompDataOnDemand.persgeslnaamcompDataOnDemand;
    
    @Autowired
    private PredikaatDataOnDemand HisPersgeslnaamcompDataOnDemand.predikaatDataOnDemand;
    
    public HisPersgeslnaamcomp HisPersgeslnaamcompDataOnDemand.getNewTransientHisPersgeslnaamcomp(int index) {
        HisPersgeslnaamcomp obj = new HisPersgeslnaamcomp();
        setActieaanpgel(obj, index);
        setActieinh(obj, index);
        setActieverval(obj, index);
        setAdellijketitel(obj, index);
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setNaam(obj, index);
        setPersgeslnaamcomp(obj, index);
        setPredikaat(obj, index);
        setScheidingsteken(obj, index);
        setTsreg(obj, index);
        setTsverval(obj, index);
        setVoorvoegsel(obj, index);
        return obj;
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setActieaanpgel(HisPersgeslnaamcomp obj, int index) {
        Actie actieaanpgel = actieDataOnDemand.getRandomActie();
        obj.setActieaanpgel(actieaanpgel);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setActieinh(HisPersgeslnaamcomp obj, int index) {
        Actie actieinh = actieDataOnDemand.getRandomActie();
        obj.setActieinh(actieinh);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setActieverval(HisPersgeslnaamcomp obj, int index) {
        Actie actieverval = actieDataOnDemand.getRandomActie();
        obj.setActieverval(actieverval);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setAdellijketitel(HisPersgeslnaamcomp obj, int index) {
        Adellijketitel adellijketitel = adellijketitelDataOnDemand.getRandomAdellijketitel();
        obj.setAdellijketitel(adellijketitel);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setDataanvgel(HisPersgeslnaamcomp obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setDateindegel(HisPersgeslnaamcomp obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setNaam(HisPersgeslnaamcomp obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 200) {
            naam = naam.substring(0, 200);
        }
        obj.setNaam(naam);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setPersgeslnaamcomp(HisPersgeslnaamcomp obj, int index) {
        Persgeslnaamcomp persgeslnaamcomp = persgeslnaamcompDataOnDemand.getRandomPersgeslnaamcomp();
        obj.setPersgeslnaamcomp(persgeslnaamcomp);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setPredikaat(HisPersgeslnaamcomp obj, int index) {
        Predikaat predikaat = predikaatDataOnDemand.getRandomPredikaat();
        obj.setPredikaat(predikaat);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setScheidingsteken(HisPersgeslnaamcomp obj, int index) {
        String scheidingsteken = String.valueOf(index);
        if (scheidingsteken.length() > 1) {
            scheidingsteken = scheidingsteken.substring(0, 1);
        }
        obj.setScheidingsteken(scheidingsteken);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setTsreg(HisPersgeslnaamcomp obj, int index) {
        Date tsreg = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsreg(tsreg);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setTsverval(HisPersgeslnaamcomp obj, int index) {
        Date tsverval = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsverval(tsverval);
    }
    
    public void HisPersgeslnaamcompDataOnDemand.setVoorvoegsel(HisPersgeslnaamcomp obj, int index) {
        String voorvoegsel = "voorvoeg_" + index;
        if (voorvoegsel.length() > 10) {
            voorvoegsel = voorvoegsel.substring(0, 10);
        }
        obj.setVoorvoegsel(voorvoegsel);
    }
    
    public HisPersgeslnaamcomp HisPersgeslnaamcompDataOnDemand.getSpecificHisPersgeslnaamcomp(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        HisPersgeslnaamcomp obj = data.get(index);
        Long id = obj.getId();
        return HisPersgeslnaamcomp.findHisPersgeslnaamcomp(id);
    }
    
    public HisPersgeslnaamcomp HisPersgeslnaamcompDataOnDemand.getRandomHisPersgeslnaamcomp() {
        init();
        HisPersgeslnaamcomp obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return HisPersgeslnaamcomp.findHisPersgeslnaamcomp(id);
    }
    
    public boolean HisPersgeslnaamcompDataOnDemand.modifyHisPersgeslnaamcomp(HisPersgeslnaamcomp obj) {
        return false;
    }
    
    public void HisPersgeslnaamcompDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = HisPersgeslnaamcomp.findHisPersgeslnaamcompEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'HisPersgeslnaamcomp' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<HisPersgeslnaamcomp>();
        for (int i = 0; i < 10; i++) {
            HisPersgeslnaamcomp obj = getNewTransientHisPersgeslnaamcomp(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
