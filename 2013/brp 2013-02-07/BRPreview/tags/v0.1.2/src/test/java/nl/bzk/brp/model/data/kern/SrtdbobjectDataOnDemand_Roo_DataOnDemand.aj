// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Srtdbobject;
import nl.bzk.brp.model.data.kern.SrtdbobjectDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect SrtdbobjectDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SrtdbobjectDataOnDemand: @Component;
    
    private Random SrtdbobjectDataOnDemand.rnd = new SecureRandom();
    
    private List<Srtdbobject> SrtdbobjectDataOnDemand.data;
    
    public Srtdbobject SrtdbobjectDataOnDemand.getNewTransientSrtdbobject(int index) {
        Srtdbobject obj = new Srtdbobject();
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setNaam(obj, index);
        return obj;
    }
    
    public void SrtdbobjectDataOnDemand.setDataanvgel(Srtdbobject obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void SrtdbobjectDataOnDemand.setDateindegel(Srtdbobject obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void SrtdbobjectDataOnDemand.setNaam(Srtdbobject obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = naam.substring(0, 40);
        }
        obj.setNaam(naam);
    }
    
    public Srtdbobject SrtdbobjectDataOnDemand.getSpecificSrtdbobject(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Srtdbobject obj = data.get(index);
        Short id = obj.getId();
        return Srtdbobject.findSrtdbobject(id);
    }
    
    public Srtdbobject SrtdbobjectDataOnDemand.getRandomSrtdbobject() {
        init();
        Srtdbobject obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Srtdbobject.findSrtdbobject(id);
    }
    
    public boolean SrtdbobjectDataOnDemand.modifySrtdbobject(Srtdbobject obj) {
        return false;
    }
    
    public void SrtdbobjectDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Srtdbobject.findSrtdbobjectEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Srtdbobject' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Srtdbobject>();
        for (int i = 0; i < 10; i++) {
            Srtdbobject obj = getNewTransientSrtdbobject(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
