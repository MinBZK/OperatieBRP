// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.Bijhautorisatie;
import nl.bzk.brp.model.data.autaut.BijhautorisatieDataOnDemand;
import nl.bzk.brp.model.data.autaut.BijhautorisatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect BijhautorisatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: BijhautorisatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: BijhautorisatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: BijhautorisatieIntegrationTest: @Transactional;
    
    @Autowired
    private BijhautorisatieDataOnDemand BijhautorisatieIntegrationTest.dod;
    
    @Test
    public void BijhautorisatieIntegrationTest.testCountBijhautorisaties() {
        Assert.assertNotNull("Data on demand for 'Bijhautorisatie' failed to initialize correctly", dod.getRandomBijhautorisatie());
        long count = Bijhautorisatie.countBijhautorisaties();
        Assert.assertTrue("Counter for 'Bijhautorisatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void BijhautorisatieIntegrationTest.testFindBijhautorisatie() {
        Bijhautorisatie obj = dod.getRandomBijhautorisatie();
        Assert.assertNotNull("Data on demand for 'Bijhautorisatie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Bijhautorisatie' failed to provide an identifier", id);
        obj = Bijhautorisatie.findBijhautorisatie(id);
        Assert.assertNotNull("Find method for 'Bijhautorisatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Bijhautorisatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void BijhautorisatieIntegrationTest.testFindAllBijhautorisaties() {
        Assert.assertNotNull("Data on demand for 'Bijhautorisatie' failed to initialize correctly", dod.getRandomBijhautorisatie());
        long count = Bijhautorisatie.countBijhautorisaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Bijhautorisatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Bijhautorisatie> result = Bijhautorisatie.findAllBijhautorisaties();
        Assert.assertNotNull("Find all method for 'Bijhautorisatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Bijhautorisatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void BijhautorisatieIntegrationTest.testFindBijhautorisatieEntries() {
        Assert.assertNotNull("Data on demand for 'Bijhautorisatie' failed to initialize correctly", dod.getRandomBijhautorisatie());
        long count = Bijhautorisatie.countBijhautorisaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Bijhautorisatie> result = Bijhautorisatie.findBijhautorisatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Bijhautorisatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Bijhautorisatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void BijhautorisatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Bijhautorisatie' failed to initialize correctly", dod.getRandomBijhautorisatie());
        Bijhautorisatie obj = dod.getNewTransientBijhautorisatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Bijhautorisatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Bijhautorisatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Bijhautorisatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void BijhautorisatieIntegrationTest.testRemove() {
        Bijhautorisatie obj = dod.getRandomBijhautorisatie();
        Assert.assertNotNull("Data on demand for 'Bijhautorisatie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Bijhautorisatie' failed to provide an identifier", id);
        obj = Bijhautorisatie.findBijhautorisatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Bijhautorisatie' with identifier '" + id + "'", Bijhautorisatie.findBijhautorisatie(id));
    }
    
}
