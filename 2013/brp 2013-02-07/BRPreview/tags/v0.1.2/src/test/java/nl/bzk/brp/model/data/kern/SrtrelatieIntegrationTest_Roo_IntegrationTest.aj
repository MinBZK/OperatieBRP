// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Srtrelatie;
import nl.bzk.brp.model.data.kern.SrtrelatieDataOnDemand;
import nl.bzk.brp.model.data.kern.SrtrelatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SrtrelatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SrtrelatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SrtrelatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SrtrelatieIntegrationTest: @Transactional;
    
    @Autowired
    private SrtrelatieDataOnDemand SrtrelatieIntegrationTest.dod;
    
    @Test
    public void SrtrelatieIntegrationTest.testCountSrtrelaties() {
        Assert.assertNotNull("Data on demand for 'Srtrelatie' failed to initialize correctly", dod.getRandomSrtrelatie());
        long count = Srtrelatie.countSrtrelaties();
        Assert.assertTrue("Counter for 'Srtrelatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SrtrelatieIntegrationTest.testFindSrtrelatie() {
        Srtrelatie obj = dod.getRandomSrtrelatie();
        Assert.assertNotNull("Data on demand for 'Srtrelatie' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtrelatie' failed to provide an identifier", id);
        obj = Srtrelatie.findSrtrelatie(id);
        Assert.assertNotNull("Find method for 'Srtrelatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Srtrelatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SrtrelatieIntegrationTest.testFindAllSrtrelaties() {
        Assert.assertNotNull("Data on demand for 'Srtrelatie' failed to initialize correctly", dod.getRandomSrtrelatie());
        long count = Srtrelatie.countSrtrelaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Srtrelatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Srtrelatie> result = Srtrelatie.findAllSrtrelaties();
        Assert.assertNotNull("Find all method for 'Srtrelatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Srtrelatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SrtrelatieIntegrationTest.testFindSrtrelatieEntries() {
        Assert.assertNotNull("Data on demand for 'Srtrelatie' failed to initialize correctly", dod.getRandomSrtrelatie());
        long count = Srtrelatie.countSrtrelaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Srtrelatie> result = Srtrelatie.findSrtrelatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Srtrelatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Srtrelatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SrtrelatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Srtrelatie' failed to initialize correctly", dod.getRandomSrtrelatie());
        Srtrelatie obj = dod.getNewTransientSrtrelatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Srtrelatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Srtrelatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Srtrelatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SrtrelatieIntegrationTest.testRemove() {
        Srtrelatie obj = dod.getRandomSrtrelatie();
        Assert.assertNotNull("Data on demand for 'Srtrelatie' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtrelatie' failed to provide an identifier", id);
        obj = Srtrelatie.findSrtrelatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Srtrelatie' with identifier '" + id + "'", Srtrelatie.findSrtrelatie(id));
    }
    
}
