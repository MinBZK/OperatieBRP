// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Geslachtsaand;
import nl.bzk.brp.model.data.kern.GeslachtsaandDataOnDemand;
import nl.bzk.brp.model.data.kern.GeslachtsaandIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect GeslachtsaandIntegrationTest_Roo_IntegrationTest {
    
    declare @type: GeslachtsaandIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: GeslachtsaandIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: GeslachtsaandIntegrationTest: @Transactional;
    
    @Autowired
    private GeslachtsaandDataOnDemand GeslachtsaandIntegrationTest.dod;
    
    @Test
    public void GeslachtsaandIntegrationTest.testCountGeslachtsaands() {
        Assert.assertNotNull("Data on demand for 'Geslachtsaand' failed to initialize correctly", dod.getRandomGeslachtsaand());
        long count = Geslachtsaand.countGeslachtsaands();
        Assert.assertTrue("Counter for 'Geslachtsaand' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void GeslachtsaandIntegrationTest.testFindGeslachtsaand() {
        Geslachtsaand obj = dod.getRandomGeslachtsaand();
        Assert.assertNotNull("Data on demand for 'Geslachtsaand' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Geslachtsaand' failed to provide an identifier", id);
        obj = Geslachtsaand.findGeslachtsaand(id);
        Assert.assertNotNull("Find method for 'Geslachtsaand' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Geslachtsaand' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void GeslachtsaandIntegrationTest.testFindAllGeslachtsaands() {
        Assert.assertNotNull("Data on demand for 'Geslachtsaand' failed to initialize correctly", dod.getRandomGeslachtsaand());
        long count = Geslachtsaand.countGeslachtsaands();
        Assert.assertTrue("Too expensive to perform a find all test for 'Geslachtsaand', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Geslachtsaand> result = Geslachtsaand.findAllGeslachtsaands();
        Assert.assertNotNull("Find all method for 'Geslachtsaand' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Geslachtsaand' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void GeslachtsaandIntegrationTest.testFindGeslachtsaandEntries() {
        Assert.assertNotNull("Data on demand for 'Geslachtsaand' failed to initialize correctly", dod.getRandomGeslachtsaand());
        long count = Geslachtsaand.countGeslachtsaands();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Geslachtsaand> result = Geslachtsaand.findGeslachtsaandEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Geslachtsaand' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Geslachtsaand' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void GeslachtsaandIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Geslachtsaand' failed to initialize correctly", dod.getRandomGeslachtsaand());
        Geslachtsaand obj = dod.getNewTransientGeslachtsaand(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Geslachtsaand' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Geslachtsaand' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Geslachtsaand' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void GeslachtsaandIntegrationTest.testRemove() {
        Geslachtsaand obj = dod.getRandomGeslachtsaand();
        Assert.assertNotNull("Data on demand for 'Geslachtsaand' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Geslachtsaand' failed to provide an identifier", id);
        obj = Geslachtsaand.findGeslachtsaand(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Geslachtsaand' with identifier '" + id + "'", Geslachtsaand.findGeslachtsaand(id));
    }
    
}
