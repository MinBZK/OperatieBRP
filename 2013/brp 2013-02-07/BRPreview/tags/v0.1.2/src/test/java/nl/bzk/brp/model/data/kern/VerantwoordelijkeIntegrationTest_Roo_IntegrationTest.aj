// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Verantwoordelijke;
import nl.bzk.brp.model.data.kern.VerantwoordelijkeDataOnDemand;
import nl.bzk.brp.model.data.kern.VerantwoordelijkeIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect VerantwoordelijkeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: VerantwoordelijkeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: VerantwoordelijkeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: VerantwoordelijkeIntegrationTest: @Transactional;
    
    @Autowired
    private VerantwoordelijkeDataOnDemand VerantwoordelijkeIntegrationTest.dod;
    
    @Test
    public void VerantwoordelijkeIntegrationTest.testCountVerantwoordelijkes() {
        Assert.assertNotNull("Data on demand for 'Verantwoordelijke' failed to initialize correctly", dod.getRandomVerantwoordelijke());
        long count = Verantwoordelijke.countVerantwoordelijkes();
        Assert.assertTrue("Counter for 'Verantwoordelijke' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void VerantwoordelijkeIntegrationTest.testFindVerantwoordelijke() {
        Verantwoordelijke obj = dod.getRandomVerantwoordelijke();
        Assert.assertNotNull("Data on demand for 'Verantwoordelijke' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Verantwoordelijke' failed to provide an identifier", id);
        obj = Verantwoordelijke.findVerantwoordelijke(id);
        Assert.assertNotNull("Find method for 'Verantwoordelijke' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Verantwoordelijke' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void VerantwoordelijkeIntegrationTest.testFindAllVerantwoordelijkes() {
        Assert.assertNotNull("Data on demand for 'Verantwoordelijke' failed to initialize correctly", dod.getRandomVerantwoordelijke());
        long count = Verantwoordelijke.countVerantwoordelijkes();
        Assert.assertTrue("Too expensive to perform a find all test for 'Verantwoordelijke', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Verantwoordelijke> result = Verantwoordelijke.findAllVerantwoordelijkes();
        Assert.assertNotNull("Find all method for 'Verantwoordelijke' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Verantwoordelijke' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void VerantwoordelijkeIntegrationTest.testFindVerantwoordelijkeEntries() {
        Assert.assertNotNull("Data on demand for 'Verantwoordelijke' failed to initialize correctly", dod.getRandomVerantwoordelijke());
        long count = Verantwoordelijke.countVerantwoordelijkes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Verantwoordelijke> result = Verantwoordelijke.findVerantwoordelijkeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Verantwoordelijke' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Verantwoordelijke' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void VerantwoordelijkeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Verantwoordelijke' failed to initialize correctly", dod.getRandomVerantwoordelijke());
        Verantwoordelijke obj = dod.getNewTransientVerantwoordelijke(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Verantwoordelijke' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Verantwoordelijke' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Verantwoordelijke' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void VerantwoordelijkeIntegrationTest.testRemove() {
        Verantwoordelijke obj = dod.getRandomVerantwoordelijke();
        Assert.assertNotNull("Data on demand for 'Verantwoordelijke' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Verantwoordelijke' failed to provide an identifier", id);
        obj = Verantwoordelijke.findVerantwoordelijke(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Verantwoordelijke' with identifier '" + id + "'", Verantwoordelijke.findVerantwoordelijke(id));
    }
    
}
