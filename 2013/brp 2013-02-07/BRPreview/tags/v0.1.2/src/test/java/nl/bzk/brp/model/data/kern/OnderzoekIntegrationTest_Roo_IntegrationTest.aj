// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Onderzoek;
import nl.bzk.brp.model.data.kern.OnderzoekDataOnDemand;
import nl.bzk.brp.model.data.kern.OnderzoekIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect OnderzoekIntegrationTest_Roo_IntegrationTest {
    
    declare @type: OnderzoekIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: OnderzoekIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: OnderzoekIntegrationTest: @Transactional;
    
    @Autowired
    private OnderzoekDataOnDemand OnderzoekIntegrationTest.dod;
    
    @Test
    public void OnderzoekIntegrationTest.testCountOnderzoeks() {
        Assert.assertNotNull("Data on demand for 'Onderzoek' failed to initialize correctly", dod.getRandomOnderzoek());
        long count = Onderzoek.countOnderzoeks();
        Assert.assertTrue("Counter for 'Onderzoek' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void OnderzoekIntegrationTest.testFindOnderzoek() {
        Onderzoek obj = dod.getRandomOnderzoek();
        Assert.assertNotNull("Data on demand for 'Onderzoek' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Onderzoek' failed to provide an identifier", id);
        obj = Onderzoek.findOnderzoek(id);
        Assert.assertNotNull("Find method for 'Onderzoek' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Onderzoek' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void OnderzoekIntegrationTest.testFindAllOnderzoeks() {
        Assert.assertNotNull("Data on demand for 'Onderzoek' failed to initialize correctly", dod.getRandomOnderzoek());
        long count = Onderzoek.countOnderzoeks();
        Assert.assertTrue("Too expensive to perform a find all test for 'Onderzoek', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Onderzoek> result = Onderzoek.findAllOnderzoeks();
        Assert.assertNotNull("Find all method for 'Onderzoek' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Onderzoek' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void OnderzoekIntegrationTest.testFindOnderzoekEntries() {
        Assert.assertNotNull("Data on demand for 'Onderzoek' failed to initialize correctly", dod.getRandomOnderzoek());
        long count = Onderzoek.countOnderzoeks();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Onderzoek> result = Onderzoek.findOnderzoekEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Onderzoek' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Onderzoek' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void OnderzoekIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Onderzoek' failed to initialize correctly", dod.getRandomOnderzoek());
        Onderzoek obj = dod.getNewTransientOnderzoek(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Onderzoek' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Onderzoek' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Onderzoek' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void OnderzoekIntegrationTest.testRemove() {
        Onderzoek obj = dod.getRandomOnderzoek();
        Assert.assertNotNull("Data on demand for 'Onderzoek' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Onderzoek' failed to provide an identifier", id);
        obj = Onderzoek.findOnderzoek(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Onderzoek' with identifier '" + id + "'", Onderzoek.findOnderzoek(id));
    }
    
}
