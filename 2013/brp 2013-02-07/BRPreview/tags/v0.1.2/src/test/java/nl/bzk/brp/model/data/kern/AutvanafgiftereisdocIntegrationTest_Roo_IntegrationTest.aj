// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Autvanafgiftereisdoc;
import nl.bzk.brp.model.data.kern.AutvanafgiftereisdocDataOnDemand;
import nl.bzk.brp.model.data.kern.AutvanafgiftereisdocIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AutvanafgiftereisdocIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AutvanafgiftereisdocIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AutvanafgiftereisdocIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AutvanafgiftereisdocIntegrationTest: @Transactional;
    
    @Autowired
    private AutvanafgiftereisdocDataOnDemand AutvanafgiftereisdocIntegrationTest.dod;
    
    @Test
    public void AutvanafgiftereisdocIntegrationTest.testCountAutvanafgiftereisdocs() {
        Assert.assertNotNull("Data on demand for 'Autvanafgiftereisdoc' failed to initialize correctly", dod.getRandomAutvanafgiftereisdoc());
        long count = Autvanafgiftereisdoc.countAutvanafgiftereisdocs();
        Assert.assertTrue("Counter for 'Autvanafgiftereisdoc' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AutvanafgiftereisdocIntegrationTest.testFindAutvanafgiftereisdoc() {
        Autvanafgiftereisdoc obj = dod.getRandomAutvanafgiftereisdoc();
        Assert.assertNotNull("Data on demand for 'Autvanafgiftereisdoc' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Autvanafgiftereisdoc' failed to provide an identifier", id);
        obj = Autvanafgiftereisdoc.findAutvanafgiftereisdoc(id);
        Assert.assertNotNull("Find method for 'Autvanafgiftereisdoc' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Autvanafgiftereisdoc' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AutvanafgiftereisdocIntegrationTest.testFindAllAutvanafgiftereisdocs() {
        Assert.assertNotNull("Data on demand for 'Autvanafgiftereisdoc' failed to initialize correctly", dod.getRandomAutvanafgiftereisdoc());
        long count = Autvanafgiftereisdoc.countAutvanafgiftereisdocs();
        Assert.assertTrue("Too expensive to perform a find all test for 'Autvanafgiftereisdoc', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Autvanafgiftereisdoc> result = Autvanafgiftereisdoc.findAllAutvanafgiftereisdocs();
        Assert.assertNotNull("Find all method for 'Autvanafgiftereisdoc' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Autvanafgiftereisdoc' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AutvanafgiftereisdocIntegrationTest.testFindAutvanafgiftereisdocEntries() {
        Assert.assertNotNull("Data on demand for 'Autvanafgiftereisdoc' failed to initialize correctly", dod.getRandomAutvanafgiftereisdoc());
        long count = Autvanafgiftereisdoc.countAutvanafgiftereisdocs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Autvanafgiftereisdoc> result = Autvanafgiftereisdoc.findAutvanafgiftereisdocEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Autvanafgiftereisdoc' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Autvanafgiftereisdoc' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AutvanafgiftereisdocIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Autvanafgiftereisdoc' failed to initialize correctly", dod.getRandomAutvanafgiftereisdoc());
        Autvanafgiftereisdoc obj = dod.getNewTransientAutvanafgiftereisdoc(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Autvanafgiftereisdoc' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Autvanafgiftereisdoc' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Autvanafgiftereisdoc' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AutvanafgiftereisdocIntegrationTest.testRemove() {
        Autvanafgiftereisdoc obj = dod.getRandomAutvanafgiftereisdoc();
        Assert.assertNotNull("Data on demand for 'Autvanafgiftereisdoc' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Autvanafgiftereisdoc' failed to provide an identifier", id);
        obj = Autvanafgiftereisdoc.findAutvanafgiftereisdoc(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Autvanafgiftereisdoc' with identifier '" + id + "'", Autvanafgiftereisdoc.findAutvanafgiftereisdoc(id));
    }
    
}
