// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.brm;

import java.util.List;
import nl.bzk.brp.model.data.brm.Srtregel;
import nl.bzk.brp.model.data.brm.SrtregelDataOnDemand;
import nl.bzk.brp.model.data.brm.SrtregelIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SrtregelIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SrtregelIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SrtregelIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SrtregelIntegrationTest: @Transactional;
    
    @Autowired
    private SrtregelDataOnDemand SrtregelIntegrationTest.dod;
    
    @Test
    public void SrtregelIntegrationTest.testCountSrtregels() {
        Assert.assertNotNull("Data on demand for 'Srtregel' failed to initialize correctly", dod.getRandomSrtregel());
        long count = Srtregel.countSrtregels();
        Assert.assertTrue("Counter for 'Srtregel' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SrtregelIntegrationTest.testFindSrtregel() {
        Srtregel obj = dod.getRandomSrtregel();
        Assert.assertNotNull("Data on demand for 'Srtregel' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtregel' failed to provide an identifier", id);
        obj = Srtregel.findSrtregel(id);
        Assert.assertNotNull("Find method for 'Srtregel' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Srtregel' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SrtregelIntegrationTest.testFindAllSrtregels() {
        Assert.assertNotNull("Data on demand for 'Srtregel' failed to initialize correctly", dod.getRandomSrtregel());
        long count = Srtregel.countSrtregels();
        Assert.assertTrue("Too expensive to perform a find all test for 'Srtregel', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Srtregel> result = Srtregel.findAllSrtregels();
        Assert.assertNotNull("Find all method for 'Srtregel' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Srtregel' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SrtregelIntegrationTest.testFindSrtregelEntries() {
        Assert.assertNotNull("Data on demand for 'Srtregel' failed to initialize correctly", dod.getRandomSrtregel());
        long count = Srtregel.countSrtregels();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Srtregel> result = Srtregel.findSrtregelEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Srtregel' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Srtregel' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SrtregelIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Srtregel' failed to initialize correctly", dod.getRandomSrtregel());
        Srtregel obj = dod.getNewTransientSrtregel(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Srtregel' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Srtregel' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Srtregel' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SrtregelIntegrationTest.testRemove() {
        Srtregel obj = dod.getRandomSrtregel();
        Assert.assertNotNull("Data on demand for 'Srtregel' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtregel' failed to provide an identifier", id);
        obj = Srtregel.findSrtregel(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Srtregel' with identifier '" + id + "'", Srtregel.findSrtregel(id));
    }
    
}
