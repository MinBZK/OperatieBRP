// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Rdnbeeindrelatie;
import nl.bzk.brp.model.data.kern.RdnbeeindrelatieDataOnDemand;
import nl.bzk.brp.model.data.kern.RdnbeeindrelatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect RdnbeeindrelatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: RdnbeeindrelatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: RdnbeeindrelatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: RdnbeeindrelatieIntegrationTest: @Transactional;
    
    @Autowired
    private RdnbeeindrelatieDataOnDemand RdnbeeindrelatieIntegrationTest.dod;
    
    @Test
    public void RdnbeeindrelatieIntegrationTest.testCountRdnbeeindrelaties() {
        Assert.assertNotNull("Data on demand for 'Rdnbeeindrelatie' failed to initialize correctly", dod.getRandomRdnbeeindrelatie());
        long count = Rdnbeeindrelatie.countRdnbeeindrelaties();
        Assert.assertTrue("Counter for 'Rdnbeeindrelatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void RdnbeeindrelatieIntegrationTest.testFindRdnbeeindrelatie() {
        Rdnbeeindrelatie obj = dod.getRandomRdnbeeindrelatie();
        Assert.assertNotNull("Data on demand for 'Rdnbeeindrelatie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Rdnbeeindrelatie' failed to provide an identifier", id);
        obj = Rdnbeeindrelatie.findRdnbeeindrelatie(id);
        Assert.assertNotNull("Find method for 'Rdnbeeindrelatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Rdnbeeindrelatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void RdnbeeindrelatieIntegrationTest.testFindAllRdnbeeindrelaties() {
        Assert.assertNotNull("Data on demand for 'Rdnbeeindrelatie' failed to initialize correctly", dod.getRandomRdnbeeindrelatie());
        long count = Rdnbeeindrelatie.countRdnbeeindrelaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Rdnbeeindrelatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Rdnbeeindrelatie> result = Rdnbeeindrelatie.findAllRdnbeeindrelaties();
        Assert.assertNotNull("Find all method for 'Rdnbeeindrelatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Rdnbeeindrelatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void RdnbeeindrelatieIntegrationTest.testFindRdnbeeindrelatieEntries() {
        Assert.assertNotNull("Data on demand for 'Rdnbeeindrelatie' failed to initialize correctly", dod.getRandomRdnbeeindrelatie());
        long count = Rdnbeeindrelatie.countRdnbeeindrelaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Rdnbeeindrelatie> result = Rdnbeeindrelatie.findRdnbeeindrelatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Rdnbeeindrelatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Rdnbeeindrelatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void RdnbeeindrelatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Rdnbeeindrelatie' failed to initialize correctly", dod.getRandomRdnbeeindrelatie());
        Rdnbeeindrelatie obj = dod.getNewTransientRdnbeeindrelatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Rdnbeeindrelatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Rdnbeeindrelatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Rdnbeeindrelatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void RdnbeeindrelatieIntegrationTest.testRemove() {
        Rdnbeeindrelatie obj = dod.getRandomRdnbeeindrelatie();
        Assert.assertNotNull("Data on demand for 'Rdnbeeindrelatie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Rdnbeeindrelatie' failed to provide an identifier", id);
        obj = Rdnbeeindrelatie.findRdnbeeindrelatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Rdnbeeindrelatie' with identifier '" + id + "'", Rdnbeeindrelatie.findRdnbeeindrelatie(id));
    }
    
}
