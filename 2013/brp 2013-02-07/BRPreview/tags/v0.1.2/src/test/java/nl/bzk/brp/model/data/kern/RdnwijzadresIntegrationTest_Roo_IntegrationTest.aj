// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Rdnwijzadres;
import nl.bzk.brp.model.data.kern.RdnwijzadresDataOnDemand;
import nl.bzk.brp.model.data.kern.RdnwijzadresIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect RdnwijzadresIntegrationTest_Roo_IntegrationTest {
    
    declare @type: RdnwijzadresIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: RdnwijzadresIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: RdnwijzadresIntegrationTest: @Transactional;
    
    @Autowired
    private RdnwijzadresDataOnDemand RdnwijzadresIntegrationTest.dod;
    
    @Test
    public void RdnwijzadresIntegrationTest.testCountRdnwijzadreses() {
        Assert.assertNotNull("Data on demand for 'Rdnwijzadres' failed to initialize correctly", dod.getRandomRdnwijzadres());
        long count = Rdnwijzadres.countRdnwijzadreses();
        Assert.assertTrue("Counter for 'Rdnwijzadres' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void RdnwijzadresIntegrationTest.testFindRdnwijzadres() {
        Rdnwijzadres obj = dod.getRandomRdnwijzadres();
        Assert.assertNotNull("Data on demand for 'Rdnwijzadres' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Rdnwijzadres' failed to provide an identifier", id);
        obj = Rdnwijzadres.findRdnwijzadres(id);
        Assert.assertNotNull("Find method for 'Rdnwijzadres' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Rdnwijzadres' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void RdnwijzadresIntegrationTest.testFindAllRdnwijzadreses() {
        Assert.assertNotNull("Data on demand for 'Rdnwijzadres' failed to initialize correctly", dod.getRandomRdnwijzadres());
        long count = Rdnwijzadres.countRdnwijzadreses();
        Assert.assertTrue("Too expensive to perform a find all test for 'Rdnwijzadres', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Rdnwijzadres> result = Rdnwijzadres.findAllRdnwijzadreses();
        Assert.assertNotNull("Find all method for 'Rdnwijzadres' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Rdnwijzadres' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void RdnwijzadresIntegrationTest.testFindRdnwijzadresEntries() {
        Assert.assertNotNull("Data on demand for 'Rdnwijzadres' failed to initialize correctly", dod.getRandomRdnwijzadres());
        long count = Rdnwijzadres.countRdnwijzadreses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Rdnwijzadres> result = Rdnwijzadres.findRdnwijzadresEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Rdnwijzadres' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Rdnwijzadres' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void RdnwijzadresIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Rdnwijzadres' failed to initialize correctly", dod.getRandomRdnwijzadres());
        Rdnwijzadres obj = dod.getNewTransientRdnwijzadres(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Rdnwijzadres' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Rdnwijzadres' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Rdnwijzadres' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void RdnwijzadresIntegrationTest.testRemove() {
        Rdnwijzadres obj = dod.getRandomRdnwijzadres();
        Assert.assertNotNull("Data on demand for 'Rdnwijzadres' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Rdnwijzadres' failed to provide an identifier", id);
        obj = Rdnwijzadres.findRdnwijzadres(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Rdnwijzadres' with identifier '" + id + "'", Rdnwijzadres.findRdnwijzadres(id));
    }
    
}
