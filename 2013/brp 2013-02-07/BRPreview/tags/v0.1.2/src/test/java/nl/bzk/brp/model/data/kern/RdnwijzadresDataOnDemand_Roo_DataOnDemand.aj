// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Rdnwijzadres;
import nl.bzk.brp.model.data.kern.RdnwijzadresDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect RdnwijzadresDataOnDemand_Roo_DataOnDemand {
    
    declare @type: RdnwijzadresDataOnDemand: @Component;
    
    private Random RdnwijzadresDataOnDemand.rnd = new SecureRandom();
    
    private List<Rdnwijzadres> RdnwijzadresDataOnDemand.data;
    
    public Rdnwijzadres RdnwijzadresDataOnDemand.getNewTransientRdnwijzadres(int index) {
        Rdnwijzadres obj = new Rdnwijzadres();
        setCode(obj, index);
        setNaam(obj, index);
        return obj;
    }
    
    public void RdnwijzadresDataOnDemand.setCode(Rdnwijzadres obj, int index) {
        String code = String.valueOf(index);
        if (code.length() > 1) {
            code = new Random().nextInt(10) + code.substring(1, 1);
        }
        obj.setCode(code);
    }
    
    public void RdnwijzadresDataOnDemand.setNaam(Rdnwijzadres obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public Rdnwijzadres RdnwijzadresDataOnDemand.getSpecificRdnwijzadres(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Rdnwijzadres obj = data.get(index);
        Short id = obj.getId();
        return Rdnwijzadres.findRdnwijzadres(id);
    }
    
    public Rdnwijzadres RdnwijzadresDataOnDemand.getRandomRdnwijzadres() {
        init();
        Rdnwijzadres obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Rdnwijzadres.findRdnwijzadres(id);
    }
    
    public boolean RdnwijzadresDataOnDemand.modifyRdnwijzadres(Rdnwijzadres obj) {
        return false;
    }
    
    public void RdnwijzadresDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Rdnwijzadres.findRdnwijzadresEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Rdnwijzadres' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Rdnwijzadres>();
        for (int i = 0; i < 10; i++) {
            Rdnwijzadres obj = getNewTransientRdnwijzadres(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
