// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.Beperkingpopulatie;
import nl.bzk.brp.model.data.autaut.BeperkingpopulatieDataOnDemand;
import nl.bzk.brp.model.data.autaut.BeperkingpopulatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect BeperkingpopulatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: BeperkingpopulatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: BeperkingpopulatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: BeperkingpopulatieIntegrationTest: @Transactional;
    
    @Autowired
    private BeperkingpopulatieDataOnDemand BeperkingpopulatieIntegrationTest.dod;
    
    @Test
    public void BeperkingpopulatieIntegrationTest.testCountBeperkingpopulaties() {
        Assert.assertNotNull("Data on demand for 'Beperkingpopulatie' failed to initialize correctly", dod.getRandomBeperkingpopulatie());
        long count = Beperkingpopulatie.countBeperkingpopulaties();
        Assert.assertTrue("Counter for 'Beperkingpopulatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void BeperkingpopulatieIntegrationTest.testFindBeperkingpopulatie() {
        Beperkingpopulatie obj = dod.getRandomBeperkingpopulatie();
        Assert.assertNotNull("Data on demand for 'Beperkingpopulatie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Beperkingpopulatie' failed to provide an identifier", id);
        obj = Beperkingpopulatie.findBeperkingpopulatie(id);
        Assert.assertNotNull("Find method for 'Beperkingpopulatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Beperkingpopulatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void BeperkingpopulatieIntegrationTest.testFindAllBeperkingpopulaties() {
        Assert.assertNotNull("Data on demand for 'Beperkingpopulatie' failed to initialize correctly", dod.getRandomBeperkingpopulatie());
        long count = Beperkingpopulatie.countBeperkingpopulaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Beperkingpopulatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Beperkingpopulatie> result = Beperkingpopulatie.findAllBeperkingpopulaties();
        Assert.assertNotNull("Find all method for 'Beperkingpopulatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Beperkingpopulatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void BeperkingpopulatieIntegrationTest.testFindBeperkingpopulatieEntries() {
        Assert.assertNotNull("Data on demand for 'Beperkingpopulatie' failed to initialize correctly", dod.getRandomBeperkingpopulatie());
        long count = Beperkingpopulatie.countBeperkingpopulaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Beperkingpopulatie> result = Beperkingpopulatie.findBeperkingpopulatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Beperkingpopulatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Beperkingpopulatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void BeperkingpopulatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Beperkingpopulatie' failed to initialize correctly", dod.getRandomBeperkingpopulatie());
        Beperkingpopulatie obj = dod.getNewTransientBeperkingpopulatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Beperkingpopulatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Beperkingpopulatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Beperkingpopulatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void BeperkingpopulatieIntegrationTest.testRemove() {
        Beperkingpopulatie obj = dod.getRandomBeperkingpopulatie();
        Assert.assertNotNull("Data on demand for 'Beperkingpopulatie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Beperkingpopulatie' failed to provide an identifier", id);
        obj = Beperkingpopulatie.findBeperkingpopulatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Beperkingpopulatie' with identifier '" + id + "'", Beperkingpopulatie.findBeperkingpopulatie(id));
    }
    
}
