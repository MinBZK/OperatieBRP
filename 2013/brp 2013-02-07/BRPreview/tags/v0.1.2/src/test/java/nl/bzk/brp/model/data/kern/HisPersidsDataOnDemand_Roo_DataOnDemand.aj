// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Actie;
import nl.bzk.brp.model.data.kern.ActieDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPersids;
import nl.bzk.brp.model.data.kern.HisPersidsDataOnDemand;
import nl.bzk.brp.model.data.kern.Pers;
import nl.bzk.brp.model.data.kern.PersDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect HisPersidsDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HisPersidsDataOnDemand: @Component;
    
    private Random HisPersidsDataOnDemand.rnd = new SecureRandom();
    
    private List<HisPersids> HisPersidsDataOnDemand.data;
    
    @Autowired
    private ActieDataOnDemand HisPersidsDataOnDemand.actieDataOnDemand;
    
    @Autowired
    private PersDataOnDemand HisPersidsDataOnDemand.persDataOnDemand;
    
    public HisPersids HisPersidsDataOnDemand.getNewTransientHisPersids(int index) {
        HisPersids obj = new HisPersids();
        setActieaanpgel(obj, index);
        setActieinh(obj, index);
        setActieverval(obj, index);
        setAnr(obj, index);
        setBsn(obj, index);
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setPers(obj, index);
        setTsreg(obj, index);
        setTsverval(obj, index);
        return obj;
    }
    
    public void HisPersidsDataOnDemand.setActieaanpgel(HisPersids obj, int index) {
        Actie actieaanpgel = actieDataOnDemand.getRandomActie();
        obj.setActieaanpgel(actieaanpgel);
    }
    
    public void HisPersidsDataOnDemand.setActieinh(HisPersids obj, int index) {
        Actie actieinh = actieDataOnDemand.getRandomActie();
        obj.setActieinh(actieinh);
    }
    
    public void HisPersidsDataOnDemand.setActieverval(HisPersids obj, int index) {
        Actie actieverval = actieDataOnDemand.getRandomActie();
        obj.setActieverval(actieverval);
    }
    
    public void HisPersidsDataOnDemand.setAnr(HisPersids obj, int index) {
        String anr = "anr_" + index;
        if (anr.length() > 10) {
            anr = anr.substring(0, 10);
        }
        obj.setAnr(anr);
    }
    
    public void HisPersidsDataOnDemand.setBsn(HisPersids obj, int index) {
        String bsn = "bsn_" + index;
        if (bsn.length() > 9) {
            bsn = bsn.substring(0, 9);
        }
        obj.setBsn(bsn);
    }
    
    public void HisPersidsDataOnDemand.setDataanvgel(HisPersids obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void HisPersidsDataOnDemand.setDateindegel(HisPersids obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void HisPersidsDataOnDemand.setPers(HisPersids obj, int index) {
        Pers pers = persDataOnDemand.getRandomPers();
        obj.setPers(pers);
    }
    
    public void HisPersidsDataOnDemand.setTsreg(HisPersids obj, int index) {
        Date tsreg = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsreg(tsreg);
    }
    
    public void HisPersidsDataOnDemand.setTsverval(HisPersids obj, int index) {
        Date tsverval = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsverval(tsverval);
    }
    
    public HisPersids HisPersidsDataOnDemand.getSpecificHisPersids(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        HisPersids obj = data.get(index);
        Long id = obj.getId();
        return HisPersids.findHisPersids(id);
    }
    
    public HisPersids HisPersidsDataOnDemand.getRandomHisPersids() {
        init();
        HisPersids obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return HisPersids.findHisPersids(id);
    }
    
    public boolean HisPersidsDataOnDemand.modifyHisPersids(HisPersids obj) {
        return false;
    }
    
    public void HisPersidsDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = HisPersids.findHisPersidsEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'HisPersids' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<HisPersids>();
        for (int i = 0; i < 10; i++) {
            HisPersids obj = getNewTransientHisPersids(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
