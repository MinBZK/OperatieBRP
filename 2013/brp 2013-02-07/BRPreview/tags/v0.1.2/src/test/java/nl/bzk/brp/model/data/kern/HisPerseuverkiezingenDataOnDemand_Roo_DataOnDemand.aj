// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Actie;
import nl.bzk.brp.model.data.kern.ActieDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPerseuverkiezingen;
import nl.bzk.brp.model.data.kern.HisPerseuverkiezingenDataOnDemand;
import nl.bzk.brp.model.data.kern.Pers;
import nl.bzk.brp.model.data.kern.PersDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect HisPerseuverkiezingenDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HisPerseuverkiezingenDataOnDemand: @Component;
    
    private Random HisPerseuverkiezingenDataOnDemand.rnd = new SecureRandom();
    
    private List<HisPerseuverkiezingen> HisPerseuverkiezingenDataOnDemand.data;
    
    @Autowired
    private ActieDataOnDemand HisPerseuverkiezingenDataOnDemand.actieDataOnDemand;
    
    @Autowired
    private PersDataOnDemand HisPerseuverkiezingenDataOnDemand.persDataOnDemand;
    
    public HisPerseuverkiezingen HisPerseuverkiezingenDataOnDemand.getNewTransientHisPerseuverkiezingen(int index) {
        HisPerseuverkiezingen obj = new HisPerseuverkiezingen();
        setActieinh(obj, index);
        setActieverval(obj, index);
        setDataanlaanpdeelneuverkiezing(obj, index);
        setDateindeuitsleukiesr(obj, index);
        setInddeelneuverkiezingen(obj, index);
        setPers(obj, index);
        setTsreg(obj, index);
        setTsverval(obj, index);
        return obj;
    }
    
    public void HisPerseuverkiezingenDataOnDemand.setActieinh(HisPerseuverkiezingen obj, int index) {
        Actie actieinh = actieDataOnDemand.getRandomActie();
        obj.setActieinh(actieinh);
    }
    
    public void HisPerseuverkiezingenDataOnDemand.setActieverval(HisPerseuverkiezingen obj, int index) {
        Actie actieverval = actieDataOnDemand.getRandomActie();
        obj.setActieverval(actieverval);
    }
    
    public void HisPerseuverkiezingenDataOnDemand.setDataanlaanpdeelneuverkiezing(HisPerseuverkiezingen obj, int index) {
        BigDecimal dataanlaanpdeelneuverkiezing = BigDecimal.valueOf(index);
        obj.setDataanlaanpdeelneuverkiezing(dataanlaanpdeelneuverkiezing);
    }
    
    public void HisPerseuverkiezingenDataOnDemand.setDateindeuitsleukiesr(HisPerseuverkiezingen obj, int index) {
        BigDecimal dateindeuitsleukiesr = BigDecimal.valueOf(index);
        obj.setDateindeuitsleukiesr(dateindeuitsleukiesr);
    }
    
    public void HisPerseuverkiezingenDataOnDemand.setInddeelneuverkiezingen(HisPerseuverkiezingen obj, int index) {
        Boolean inddeelneuverkiezingen = true;
        obj.setInddeelneuverkiezingen(inddeelneuverkiezingen);
    }
    
    public void HisPerseuverkiezingenDataOnDemand.setPers(HisPerseuverkiezingen obj, int index) {
        Pers pers = persDataOnDemand.getRandomPers();
        obj.setPers(pers);
    }
    
    public void HisPerseuverkiezingenDataOnDemand.setTsreg(HisPerseuverkiezingen obj, int index) {
        Date tsreg = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsreg(tsreg);
    }
    
    public void HisPerseuverkiezingenDataOnDemand.setTsverval(HisPerseuverkiezingen obj, int index) {
        Date tsverval = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsverval(tsverval);
    }
    
    public HisPerseuverkiezingen HisPerseuverkiezingenDataOnDemand.getSpecificHisPerseuverkiezingen(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        HisPerseuverkiezingen obj = data.get(index);
        Long id = obj.getId();
        return HisPerseuverkiezingen.findHisPerseuverkiezingen(id);
    }
    
    public HisPerseuverkiezingen HisPerseuverkiezingenDataOnDemand.getRandomHisPerseuverkiezingen() {
        init();
        HisPerseuverkiezingen obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return HisPerseuverkiezingen.findHisPerseuverkiezingen(id);
    }
    
    public boolean HisPerseuverkiezingenDataOnDemand.modifyHisPerseuverkiezingen(HisPerseuverkiezingen obj) {
        return false;
    }
    
    public void HisPerseuverkiezingenDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = HisPerseuverkiezingen.findHisPerseuverkiezingenEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'HisPerseuverkiezingen' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<HisPerseuverkiezingen>();
        for (int i = 0; i < 10; i++) {
            HisPerseuverkiezingen obj = getNewTransientHisPerseuverkiezingen(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
