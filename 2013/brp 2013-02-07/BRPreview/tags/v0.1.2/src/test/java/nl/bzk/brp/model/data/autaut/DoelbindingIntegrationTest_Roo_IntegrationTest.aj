// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.Doelbinding;
import nl.bzk.brp.model.data.autaut.DoelbindingDataOnDemand;
import nl.bzk.brp.model.data.autaut.DoelbindingIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DoelbindingIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DoelbindingIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DoelbindingIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DoelbindingIntegrationTest: @Transactional;
    
    @Autowired
    private DoelbindingDataOnDemand DoelbindingIntegrationTest.dod;
    
    @Test
    public void DoelbindingIntegrationTest.testCountDoelbindings() {
        Assert.assertNotNull("Data on demand for 'Doelbinding' failed to initialize correctly", dod.getRandomDoelbinding());
        long count = Doelbinding.countDoelbindings();
        Assert.assertTrue("Counter for 'Doelbinding' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DoelbindingIntegrationTest.testFindDoelbinding() {
        Doelbinding obj = dod.getRandomDoelbinding();
        Assert.assertNotNull("Data on demand for 'Doelbinding' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Doelbinding' failed to provide an identifier", id);
        obj = Doelbinding.findDoelbinding(id);
        Assert.assertNotNull("Find method for 'Doelbinding' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Doelbinding' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DoelbindingIntegrationTest.testFindAllDoelbindings() {
        Assert.assertNotNull("Data on demand for 'Doelbinding' failed to initialize correctly", dod.getRandomDoelbinding());
        long count = Doelbinding.countDoelbindings();
        Assert.assertTrue("Too expensive to perform a find all test for 'Doelbinding', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Doelbinding> result = Doelbinding.findAllDoelbindings();
        Assert.assertNotNull("Find all method for 'Doelbinding' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Doelbinding' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DoelbindingIntegrationTest.testFindDoelbindingEntries() {
        Assert.assertNotNull("Data on demand for 'Doelbinding' failed to initialize correctly", dod.getRandomDoelbinding());
        long count = Doelbinding.countDoelbindings();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Doelbinding> result = Doelbinding.findDoelbindingEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Doelbinding' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Doelbinding' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DoelbindingIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Doelbinding' failed to initialize correctly", dod.getRandomDoelbinding());
        Doelbinding obj = dod.getNewTransientDoelbinding(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Doelbinding' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Doelbinding' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Doelbinding' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DoelbindingIntegrationTest.testRemove() {
        Doelbinding obj = dod.getRandomDoelbinding();
        Assert.assertNotNull("Data on demand for 'Doelbinding' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Doelbinding' failed to provide an identifier", id);
        obj = Doelbinding.findDoelbinding(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Doelbinding' with identifier '" + id + "'", Doelbinding.findDoelbinding(id));
    }
    
}
