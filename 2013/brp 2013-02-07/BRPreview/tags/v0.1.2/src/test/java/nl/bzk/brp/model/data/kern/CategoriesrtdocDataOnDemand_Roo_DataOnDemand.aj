// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Categoriesrtdoc;
import nl.bzk.brp.model.data.kern.CategoriesrtdocDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect CategoriesrtdocDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CategoriesrtdocDataOnDemand: @Component;
    
    private Random CategoriesrtdocDataOnDemand.rnd = new SecureRandom();
    
    private List<Categoriesrtdoc> CategoriesrtdocDataOnDemand.data;
    
    public Categoriesrtdoc CategoriesrtdocDataOnDemand.getNewTransientCategoriesrtdoc(int index) {
        Categoriesrtdoc obj = new Categoriesrtdoc();
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setNaam(obj, index);
        setOms(obj, index);
        return obj;
    }
    
    public void CategoriesrtdocDataOnDemand.setDataanvgel(Categoriesrtdoc obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void CategoriesrtdocDataOnDemand.setDateindegel(Categoriesrtdoc obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void CategoriesrtdocDataOnDemand.setNaam(Categoriesrtdoc obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public void CategoriesrtdocDataOnDemand.setOms(Categoriesrtdoc obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = new Random().nextInt(10) + oms.substring(1, 250);
        }
        obj.setOms(oms);
    }
    
    public Categoriesrtdoc CategoriesrtdocDataOnDemand.getSpecificCategoriesrtdoc(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Categoriesrtdoc obj = data.get(index);
        Short id = obj.getId();
        return Categoriesrtdoc.findCategoriesrtdoc(id);
    }
    
    public Categoriesrtdoc CategoriesrtdocDataOnDemand.getRandomCategoriesrtdoc() {
        init();
        Categoriesrtdoc obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Categoriesrtdoc.findCategoriesrtdoc(id);
    }
    
    public boolean CategoriesrtdocDataOnDemand.modifyCategoriesrtdoc(Categoriesrtdoc obj) {
        return false;
    }
    
    public void CategoriesrtdocDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Categoriesrtdoc.findCategoriesrtdocEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Categoriesrtdoc' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Categoriesrtdoc>();
        for (int i = 0; i < 10; i++) {
            Categoriesrtdoc obj = getNewTransientCategoriesrtdoc(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
