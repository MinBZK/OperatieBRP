// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Srtpers;
import nl.bzk.brp.model.data.kern.SrtpersDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect SrtpersDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SrtpersDataOnDemand: @Component;
    
    private Random SrtpersDataOnDemand.rnd = new SecureRandom();
    
    private List<Srtpers> SrtpersDataOnDemand.data;
    
    public Srtpers SrtpersDataOnDemand.getNewTransientSrtpers(int index) {
        Srtpers obj = new Srtpers();
        setCode(obj, index);
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setNaam(obj, index);
        setOms(obj, index);
        return obj;
    }
    
    public void SrtpersDataOnDemand.setCode(Srtpers obj, int index) {
        String code = String.valueOf(index);
        if (code.length() > 1) {
            code = new Random().nextInt(10) + code.substring(1, 1);
        }
        obj.setCode(code);
    }
    
    public void SrtpersDataOnDemand.setDataanvgel(Srtpers obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void SrtpersDataOnDemand.setDateindegel(Srtpers obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void SrtpersDataOnDemand.setNaam(Srtpers obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public void SrtpersDataOnDemand.setOms(Srtpers obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = oms.substring(0, 250);
        }
        obj.setOms(oms);
    }
    
    public Srtpers SrtpersDataOnDemand.getSpecificSrtpers(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Srtpers obj = data.get(index);
        Short id = obj.getId();
        return Srtpers.findSrtpers(id);
    }
    
    public Srtpers SrtpersDataOnDemand.getRandomSrtpers() {
        init();
        Srtpers obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Srtpers.findSrtpers(id);
    }
    
    public boolean SrtpersDataOnDemand.modifySrtpers(Srtpers obj) {
        return false;
    }
    
    public void SrtpersDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Srtpers.findSrtpersEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Srtpers' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Srtpers>();
        for (int i = 0; i < 10; i++) {
            Srtpers obj = getNewTransientSrtpers(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
