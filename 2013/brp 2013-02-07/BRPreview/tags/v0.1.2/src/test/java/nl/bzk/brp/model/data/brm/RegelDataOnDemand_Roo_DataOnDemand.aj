// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.brm;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.brm.Regel;
import nl.bzk.brp.model.data.brm.RegelDataOnDemand;
import nl.bzk.brp.model.data.brm.Srtregel;
import nl.bzk.brp.model.data.brm.SrtregelDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect RegelDataOnDemand_Roo_DataOnDemand {
    
    declare @type: RegelDataOnDemand: @Component;
    
    private Random RegelDataOnDemand.rnd = new SecureRandom();
    
    private List<Regel> RegelDataOnDemand.data;
    
    @Autowired
    private SrtregelDataOnDemand RegelDataOnDemand.srtregelDataOnDemand;
    
    public Regel RegelDataOnDemand.getNewTransientRegel(int index) {
        Regel obj = new Regel();
        setCode(obj, index);
        setOms(obj, index);
        setSpecificatie(obj, index);
        setSrt(obj, index);
        return obj;
    }
    
    public void RegelDataOnDemand.setCode(Regel obj, int index) {
        String code = "code_" + index;
        if (code.length() > 40) {
            code = new Random().nextInt(10) + code.substring(1, 40);
        }
        obj.setCode(code);
    }
    
    public void RegelDataOnDemand.setOms(Regel obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = new Random().nextInt(10) + oms.substring(1, 250);
        }
        obj.setOms(oms);
    }
    
    public void RegelDataOnDemand.setSpecificatie(Regel obj, int index) {
        String specificatie = "specificatie_" + index;
        obj.setSpecificatie(specificatie);
    }
    
    public void RegelDataOnDemand.setSrt(Regel obj, int index) {
        Srtregel srt = srtregelDataOnDemand.getRandomSrtregel();
        obj.setSrt(srt);
    }
    
    public Regel RegelDataOnDemand.getSpecificRegel(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Regel obj = data.get(index);
        Integer id = obj.getId();
        return Regel.findRegel(id);
    }
    
    public Regel RegelDataOnDemand.getRandomRegel() {
        init();
        Regel obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Regel.findRegel(id);
    }
    
    public boolean RegelDataOnDemand.modifyRegel(Regel obj) {
        return false;
    }
    
    public void RegelDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Regel.findRegelEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Regel' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Regel>();
        for (int i = 0; i < 10; i++) {
            Regel obj = getNewTransientRegel(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
