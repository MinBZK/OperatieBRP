// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Srtrelatie;
import nl.bzk.brp.model.data.kern.SrtrelatieDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect SrtrelatieDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SrtrelatieDataOnDemand: @Component;
    
    private Random SrtrelatieDataOnDemand.rnd = new SecureRandom();
    
    private List<Srtrelatie> SrtrelatieDataOnDemand.data;
    
    public Srtrelatie SrtrelatieDataOnDemand.getNewTransientSrtrelatie(int index) {
        Srtrelatie obj = new Srtrelatie();
        setCode(obj, index);
        setNaam(obj, index);
        setOms(obj, index);
        return obj;
    }
    
    public void SrtrelatieDataOnDemand.setCode(Srtrelatie obj, int index) {
        String code = String.valueOf(index);
        if (code.length() > 1) {
            code = new Random().nextInt(10) + code.substring(1, 1);
        }
        obj.setCode(code);
    }
    
    public void SrtrelatieDataOnDemand.setNaam(Srtrelatie obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public void SrtrelatieDataOnDemand.setOms(Srtrelatie obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = oms.substring(0, 250);
        }
        obj.setOms(oms);
    }
    
    public Srtrelatie SrtrelatieDataOnDemand.getSpecificSrtrelatie(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Srtrelatie obj = data.get(index);
        Integer id = obj.getId();
        return Srtrelatie.findSrtrelatie(id);
    }
    
    public Srtrelatie SrtrelatieDataOnDemand.getRandomSrtrelatie() {
        init();
        Srtrelatie obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Srtrelatie.findSrtrelatie(id);
    }
    
    public boolean SrtrelatieDataOnDemand.modifySrtrelatie(Srtrelatie obj) {
        return false;
    }
    
    public void SrtrelatieDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Srtrelatie.findSrtrelatieEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Srtrelatie' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Srtrelatie>();
        for (int i = 0; i < 10; i++) {
            Srtrelatie obj = getNewTransientSrtrelatie(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
