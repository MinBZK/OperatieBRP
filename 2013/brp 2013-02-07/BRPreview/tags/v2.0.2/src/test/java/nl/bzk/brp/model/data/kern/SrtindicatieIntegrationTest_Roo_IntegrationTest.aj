// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Srtindicatie;
import nl.bzk.brp.model.data.kern.SrtindicatieDataOnDemand;
import nl.bzk.brp.model.data.kern.SrtindicatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SrtindicatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SrtindicatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SrtindicatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SrtindicatieIntegrationTest: @Transactional;
    
    @Autowired
    private SrtindicatieDataOnDemand SrtindicatieIntegrationTest.dod;
    
    @Test
    public void SrtindicatieIntegrationTest.testCountSrtindicaties() {
        Assert.assertNotNull("Data on demand for 'Srtindicatie' failed to initialize correctly", dod.getRandomSrtindicatie());
        long count = Srtindicatie.countSrtindicaties();
        Assert.assertTrue("Counter for 'Srtindicatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SrtindicatieIntegrationTest.testFindSrtindicatie() {
        Srtindicatie obj = dod.getRandomSrtindicatie();
        Assert.assertNotNull("Data on demand for 'Srtindicatie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtindicatie' failed to provide an identifier", id);
        obj = Srtindicatie.findSrtindicatie(id);
        Assert.assertNotNull("Find method for 'Srtindicatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Srtindicatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SrtindicatieIntegrationTest.testFindAllSrtindicaties() {
        Assert.assertNotNull("Data on demand for 'Srtindicatie' failed to initialize correctly", dod.getRandomSrtindicatie());
        long count = Srtindicatie.countSrtindicaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Srtindicatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Srtindicatie> result = Srtindicatie.findAllSrtindicaties();
        Assert.assertNotNull("Find all method for 'Srtindicatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Srtindicatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SrtindicatieIntegrationTest.testFindSrtindicatieEntries() {
        Assert.assertNotNull("Data on demand for 'Srtindicatie' failed to initialize correctly", dod.getRandomSrtindicatie());
        long count = Srtindicatie.countSrtindicaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Srtindicatie> result = Srtindicatie.findSrtindicatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Srtindicatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Srtindicatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SrtindicatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Srtindicatie' failed to initialize correctly", dod.getRandomSrtindicatie());
        Srtindicatie obj = dod.getNewTransientSrtindicatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Srtindicatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Srtindicatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Srtindicatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SrtindicatieIntegrationTest.testRemove() {
        Srtindicatie obj = dod.getRandomSrtindicatie();
        Assert.assertNotNull("Data on demand for 'Srtindicatie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtindicatie' failed to provide an identifier", id);
        obj = Srtindicatie.findSrtindicatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Srtindicatie' with identifier '" + id + "'", Srtindicatie.findSrtindicatie(id));
    }
    
}
