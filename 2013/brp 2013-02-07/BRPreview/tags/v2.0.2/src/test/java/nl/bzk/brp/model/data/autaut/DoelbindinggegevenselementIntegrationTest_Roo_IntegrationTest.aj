// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.Doelbindinggegevenselement;
import nl.bzk.brp.model.data.autaut.DoelbindinggegevenselementDataOnDemand;
import nl.bzk.brp.model.data.autaut.DoelbindinggegevenselementIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DoelbindinggegevenselementIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DoelbindinggegevenselementIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DoelbindinggegevenselementIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DoelbindinggegevenselementIntegrationTest: @Transactional;
    
    @Autowired
    private DoelbindinggegevenselementDataOnDemand DoelbindinggegevenselementIntegrationTest.dod;
    
    @Test
    public void DoelbindinggegevenselementIntegrationTest.testCountDoelbindinggegevenselements() {
        Assert.assertNotNull("Data on demand for 'Doelbindinggegevenselement' failed to initialize correctly", dod.getRandomDoelbindinggegevenselement());
        long count = Doelbindinggegevenselement.countDoelbindinggegevenselements();
        Assert.assertTrue("Counter for 'Doelbindinggegevenselement' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DoelbindinggegevenselementIntegrationTest.testFindDoelbindinggegevenselement() {
        Doelbindinggegevenselement obj = dod.getRandomDoelbindinggegevenselement();
        Assert.assertNotNull("Data on demand for 'Doelbindinggegevenselement' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Doelbindinggegevenselement' failed to provide an identifier", id);
        obj = Doelbindinggegevenselement.findDoelbindinggegevenselement(id);
        Assert.assertNotNull("Find method for 'Doelbindinggegevenselement' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Doelbindinggegevenselement' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DoelbindinggegevenselementIntegrationTest.testFindAllDoelbindinggegevenselements() {
        Assert.assertNotNull("Data on demand for 'Doelbindinggegevenselement' failed to initialize correctly", dod.getRandomDoelbindinggegevenselement());
        long count = Doelbindinggegevenselement.countDoelbindinggegevenselements();
        Assert.assertTrue("Too expensive to perform a find all test for 'Doelbindinggegevenselement', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Doelbindinggegevenselement> result = Doelbindinggegevenselement.findAllDoelbindinggegevenselements();
        Assert.assertNotNull("Find all method for 'Doelbindinggegevenselement' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Doelbindinggegevenselement' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DoelbindinggegevenselementIntegrationTest.testFindDoelbindinggegevenselementEntries() {
        Assert.assertNotNull("Data on demand for 'Doelbindinggegevenselement' failed to initialize correctly", dod.getRandomDoelbindinggegevenselement());
        long count = Doelbindinggegevenselement.countDoelbindinggegevenselements();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Doelbindinggegevenselement> result = Doelbindinggegevenselement.findDoelbindinggegevenselementEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Doelbindinggegevenselement' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Doelbindinggegevenselement' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DoelbindinggegevenselementIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Doelbindinggegevenselement' failed to initialize correctly", dod.getRandomDoelbindinggegevenselement());
        Doelbindinggegevenselement obj = dod.getNewTransientDoelbindinggegevenselement(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Doelbindinggegevenselement' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Doelbindinggegevenselement' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Doelbindinggegevenselement' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DoelbindinggegevenselementIntegrationTest.testRemove() {
        Doelbindinggegevenselement obj = dod.getRandomDoelbindinggegevenselement();
        Assert.assertNotNull("Data on demand for 'Doelbindinggegevenselement' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Doelbindinggegevenselement' failed to provide an identifier", id);
        obj = Doelbindinggegevenselement.findDoelbindinggegevenselement(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Doelbindinggegevenselement' with identifier '" + id + "'", Doelbindinggegevenselement.findDoelbindinggegevenselement(id));
    }
    
}
