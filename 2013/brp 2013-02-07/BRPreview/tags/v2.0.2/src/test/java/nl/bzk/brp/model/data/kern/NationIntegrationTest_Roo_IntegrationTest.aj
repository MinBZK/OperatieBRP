// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Nation;
import nl.bzk.brp.model.data.kern.NationDataOnDemand;
import nl.bzk.brp.model.data.kern.NationIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect NationIntegrationTest_Roo_IntegrationTest {
    
    declare @type: NationIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: NationIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: NationIntegrationTest: @Transactional;
    
    @Autowired
    private NationDataOnDemand NationIntegrationTest.dod;
    
    @Test
    public void NationIntegrationTest.testCountNations() {
        Assert.assertNotNull("Data on demand for 'Nation' failed to initialize correctly", dod.getRandomNation());
        long count = Nation.countNations();
        Assert.assertTrue("Counter for 'Nation' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void NationIntegrationTest.testFindNation() {
        Nation obj = dod.getRandomNation();
        Assert.assertNotNull("Data on demand for 'Nation' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Nation' failed to provide an identifier", id);
        obj = Nation.findNation(id);
        Assert.assertNotNull("Find method for 'Nation' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Nation' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void NationIntegrationTest.testFindAllNations() {
        Assert.assertNotNull("Data on demand for 'Nation' failed to initialize correctly", dod.getRandomNation());
        long count = Nation.countNations();
        Assert.assertTrue("Too expensive to perform a find all test for 'Nation', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Nation> result = Nation.findAllNations();
        Assert.assertNotNull("Find all method for 'Nation' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Nation' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void NationIntegrationTest.testFindNationEntries() {
        Assert.assertNotNull("Data on demand for 'Nation' failed to initialize correctly", dod.getRandomNation());
        long count = Nation.countNations();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Nation> result = Nation.findNationEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Nation' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Nation' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void NationIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Nation' failed to initialize correctly", dod.getRandomNation());
        Nation obj = dod.getNewTransientNation(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Nation' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Nation' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Nation' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void NationIntegrationTest.testRemove() {
        Nation obj = dod.getRandomNation();
        Assert.assertNotNull("Data on demand for 'Nation' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Nation' failed to provide an identifier", id);
        obj = Nation.findNation(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Nation' with identifier '" + id + "'", Nation.findNation(id));
    }
    
}
