// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.lev;

import java.util.List;
import nl.bzk.brp.model.data.lev.Lev;
import nl.bzk.brp.model.data.lev.LevDataOnDemand;
import nl.bzk.brp.model.data.lev.LevIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect LevIntegrationTest_Roo_IntegrationTest {
    
    declare @type: LevIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: LevIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: LevIntegrationTest: @Transactional;
    
    @Autowired
    private LevDataOnDemand LevIntegrationTest.dod;
    
    @Test
    public void LevIntegrationTest.testCountLevs() {
        Assert.assertNotNull("Data on demand for 'Lev' failed to initialize correctly", dod.getRandomLev());
        long count = Lev.countLevs();
        Assert.assertTrue("Counter for 'Lev' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void LevIntegrationTest.testFindLev() {
        Lev obj = dod.getRandomLev();
        Assert.assertNotNull("Data on demand for 'Lev' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Lev' failed to provide an identifier", id);
        obj = Lev.findLev(id);
        Assert.assertNotNull("Find method for 'Lev' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Lev' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void LevIntegrationTest.testFindAllLevs() {
        Assert.assertNotNull("Data on demand for 'Lev' failed to initialize correctly", dod.getRandomLev());
        long count = Lev.countLevs();
        Assert.assertTrue("Too expensive to perform a find all test for 'Lev', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Lev> result = Lev.findAllLevs();
        Assert.assertNotNull("Find all method for 'Lev' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Lev' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void LevIntegrationTest.testFindLevEntries() {
        Assert.assertNotNull("Data on demand for 'Lev' failed to initialize correctly", dod.getRandomLev());
        long count = Lev.countLevs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Lev> result = Lev.findLevEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Lev' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Lev' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void LevIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Lev' failed to initialize correctly", dod.getRandomLev());
        Lev obj = dod.getNewTransientLev(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Lev' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Lev' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Lev' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void LevIntegrationTest.testRemove() {
        Lev obj = dod.getRandomLev();
        Assert.assertNotNull("Data on demand for 'Lev' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Lev' failed to provide an identifier", id);
        obj = Lev.findLev(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Lev' with identifier '" + id + "'", Lev.findLev(id));
    }
    
}
