// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.brm;

import java.util.List;
import nl.bzk.brp.model.data.brm.HisRegelimplementatiesituat;
import nl.bzk.brp.model.data.brm.HisRegelimplementatiesituatDataOnDemand;
import nl.bzk.brp.model.data.brm.HisRegelimplementatiesituatIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisRegelimplementatiesituatIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisRegelimplementatiesituatIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisRegelimplementatiesituatIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisRegelimplementatiesituatIntegrationTest: @Transactional;
    
    @Autowired
    private HisRegelimplementatiesituatDataOnDemand HisRegelimplementatiesituatIntegrationTest.dod;
    
    @Test
    public void HisRegelimplementatiesituatIntegrationTest.testCountHisRegelimplementatiesituats() {
        Assert.assertNotNull("Data on demand for 'HisRegelimplementatiesituat' failed to initialize correctly", dod.getRandomHisRegelimplementatiesituat());
        long count = HisRegelimplementatiesituat.countHisRegelimplementatiesituats();
        Assert.assertTrue("Counter for 'HisRegelimplementatiesituat' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisRegelimplementatiesituatIntegrationTest.testFindHisRegelimplementatiesituat() {
        HisRegelimplementatiesituat obj = dod.getRandomHisRegelimplementatiesituat();
        Assert.assertNotNull("Data on demand for 'HisRegelimplementatiesituat' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisRegelimplementatiesituat' failed to provide an identifier", id);
        obj = HisRegelimplementatiesituat.findHisRegelimplementatiesituat(id);
        Assert.assertNotNull("Find method for 'HisRegelimplementatiesituat' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisRegelimplementatiesituat' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisRegelimplementatiesituatIntegrationTest.testFindAllHisRegelimplementatiesituats() {
        Assert.assertNotNull("Data on demand for 'HisRegelimplementatiesituat' failed to initialize correctly", dod.getRandomHisRegelimplementatiesituat());
        long count = HisRegelimplementatiesituat.countHisRegelimplementatiesituats();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisRegelimplementatiesituat', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisRegelimplementatiesituat> result = HisRegelimplementatiesituat.findAllHisRegelimplementatiesituats();
        Assert.assertNotNull("Find all method for 'HisRegelimplementatiesituat' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisRegelimplementatiesituat' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisRegelimplementatiesituatIntegrationTest.testFindHisRegelimplementatiesituatEntries() {
        Assert.assertNotNull("Data on demand for 'HisRegelimplementatiesituat' failed to initialize correctly", dod.getRandomHisRegelimplementatiesituat());
        long count = HisRegelimplementatiesituat.countHisRegelimplementatiesituats();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisRegelimplementatiesituat> result = HisRegelimplementatiesituat.findHisRegelimplementatiesituatEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisRegelimplementatiesituat' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisRegelimplementatiesituat' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisRegelimplementatiesituatIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisRegelimplementatiesituat' failed to initialize correctly", dod.getRandomHisRegelimplementatiesituat());
        HisRegelimplementatiesituat obj = dod.getNewTransientHisRegelimplementatiesituat(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisRegelimplementatiesituat' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisRegelimplementatiesituat' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisRegelimplementatiesituat' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisRegelimplementatiesituatIntegrationTest.testRemove() {
        HisRegelimplementatiesituat obj = dod.getRandomHisRegelimplementatiesituat();
        Assert.assertNotNull("Data on demand for 'HisRegelimplementatiesituat' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisRegelimplementatiesituat' failed to provide an identifier", id);
        obj = HisRegelimplementatiesituat.findHisRegelimplementatiesituat(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisRegelimplementatiesituat' with identifier '" + id + "'", HisRegelimplementatiesituat.findHisRegelimplementatiesituat(id));
    }
    
}
