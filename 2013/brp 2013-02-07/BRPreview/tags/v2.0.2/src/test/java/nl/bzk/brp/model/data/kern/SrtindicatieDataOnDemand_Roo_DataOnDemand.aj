// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Srtindicatie;
import nl.bzk.brp.model.data.kern.SrtindicatieDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect SrtindicatieDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SrtindicatieDataOnDemand: @Component;
    
    private Random SrtindicatieDataOnDemand.rnd = new SecureRandom();
    
    private List<Srtindicatie> SrtindicatieDataOnDemand.data;
    
    public Srtindicatie SrtindicatieDataOnDemand.getNewTransientSrtindicatie(int index) {
        Srtindicatie obj = new Srtindicatie();
        setIndmaterielehistorievantoepa(obj, index);
        setNaam(obj, index);
        return obj;
    }
    
    public void SrtindicatieDataOnDemand.setIndmaterielehistorievantoepa(Srtindicatie obj, int index) {
        Boolean indmaterielehistorievantoepa = true;
        obj.setIndmaterielehistorievantoepa(indmaterielehistorievantoepa);
    }
    
    public void SrtindicatieDataOnDemand.setNaam(Srtindicatie obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public Srtindicatie SrtindicatieDataOnDemand.getSpecificSrtindicatie(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Srtindicatie obj = data.get(index);
        Short id = obj.getId();
        return Srtindicatie.findSrtindicatie(id);
    }
    
    public Srtindicatie SrtindicatieDataOnDemand.getRandomSrtindicatie() {
        init();
        Srtindicatie obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Srtindicatie.findSrtindicatie(id);
    }
    
    public boolean SrtindicatieDataOnDemand.modifySrtindicatie(Srtindicatie obj) {
        return false;
    }
    
    public void SrtindicatieDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Srtindicatie.findSrtindicatieEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Srtindicatie' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Srtindicatie>();
        for (int i = 0; i < 10; i++) {
            Srtindicatie obj = getNewTransientSrtindicatie(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
