// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Srtverificatie;
import nl.bzk.brp.model.data.kern.SrtverificatieDataOnDemand;
import nl.bzk.brp.model.data.kern.SrtverificatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SrtverificatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SrtverificatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SrtverificatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SrtverificatieIntegrationTest: @Transactional;
    
    @Autowired
    private SrtverificatieDataOnDemand SrtverificatieIntegrationTest.dod;
    
    @Test
    public void SrtverificatieIntegrationTest.testCountSrtverificaties() {
        Assert.assertNotNull("Data on demand for 'Srtverificatie' failed to initialize correctly", dod.getRandomSrtverificatie());
        long count = Srtverificatie.countSrtverificaties();
        Assert.assertTrue("Counter for 'Srtverificatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SrtverificatieIntegrationTest.testFindSrtverificatie() {
        Srtverificatie obj = dod.getRandomSrtverificatie();
        Assert.assertNotNull("Data on demand for 'Srtverificatie' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtverificatie' failed to provide an identifier", id);
        obj = Srtverificatie.findSrtverificatie(id);
        Assert.assertNotNull("Find method for 'Srtverificatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Srtverificatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SrtverificatieIntegrationTest.testFindAllSrtverificaties() {
        Assert.assertNotNull("Data on demand for 'Srtverificatie' failed to initialize correctly", dod.getRandomSrtverificatie());
        long count = Srtverificatie.countSrtverificaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Srtverificatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Srtverificatie> result = Srtverificatie.findAllSrtverificaties();
        Assert.assertNotNull("Find all method for 'Srtverificatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Srtverificatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SrtverificatieIntegrationTest.testFindSrtverificatieEntries() {
        Assert.assertNotNull("Data on demand for 'Srtverificatie' failed to initialize correctly", dod.getRandomSrtverificatie());
        long count = Srtverificatie.countSrtverificaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Srtverificatie> result = Srtverificatie.findSrtverificatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Srtverificatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Srtverificatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SrtverificatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Srtverificatie' failed to initialize correctly", dod.getRandomSrtverificatie());
        Srtverificatie obj = dod.getNewTransientSrtverificatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Srtverificatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Srtverificatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Srtverificatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SrtverificatieIntegrationTest.testRemove() {
        Srtverificatie obj = dod.getRandomSrtverificatie();
        Assert.assertNotNull("Data on demand for 'Srtverificatie' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtverificatie' failed to provide an identifier", id);
        obj = Srtverificatie.findSrtverificatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Srtverificatie' with identifier '" + id + "'", Srtverificatie.findSrtverificatie(id));
    }
    
}
