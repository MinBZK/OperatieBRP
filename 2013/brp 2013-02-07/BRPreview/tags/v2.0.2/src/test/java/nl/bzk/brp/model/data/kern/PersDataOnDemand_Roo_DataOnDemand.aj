// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Adellijketitel;
import nl.bzk.brp.model.data.kern.AdellijketitelDataOnDemand;
import nl.bzk.brp.model.data.kern.Geslachtsaand;
import nl.bzk.brp.model.data.kern.GeslachtsaandDataOnDemand;
import nl.bzk.brp.model.data.kern.Land;
import nl.bzk.brp.model.data.kern.LandDataOnDemand;
import nl.bzk.brp.model.data.kern.Partij;
import nl.bzk.brp.model.data.kern.PartijDataOnDemand;
import nl.bzk.brp.model.data.kern.Pers;
import nl.bzk.brp.model.data.kern.PersDataOnDemand;
import nl.bzk.brp.model.data.kern.Plaats;
import nl.bzk.brp.model.data.kern.PlaatsDataOnDemand;
import nl.bzk.brp.model.data.kern.Predikaat;
import nl.bzk.brp.model.data.kern.PredikaatDataOnDemand;
import nl.bzk.brp.model.data.kern.Rdnopschorting;
import nl.bzk.brp.model.data.kern.RdnopschortingDataOnDemand;
import nl.bzk.brp.model.data.kern.Srtpers;
import nl.bzk.brp.model.data.kern.SrtpersDataOnDemand;
import nl.bzk.brp.model.data.kern.Verantwoordelijke;
import nl.bzk.brp.model.data.kern.VerantwoordelijkeDataOnDemand;
import nl.bzk.brp.model.data.kern.Verblijfsr;
import nl.bzk.brp.model.data.kern.VerblijfsrDataOnDemand;
import nl.bzk.brp.model.data.kern.Wijzegebruikgeslnaam;
import nl.bzk.brp.model.data.kern.WijzegebruikgeslnaamDataOnDemand;
import nl.bzk.brp.model.service.PersonService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect PersDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PersDataOnDemand: @Component;
    
    private Random PersDataOnDemand.rnd = new SecureRandom();
    
    private List<Pers> PersDataOnDemand.data;
    
    @Autowired
    private AdellijketitelDataOnDemand PersDataOnDemand.adellijketitelDataOnDemand;
    
    @Autowired
    private PartijDataOnDemand PersDataOnDemand.partijDataOnDemand;
    
    @Autowired
    private WijzegebruikgeslnaamDataOnDemand PersDataOnDemand.wijzegebruikgeslnaamDataOnDemand;
    
    @Autowired
    private GeslachtsaandDataOnDemand PersDataOnDemand.geslachtsaandDataOnDemand;
    
    @Autowired
    private LandDataOnDemand PersDataOnDemand.landDataOnDemand;
    
    @Autowired
    private PredikaatDataOnDemand PersDataOnDemand.predikaatDataOnDemand;
    
    @Autowired
    private RdnopschortingDataOnDemand PersDataOnDemand.rdnopschortingDataOnDemand;
    
    @Autowired
    private SrtpersDataOnDemand PersDataOnDemand.srtpersDataOnDemand;
    
    @Autowired
    private VerantwoordelijkeDataOnDemand PersDataOnDemand.verantwoordelijkeDataOnDemand;
    
    @Autowired
    private VerblijfsrDataOnDemand PersDataOnDemand.verblijfsrDataOnDemand;
    
    @Autowired
    private PlaatsDataOnDemand PersDataOnDemand.plaatsDataOnDemand;
    
    @Autowired
    PersonService PersDataOnDemand.personService;
    
    public Pers PersDataOnDemand.getNewTransientPers(int index) {
        Pers obj = new Pers();
        setAanschrstatushis(obj, index);
        setAdellijketitel(obj, index);
        setAnr(obj, index);
        setBijhgem(obj, index);
        setBijhgemstatushis(obj, index);
        setBijhverantwoordelijkheidstat(obj, index);
        setBlgeboorteplaats(obj, index);
        setBlplaatsoverlijden(obj, index);
        setBlregiogeboorte(obj, index);
        setBlregiooverlijden(obj, index);
        setBsn(obj, index);
        setDataanlaanpdeelneuverkiezing(obj, index);
        setDataanvaaneenslverblijfsr(obj, index);
        setDataanvverblijfsr(obj, index);
        setDateindeuitsleukiesr(obj, index);
        setDateindeuitslnlkiesr(obj, index);
        setDatgeboorte(obj, index);
        setDatinschr(obj, index);
        setDatinschringem(obj, index);
        setDatoverlijden(obj, index);
        setDatvestiginginnederland(obj, index);
        setDatvoorzeindeverblijfsr(obj, index);
        setEuverkiezingenstatushis(obj, index);
        setGeboortestatushis(obj, index);
        setGebrgeslnaamegp(obj, index);
        setGemgeboorte(obj, index);
        setGemoverlijden(obj, index);
        setGempk(obj, index);
        setGeslachtsaand(obj, index);
        setGeslachtsaandstatushis(obj, index);
        setGeslnaam(obj, index);
        setGeslnaamaanschr(obj, index);
        setIdsstatushis(obj, index);
        setImmigratiestatushis(obj, index);
        setIndaanschralgoritmischafgele(obj, index);
        setIndaanschrmetadellijketitels(obj, index);
        setIndalgoritmischafgeleid(obj, index);
        setInddeelneuverkiezingen(obj, index);
        setIndgegevensinonderzoek(obj, index);
        setIndnreeksalsgeslnaam(obj, index);
        setIndonverwdocaanw(obj, index);
        setIndpkvollediggeconv(obj, index);
        setInduitslnlkiesr(obj, index);
        setInschrstatushis(obj, index);
        setLandgeboorte(obj, index);
        setLandoverlijden(obj, index);
        setLandvanwaargevestigd(obj, index);
        setOmsgeboorteloc(obj, index);
        setOmslocoverlijden(obj, index);
        setOpschortingstatushis(obj, index);
        setOverlijdenstatushis(obj, index);
        setPkstatushis(obj, index);
        setPredikaat(obj, index);
        setPredikaataanschr(obj, index);
        setRdnopschortingbijhouding(obj, index);
        setSamengesteldenaamstatushis(obj, index);
        setScheidingsteken(obj, index);
        setScheidingstekenaanschr(obj, index);
        setSrt(obj, index);
        setTijdstiplaatstewijz(obj, index);
        setUitslnlkiesrstatushis(obj, index);
        setVerantwoordelijke(obj, index);
        setVerblijfsr(obj, index);
        setVerblijfsrstatushis(obj, index);
        setVersienr(obj, index);
        setVolgendepers(obj, index);
        setVoornamen(obj, index);
        setVoornamenaanschr(obj, index);
        setVoorvoegsel(obj, index);
        setVoorvoegselaanschr(obj, index);
        setVorigepers(obj, index);
        setWplgeboorte(obj, index);
        setWploverlijden(obj, index);
        return obj;
    }
    
    public void PersDataOnDemand.setAanschrstatushis(Pers obj, int index) {
        String aanschrstatushis = String.valueOf(index);
        if (aanschrstatushis.length() > 1) {
            aanschrstatushis = aanschrstatushis.substring(0, 1);
        }
        obj.setAanschrstatushis(aanschrstatushis);
    }
    
    public void PersDataOnDemand.setAdellijketitel(Pers obj, int index) {
        Adellijketitel adellijketitel = adellijketitelDataOnDemand.getRandomAdellijketitel();
        obj.setAdellijketitel(adellijketitel);
    }
    
    public void PersDataOnDemand.setAnr(Pers obj, int index) {
        String anr = "anr_" + index;
        if (anr.length() > 10) {
            anr = anr.substring(0, 10);
        }
        obj.setAnr(anr);
    }
    
    public void PersDataOnDemand.setBijhgem(Pers obj, int index) {
        Partij bijhgem = partijDataOnDemand.getRandomPartij();
        obj.setBijhgem(bijhgem);
    }
    
    public void PersDataOnDemand.setBijhgemstatushis(Pers obj, int index) {
        String bijhgemstatushis = String.valueOf(index);
        if (bijhgemstatushis.length() > 1) {
            bijhgemstatushis = bijhgemstatushis.substring(0, 1);
        }
        obj.setBijhgemstatushis(bijhgemstatushis);
    }
    
    public void PersDataOnDemand.setBijhverantwoordelijkheidstat(Pers obj, int index) {
        String bijhverantwoordelijkheidstat = String.valueOf(index);
        if (bijhverantwoordelijkheidstat.length() > 1) {
            bijhverantwoordelijkheidstat = bijhverantwoordelijkheidstat.substring(0, 1);
        }
        obj.setBijhverantwoordelijkheidstat(bijhverantwoordelijkheidstat);
    }
    
    public void PersDataOnDemand.setBlgeboorteplaats(Pers obj, int index) {
        String blgeboorteplaats = "blgeboorteplaats_" + index;
        if (blgeboorteplaats.length() > 40) {
            blgeboorteplaats = blgeboorteplaats.substring(0, 40);
        }
        obj.setBlgeboorteplaats(blgeboorteplaats);
    }
    
    public void PersDataOnDemand.setBlplaatsoverlijden(Pers obj, int index) {
        String blplaatsoverlijden = "blplaatsoverlijden_" + index;
        if (blplaatsoverlijden.length() > 40) {
            blplaatsoverlijden = blplaatsoverlijden.substring(0, 40);
        }
        obj.setBlplaatsoverlijden(blplaatsoverlijden);
    }
    
    public void PersDataOnDemand.setBlregiogeboorte(Pers obj, int index) {
        String blregiogeboorte = "blregiogeboorte_" + index;
        if (blregiogeboorte.length() > 35) {
            blregiogeboorte = blregiogeboorte.substring(0, 35);
        }
        obj.setBlregiogeboorte(blregiogeboorte);
    }
    
    public void PersDataOnDemand.setBlregiooverlijden(Pers obj, int index) {
        String blregiooverlijden = "blregiooverlijden_" + index;
        if (blregiooverlijden.length() > 35) {
            blregiooverlijden = blregiooverlijden.substring(0, 35);
        }
        obj.setBlregiooverlijden(blregiooverlijden);
    }
    
    public void PersDataOnDemand.setBsn(Pers obj, int index) {
        String bsn = "bsn_" + index;
        if (bsn.length() > 9) {
            bsn = bsn.substring(0, 9);
        }
        obj.setBsn(bsn);
    }
    
    public void PersDataOnDemand.setDataanlaanpdeelneuverkiezing(Pers obj, int index) {
        BigDecimal dataanlaanpdeelneuverkiezing = BigDecimal.valueOf(index);
        obj.setDataanlaanpdeelneuverkiezing(dataanlaanpdeelneuverkiezing);
    }
    
    public void PersDataOnDemand.setDataanvaaneenslverblijfsr(Pers obj, int index) {
        BigDecimal dataanvaaneenslverblijfsr = BigDecimal.valueOf(index);
        obj.setDataanvaaneenslverblijfsr(dataanvaaneenslverblijfsr);
    }
    
    public void PersDataOnDemand.setDataanvverblijfsr(Pers obj, int index) {
        BigDecimal dataanvverblijfsr = BigDecimal.valueOf(index);
        obj.setDataanvverblijfsr(dataanvverblijfsr);
    }
    
    public void PersDataOnDemand.setDateindeuitsleukiesr(Pers obj, int index) {
        BigDecimal dateindeuitsleukiesr = BigDecimal.valueOf(index);
        obj.setDateindeuitsleukiesr(dateindeuitsleukiesr);
    }
    
    public void PersDataOnDemand.setDateindeuitslnlkiesr(Pers obj, int index) {
        BigDecimal dateindeuitslnlkiesr = BigDecimal.valueOf(index);
        obj.setDateindeuitslnlkiesr(dateindeuitslnlkiesr);
    }
    
    public void PersDataOnDemand.setDatgeboorte(Pers obj, int index) {
        BigDecimal datgeboorte = BigDecimal.valueOf(index);
        obj.setDatgeboorte(datgeboorte);
    }
    
    public void PersDataOnDemand.setDatinschr(Pers obj, int index) {
        BigDecimal datinschr = BigDecimal.valueOf(index);
        obj.setDatinschr(datinschr);
    }
    
    public void PersDataOnDemand.setDatinschringem(Pers obj, int index) {
        BigDecimal datinschringem = BigDecimal.valueOf(index);
        obj.setDatinschringem(datinschringem);
    }
    
    public void PersDataOnDemand.setDatoverlijden(Pers obj, int index) {
        BigDecimal datoverlijden = BigDecimal.valueOf(index);
        obj.setDatoverlijden(datoverlijden);
    }
    
    public void PersDataOnDemand.setDatvestiginginnederland(Pers obj, int index) {
        BigDecimal datvestiginginnederland = BigDecimal.valueOf(index);
        obj.setDatvestiginginnederland(datvestiginginnederland);
    }
    
    public void PersDataOnDemand.setDatvoorzeindeverblijfsr(Pers obj, int index) {
        BigDecimal datvoorzeindeverblijfsr = BigDecimal.valueOf(index);
        obj.setDatvoorzeindeverblijfsr(datvoorzeindeverblijfsr);
    }
    
    public void PersDataOnDemand.setEuverkiezingenstatushis(Pers obj, int index) {
        String euverkiezingenstatushis = String.valueOf(index);
        if (euverkiezingenstatushis.length() > 1) {
            euverkiezingenstatushis = euverkiezingenstatushis.substring(0, 1);
        }
        obj.setEuverkiezingenstatushis(euverkiezingenstatushis);
    }
    
    public void PersDataOnDemand.setGeboortestatushis(Pers obj, int index) {
        String geboortestatushis = String.valueOf(index);
        if (geboortestatushis.length() > 1) {
            geboortestatushis = geboortestatushis.substring(0, 1);
        }
        obj.setGeboortestatushis(geboortestatushis);
    }
    
    public void PersDataOnDemand.setGebrgeslnaamegp(Pers obj, int index) {
        Wijzegebruikgeslnaam gebrgeslnaamegp = wijzegebruikgeslnaamDataOnDemand.getRandomWijzegebruikgeslnaam();
        obj.setGebrgeslnaamegp(gebrgeslnaamegp);
    }
    
    public void PersDataOnDemand.setGemgeboorte(Pers obj, int index) {
        Partij gemgeboorte = partijDataOnDemand.getRandomPartij();
        obj.setGemgeboorte(gemgeboorte);
    }
    
    public void PersDataOnDemand.setGemoverlijden(Pers obj, int index) {
        Partij gemoverlijden = partijDataOnDemand.getRandomPartij();
        obj.setGemoverlijden(gemoverlijden);
    }
    
    public void PersDataOnDemand.setGempk(Pers obj, int index) {
        Partij gempk = partijDataOnDemand.getRandomPartij();
        obj.setGempk(gempk);
    }
    
    public void PersDataOnDemand.setGeslachtsaand(Pers obj, int index) {
        Geslachtsaand geslachtsaand = geslachtsaandDataOnDemand.getRandomGeslachtsaand();
        obj.setGeslachtsaand(geslachtsaand);
    }
    
    public void PersDataOnDemand.setGeslachtsaandstatushis(Pers obj, int index) {
        String geslachtsaandstatushis = String.valueOf(index);
        if (geslachtsaandstatushis.length() > 1) {
            geslachtsaandstatushis = geslachtsaandstatushis.substring(0, 1);
        }
        obj.setGeslachtsaandstatushis(geslachtsaandstatushis);
    }
    
    public void PersDataOnDemand.setGeslnaam(Pers obj, int index) {
        String geslnaam = "geslnaam_" + index;
        if (geslnaam.length() > 200) {
            geslnaam = geslnaam.substring(0, 200);
        }
        obj.setGeslnaam(geslnaam);
    }
    
    public void PersDataOnDemand.setGeslnaamaanschr(Pers obj, int index) {
        String geslnaamaanschr = "geslnaamaanschr_" + index;
        if (geslnaamaanschr.length() > 200) {
            geslnaamaanschr = geslnaamaanschr.substring(0, 200);
        }
        obj.setGeslnaamaanschr(geslnaamaanschr);
    }
    
    public void PersDataOnDemand.setIdsstatushis(Pers obj, int index) {
        String idsstatushis = String.valueOf(index);
        if (idsstatushis.length() > 1) {
            idsstatushis = idsstatushis.substring(0, 1);
        }
        obj.setIdsstatushis(idsstatushis);
    }
    
    public void PersDataOnDemand.setImmigratiestatushis(Pers obj, int index) {
        String immigratiestatushis = String.valueOf(index);
        if (immigratiestatushis.length() > 1) {
            immigratiestatushis = immigratiestatushis.substring(0, 1);
        }
        obj.setImmigratiestatushis(immigratiestatushis);
    }
    
    public void PersDataOnDemand.setIndaanschralgoritmischafgele(Pers obj, int index) {
        Boolean indaanschralgoritmischafgele = Boolean.TRUE;
        obj.setIndaanschralgoritmischafgele(indaanschralgoritmischafgele);
    }
    
    public void PersDataOnDemand.setIndaanschrmetadellijketitels(Pers obj, int index) {
        Boolean indaanschrmetadellijketitels = Boolean.TRUE;
        obj.setIndaanschrmetadellijketitels(indaanschrmetadellijketitels);
    }
    
    public void PersDataOnDemand.setIndalgoritmischafgeleid(Pers obj, int index) {
        Boolean indalgoritmischafgeleid = Boolean.TRUE;
        obj.setIndalgoritmischafgeleid(indalgoritmischafgeleid);
    }
    
    public void PersDataOnDemand.setInddeelneuverkiezingen(Pers obj, int index) {
        Boolean inddeelneuverkiezingen = Boolean.TRUE;
        obj.setInddeelneuverkiezingen(inddeelneuverkiezingen);
    }
    
    public void PersDataOnDemand.setIndgegevensinonderzoek(Pers obj, int index) {
        Boolean indgegevensinonderzoek = Boolean.TRUE;
        obj.setIndgegevensinonderzoek(indgegevensinonderzoek);
    }
    
    public void PersDataOnDemand.setIndnreeksalsgeslnaam(Pers obj, int index) {
        Boolean indnreeksalsgeslnaam = Boolean.TRUE;
        obj.setIndnreeksalsgeslnaam(indnreeksalsgeslnaam);
    }
    
    public void PersDataOnDemand.setIndonverwdocaanw(Pers obj, int index) {
        Boolean indonverwdocaanw = Boolean.TRUE;
        obj.setIndonverwdocaanw(indonverwdocaanw);
    }
    
    public void PersDataOnDemand.setIndpkvollediggeconv(Pers obj, int index) {
        Boolean indpkvollediggeconv = Boolean.TRUE;
        obj.setIndpkvollediggeconv(indpkvollediggeconv);
    }
    
    public void PersDataOnDemand.setInduitslnlkiesr(Pers obj, int index) {
        Boolean induitslnlkiesr = Boolean.TRUE;
        obj.setInduitslnlkiesr(induitslnlkiesr);
    }
    
    public void PersDataOnDemand.setInschrstatushis(Pers obj, int index) {
        String inschrstatushis = String.valueOf(index);
        if (inschrstatushis.length() > 1) {
            inschrstatushis = inschrstatushis.substring(0, 1);
        }
        obj.setInschrstatushis(inschrstatushis);
    }
    
    public void PersDataOnDemand.setLandgeboorte(Pers obj, int index) {
        Land landgeboorte = landDataOnDemand.getRandomLand();
        obj.setLandgeboorte(landgeboorte);
    }
    
    public void PersDataOnDemand.setLandoverlijden(Pers obj, int index) {
        Land landoverlijden = landDataOnDemand.getRandomLand();
        obj.setLandoverlijden(landoverlijden);
    }
    
    public void PersDataOnDemand.setLandvanwaargevestigd(Pers obj, int index) {
        Land landvanwaargevestigd = landDataOnDemand.getRandomLand();
        obj.setLandvanwaargevestigd(landvanwaargevestigd);
    }
    
    public void PersDataOnDemand.setOmsgeboorteloc(Pers obj, int index) {
        String omsgeboorteloc = "omsgeboorteloc_" + index;
        if (omsgeboorteloc.length() > 40) {
            omsgeboorteloc = omsgeboorteloc.substring(0, 40);
        }
        obj.setOmsgeboorteloc(omsgeboorteloc);
    }
    
    public void PersDataOnDemand.setOmslocoverlijden(Pers obj, int index) {
        String omslocoverlijden = "omslocoverlijden_" + index;
        if (omslocoverlijden.length() > 40) {
            omslocoverlijden = omslocoverlijden.substring(0, 40);
        }
        obj.setOmslocoverlijden(omslocoverlijden);
    }
    
    public void PersDataOnDemand.setOpschortingstatushis(Pers obj, int index) {
        String opschortingstatushis = String.valueOf(index);
        if (opschortingstatushis.length() > 1) {
            opschortingstatushis = opschortingstatushis.substring(0, 1);
        }
        obj.setOpschortingstatushis(opschortingstatushis);
    }
    
    public void PersDataOnDemand.setOverlijdenstatushis(Pers obj, int index) {
        String overlijdenstatushis = String.valueOf(index);
        if (overlijdenstatushis.length() > 1) {
            overlijdenstatushis = overlijdenstatushis.substring(0, 1);
        }
        obj.setOverlijdenstatushis(overlijdenstatushis);
    }
    
    public void PersDataOnDemand.setPkstatushis(Pers obj, int index) {
        String pkstatushis = String.valueOf(index);
        if (pkstatushis.length() > 1) {
            pkstatushis = pkstatushis.substring(0, 1);
        }
        obj.setPkstatushis(pkstatushis);
    }
    
    public void PersDataOnDemand.setPredikaat(Pers obj, int index) {
        Predikaat predikaat = predikaatDataOnDemand.getRandomPredikaat();
        obj.setPredikaat(predikaat);
    }
    
    public void PersDataOnDemand.setPredikaataanschr(Pers obj, int index) {
        Predikaat predikaataanschr = predikaatDataOnDemand.getRandomPredikaat();
        obj.setPredikaataanschr(predikaataanschr);
    }
    
    public void PersDataOnDemand.setRdnopschortingbijhouding(Pers obj, int index) {
        Rdnopschorting rdnopschortingbijhouding = rdnopschortingDataOnDemand.getRandomRdnopschorting();
        obj.setRdnopschortingbijhouding(rdnopschortingbijhouding);
    }
    
    public void PersDataOnDemand.setSamengesteldenaamstatushis(Pers obj, int index) {
        String samengesteldenaamstatushis = String.valueOf(index);
        if (samengesteldenaamstatushis.length() > 1) {
            samengesteldenaamstatushis = samengesteldenaamstatushis.substring(0, 1);
        }
        obj.setSamengesteldenaamstatushis(samengesteldenaamstatushis);
    }
    
    public void PersDataOnDemand.setScheidingsteken(Pers obj, int index) {
        String scheidingsteken = String.valueOf(index);
        if (scheidingsteken.length() > 1) {
            scheidingsteken = scheidingsteken.substring(0, 1);
        }
        obj.setScheidingsteken(scheidingsteken);
    }
    
    public void PersDataOnDemand.setScheidingstekenaanschr(Pers obj, int index) {
        String scheidingstekenaanschr = String.valueOf(index);
        if (scheidingstekenaanschr.length() > 1) {
            scheidingstekenaanschr = scheidingstekenaanschr.substring(0, 1);
        }
        obj.setScheidingstekenaanschr(scheidingstekenaanschr);
    }
    
    public void PersDataOnDemand.setSrt(Pers obj, int index) {
        Srtpers srt = srtpersDataOnDemand.getRandomSrtpers();
        obj.setSrt(srt);
    }
    
    public void PersDataOnDemand.setTijdstiplaatstewijz(Pers obj, int index) {
        Date tijdstiplaatstewijz = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTijdstiplaatstewijz(tijdstiplaatstewijz);
    }
    
    public void PersDataOnDemand.setUitslnlkiesrstatushis(Pers obj, int index) {
        String uitslnlkiesrstatushis = String.valueOf(index);
        if (uitslnlkiesrstatushis.length() > 1) {
            uitslnlkiesrstatushis = uitslnlkiesrstatushis.substring(0, 1);
        }
        obj.setUitslnlkiesrstatushis(uitslnlkiesrstatushis);
    }
    
    public void PersDataOnDemand.setVerantwoordelijke(Pers obj, int index) {
        Verantwoordelijke verantwoordelijke = verantwoordelijkeDataOnDemand.getRandomVerantwoordelijke();
        obj.setVerantwoordelijke(verantwoordelijke);
    }
    
    public void PersDataOnDemand.setVerblijfsr(Pers obj, int index) {
        Verblijfsr verblijfsr = verblijfsrDataOnDemand.getRandomVerblijfsr();
        obj.setVerblijfsr(verblijfsr);
    }
    
    public void PersDataOnDemand.setVerblijfsrstatushis(Pers obj, int index) {
        String verblijfsrstatushis = String.valueOf(index);
        if (verblijfsrstatushis.length() > 1) {
            verblijfsrstatushis = verblijfsrstatushis.substring(0, 1);
        }
        obj.setVerblijfsrstatushis(verblijfsrstatushis);
    }
    
    public void PersDataOnDemand.setVersienr(Pers obj, int index) {
        Long versienr = new Integer(index).longValue();
        obj.setVersienr(versienr);
    }
    
    public void PersDataOnDemand.setVolgendepers(Pers obj, int index) {
        Pers volgendepers = obj;
        obj.setVolgendepers(volgendepers);
    }
    
    public void PersDataOnDemand.setVoornamen(Pers obj, int index) {
        String voornamen = "voornamen_" + index;
        if (voornamen.length() > 200) {
            voornamen = voornamen.substring(0, 200);
        }
        obj.setVoornamen(voornamen);
    }
    
    public void PersDataOnDemand.setVoornamenaanschr(Pers obj, int index) {
        String voornamenaanschr = "voornamenaanschr_" + index;
        if (voornamenaanschr.length() > 200) {
            voornamenaanschr = voornamenaanschr.substring(0, 200);
        }
        obj.setVoornamenaanschr(voornamenaanschr);
    }
    
    public void PersDataOnDemand.setVoorvoegsel(Pers obj, int index) {
        String voorvoegsel = "voorvoeg_" + index;
        if (voorvoegsel.length() > 10) {
            voorvoegsel = voorvoegsel.substring(0, 10);
        }
        obj.setVoorvoegsel(voorvoegsel);
    }
    
    public void PersDataOnDemand.setVoorvoegselaanschr(Pers obj, int index) {
        String voorvoegselaanschr = "voorvoeg_" + index;
        if (voorvoegselaanschr.length() > 10) {
            voorvoegselaanschr = voorvoegselaanschr.substring(0, 10);
        }
        obj.setVoorvoegselaanschr(voorvoegselaanschr);
    }
    
    public void PersDataOnDemand.setVorigepers(Pers obj, int index) {
        Pers vorigepers = obj;
        obj.setVorigepers(vorigepers);
    }
    
    public void PersDataOnDemand.setWplgeboorte(Pers obj, int index) {
        Plaats wplgeboorte = plaatsDataOnDemand.getRandomPlaats();
        obj.setWplgeboorte(wplgeboorte);
    }
    
    public void PersDataOnDemand.setWploverlijden(Pers obj, int index) {
        Plaats wploverlijden = plaatsDataOnDemand.getRandomPlaats();
        obj.setWploverlijden(wploverlijden);
    }
    
    public Pers PersDataOnDemand.getSpecificPers(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Pers obj = data.get(index);
        Long id = obj.getId();
        return personService.findPers(id);
    }
    
    public Pers PersDataOnDemand.getRandomPers() {
        init();
        Pers obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return personService.findPers(id);
    }
    
    public boolean PersDataOnDemand.modifyPers(Pers obj) {
        return false;
    }
    
    public void PersDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = personService.findPersEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Pers' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Pers>();
        for (int i = 0; i < 10; i++) {
            Pers obj = getNewTransientPers(i);
            try {
                personService.savePers(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
