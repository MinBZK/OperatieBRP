// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Categoriesrtdoc;
import nl.bzk.brp.model.data.kern.CategoriesrtdocDataOnDemand;
import nl.bzk.brp.model.data.kern.Srtdoc;
import nl.bzk.brp.model.data.kern.SrtdocDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect SrtdocDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SrtdocDataOnDemand: @Component;
    
    private Random SrtdocDataOnDemand.rnd = new SecureRandom();
    
    private List<Srtdoc> SrtdocDataOnDemand.data;
    
    @Autowired
    private CategoriesrtdocDataOnDemand SrtdocDataOnDemand.categoriesrtdocDataOnDemand;
    
    public Srtdoc SrtdocDataOnDemand.getNewTransientSrtdoc(int index) {
        Srtdoc obj = new Srtdoc();
        setCategoriesrtdoc(obj, index);
        setOms(obj, index);
        return obj;
    }
    
    public void SrtdocDataOnDemand.setCategoriesrtdoc(Srtdoc obj, int index) {
        Categoriesrtdoc categoriesrtdoc = categoriesrtdocDataOnDemand.getRandomCategoriesrtdoc();
        obj.setCategoriesrtdoc(categoriesrtdoc);
    }
    
    public void SrtdocDataOnDemand.setOms(Srtdoc obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = new Random().nextInt(10) + oms.substring(1, 250);
        }
        obj.setOms(oms);
    }
    
    public Srtdoc SrtdocDataOnDemand.getSpecificSrtdoc(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Srtdoc obj = data.get(index);
        Integer id = obj.getId();
        return Srtdoc.findSrtdoc(id);
    }
    
    public Srtdoc SrtdocDataOnDemand.getRandomSrtdoc() {
        init();
        Srtdoc obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Srtdoc.findSrtdoc(id);
    }
    
    public boolean SrtdocDataOnDemand.modifySrtdoc(Srtdoc obj) {
        return false;
    }
    
    public void SrtdocDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Srtdoc.findSrtdocEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Srtdoc' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Srtdoc>();
        for (int i = 0; i < 10; i++) {
            Srtdoc obj = getNewTransientSrtdoc(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
