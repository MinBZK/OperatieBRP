// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Persadres;
import nl.bzk.brp.model.data.kern.PersadresDataOnDemand;
import nl.bzk.brp.model.data.kern.PersadresIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PersadresIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersadresIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersadresIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersadresIntegrationTest: @Transactional;
    
    @Autowired
    private PersadresDataOnDemand PersadresIntegrationTest.dod;
    
    @Test
    public void PersadresIntegrationTest.testCountPersadreses() {
        Assert.assertNotNull("Data on demand for 'Persadres' failed to initialize correctly", dod.getRandomPersadres());
        long count = Persadres.countPersadreses();
        Assert.assertTrue("Counter for 'Persadres' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersadresIntegrationTest.testFindPersadres() {
        Persadres obj = dod.getRandomPersadres();
        Assert.assertNotNull("Data on demand for 'Persadres' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persadres' failed to provide an identifier", id);
        obj = Persadres.findPersadres(id);
        Assert.assertNotNull("Find method for 'Persadres' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Persadres' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersadresIntegrationTest.testFindAllPersadreses() {
        Assert.assertNotNull("Data on demand for 'Persadres' failed to initialize correctly", dod.getRandomPersadres());
        long count = Persadres.countPersadreses();
        Assert.assertTrue("Too expensive to perform a find all test for 'Persadres', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Persadres> result = Persadres.findAllPersadreses();
        Assert.assertNotNull("Find all method for 'Persadres' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Persadres' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersadresIntegrationTest.testFindPersadresEntries() {
        Assert.assertNotNull("Data on demand for 'Persadres' failed to initialize correctly", dod.getRandomPersadres());
        long count = Persadres.countPersadreses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Persadres> result = Persadres.findPersadresEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Persadres' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Persadres' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersadresIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Persadres' failed to initialize correctly", dod.getRandomPersadres());
        Persadres obj = dod.getNewTransientPersadres(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Persadres' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Persadres' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Persadres' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersadresIntegrationTest.testRemove() {
        Persadres obj = dod.getRandomPersadres();
        Assert.assertNotNull("Data on demand for 'Persadres' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persadres' failed to provide an identifier", id);
        obj = Persadres.findPersadres(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Persadres' with identifier '" + id + "'", Persadres.findPersadres(id));
    }
    
}
