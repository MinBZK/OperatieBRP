// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.Srtbijhouding;
import nl.bzk.brp.model.data.autaut.SrtbijhoudingDataOnDemand;
import nl.bzk.brp.model.data.autaut.SrtbijhoudingIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SrtbijhoudingIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SrtbijhoudingIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SrtbijhoudingIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SrtbijhoudingIntegrationTest: @Transactional;
    
    @Autowired
    private SrtbijhoudingDataOnDemand SrtbijhoudingIntegrationTest.dod;
    
    @Test
    public void SrtbijhoudingIntegrationTest.testCountSrtbijhoudings() {
        Assert.assertNotNull("Data on demand for 'Srtbijhouding' failed to initialize correctly", dod.getRandomSrtbijhouding());
        long count = Srtbijhouding.countSrtbijhoudings();
        Assert.assertTrue("Counter for 'Srtbijhouding' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SrtbijhoudingIntegrationTest.testFindSrtbijhouding() {
        Srtbijhouding obj = dod.getRandomSrtbijhouding();
        Assert.assertNotNull("Data on demand for 'Srtbijhouding' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtbijhouding' failed to provide an identifier", id);
        obj = Srtbijhouding.findSrtbijhouding(id);
        Assert.assertNotNull("Find method for 'Srtbijhouding' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Srtbijhouding' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SrtbijhoudingIntegrationTest.testFindAllSrtbijhoudings() {
        Assert.assertNotNull("Data on demand for 'Srtbijhouding' failed to initialize correctly", dod.getRandomSrtbijhouding());
        long count = Srtbijhouding.countSrtbijhoudings();
        Assert.assertTrue("Too expensive to perform a find all test for 'Srtbijhouding', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Srtbijhouding> result = Srtbijhouding.findAllSrtbijhoudings();
        Assert.assertNotNull("Find all method for 'Srtbijhouding' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Srtbijhouding' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SrtbijhoudingIntegrationTest.testFindSrtbijhoudingEntries() {
        Assert.assertNotNull("Data on demand for 'Srtbijhouding' failed to initialize correctly", dod.getRandomSrtbijhouding());
        long count = Srtbijhouding.countSrtbijhoudings();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Srtbijhouding> result = Srtbijhouding.findSrtbijhoudingEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Srtbijhouding' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Srtbijhouding' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SrtbijhoudingIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Srtbijhouding' failed to initialize correctly", dod.getRandomSrtbijhouding());
        Srtbijhouding obj = dod.getNewTransientSrtbijhouding(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Srtbijhouding' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Srtbijhouding' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Srtbijhouding' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SrtbijhoudingIntegrationTest.testRemove() {
        Srtbijhouding obj = dod.getRandomSrtbijhouding();
        Assert.assertNotNull("Data on demand for 'Srtbijhouding' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtbijhouding' failed to provide an identifier", id);
        obj = Srtbijhouding.findSrtbijhouding(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Srtbijhouding' with identifier '" + id + "'", Srtbijhouding.findSrtbijhouding(id));
    }
    
}
