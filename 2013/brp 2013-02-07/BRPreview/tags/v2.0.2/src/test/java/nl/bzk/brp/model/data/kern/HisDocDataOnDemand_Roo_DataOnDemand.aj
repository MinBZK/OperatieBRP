// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Actie;
import nl.bzk.brp.model.data.kern.ActieDataOnDemand;
import nl.bzk.brp.model.data.kern.Doc;
import nl.bzk.brp.model.data.kern.DocDataOnDemand;
import nl.bzk.brp.model.data.kern.HisDoc;
import nl.bzk.brp.model.data.kern.HisDocDataOnDemand;
import nl.bzk.brp.model.data.kern.Partij;
import nl.bzk.brp.model.data.kern.PartijDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect HisDocDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HisDocDataOnDemand: @Component;
    
    private Random HisDocDataOnDemand.rnd = new SecureRandom();
    
    private List<HisDoc> HisDocDataOnDemand.data;
    
    @Autowired
    private ActieDataOnDemand HisDocDataOnDemand.actieDataOnDemand;
    
    @Autowired
    private DocDataOnDemand HisDocDataOnDemand.docDataOnDemand;
    
    @Autowired
    private PartijDataOnDemand HisDocDataOnDemand.partijDataOnDemand;
    
    public HisDoc HisDocDataOnDemand.getNewTransientHisDoc(int index) {
        HisDoc obj = new HisDoc();
        setActieinh(obj, index);
        setActieverval(obj, index);
        setAktenr(obj, index);
        setDoc(obj, index);
        setIdent(obj, index);
        setOms(obj, index);
        setPartij(obj, index);
        setTsreg(obj, index);
        setTsverval(obj, index);
        return obj;
    }
    
    public void HisDocDataOnDemand.setActieinh(HisDoc obj, int index) {
        Actie actieinh = actieDataOnDemand.getRandomActie();
        obj.setActieinh(actieinh);
    }
    
    public void HisDocDataOnDemand.setActieverval(HisDoc obj, int index) {
        Actie actieverval = actieDataOnDemand.getRandomActie();
        obj.setActieverval(actieverval);
    }
    
    public void HisDocDataOnDemand.setAktenr(HisDoc obj, int index) {
        String aktenr = "akten_" + index;
        if (aktenr.length() > 7) {
            aktenr = aktenr.substring(0, 7);
        }
        obj.setAktenr(aktenr);
    }
    
    public void HisDocDataOnDemand.setDoc(HisDoc obj, int index) {
        Doc doc = docDataOnDemand.getRandomDoc();
        obj.setDoc(doc);
    }
    
    public void HisDocDataOnDemand.setIdent(HisDoc obj, int index) {
        String ident = "ident_" + index;
        if (ident.length() > 20) {
            ident = ident.substring(0, 20);
        }
        obj.setIdent(ident);
    }
    
    public void HisDocDataOnDemand.setOms(HisDoc obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 80) {
            oms = oms.substring(0, 80);
        }
        obj.setOms(oms);
    }
    
    public void HisDocDataOnDemand.setPartij(HisDoc obj, int index) {
        Partij partij = partijDataOnDemand.getRandomPartij();
        obj.setPartij(partij);
    }
    
    public void HisDocDataOnDemand.setTsreg(HisDoc obj, int index) {
        Date tsreg = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsreg(tsreg);
    }
    
    public void HisDocDataOnDemand.setTsverval(HisDoc obj, int index) {
        Date tsverval = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsverval(tsverval);
    }
    
    public HisDoc HisDocDataOnDemand.getSpecificHisDoc(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        HisDoc obj = data.get(index);
        Long id = obj.getId();
        return HisDoc.findHisDoc(id);
    }
    
    public HisDoc HisDocDataOnDemand.getRandomHisDoc() {
        init();
        HisDoc obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return HisDoc.findHisDoc(id);
    }
    
    public boolean HisDocDataOnDemand.modifyHisDoc(HisDoc obj) {
        return false;
    }
    
    public void HisDocDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = HisDoc.findHisDocEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'HisDoc' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<HisDoc>();
        for (int i = 0; i < 10; i++) {
            HisDoc obj = getNewTransientHisDoc(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
