// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.brm;

import java.util.List;
import nl.bzk.brp.model.data.brm.Regel;
import nl.bzk.brp.model.data.brm.RegelDataOnDemand;
import nl.bzk.brp.model.data.brm.RegelIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect RegelIntegrationTest_Roo_IntegrationTest {
    
    declare @type: RegelIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: RegelIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: RegelIntegrationTest: @Transactional;
    
    @Autowired
    private RegelDataOnDemand RegelIntegrationTest.dod;
    
    @Test
    public void RegelIntegrationTest.testCountRegels() {
        Assert.assertNotNull("Data on demand for 'Regel' failed to initialize correctly", dod.getRandomRegel());
        long count = Regel.countRegels();
        Assert.assertTrue("Counter for 'Regel' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void RegelIntegrationTest.testFindRegel() {
        Regel obj = dod.getRandomRegel();
        Assert.assertNotNull("Data on demand for 'Regel' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Regel' failed to provide an identifier", id);
        obj = Regel.findRegel(id);
        Assert.assertNotNull("Find method for 'Regel' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Regel' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void RegelIntegrationTest.testFindAllRegels() {
        Assert.assertNotNull("Data on demand for 'Regel' failed to initialize correctly", dod.getRandomRegel());
        long count = Regel.countRegels();
        Assert.assertTrue("Too expensive to perform a find all test for 'Regel', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Regel> result = Regel.findAllRegels();
        Assert.assertNotNull("Find all method for 'Regel' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Regel' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void RegelIntegrationTest.testFindRegelEntries() {
        Assert.assertNotNull("Data on demand for 'Regel' failed to initialize correctly", dod.getRandomRegel());
        long count = Regel.countRegels();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Regel> result = Regel.findRegelEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Regel' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Regel' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void RegelIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Regel' failed to initialize correctly", dod.getRandomRegel());
        Regel obj = dod.getNewTransientRegel(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Regel' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Regel' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Regel' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void RegelIntegrationTest.testRemove() {
        Regel obj = dod.getRandomRegel();
        Assert.assertNotNull("Data on demand for 'Regel' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Regel' failed to provide an identifier", id);
        obj = Regel.findRegel(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Regel' with identifier '" + id + "'", Regel.findRegel(id));
    }
    
}
