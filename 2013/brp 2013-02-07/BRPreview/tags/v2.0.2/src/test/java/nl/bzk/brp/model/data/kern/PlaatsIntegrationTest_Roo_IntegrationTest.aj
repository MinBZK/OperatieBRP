// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Plaats;
import nl.bzk.brp.model.data.kern.PlaatsDataOnDemand;
import nl.bzk.brp.model.data.kern.PlaatsIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PlaatsIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PlaatsIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PlaatsIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PlaatsIntegrationTest: @Transactional;
    
    @Autowired
    private PlaatsDataOnDemand PlaatsIntegrationTest.dod;
    
    @Test
    public void PlaatsIntegrationTest.testCountPlaatses() {
        Assert.assertNotNull("Data on demand for 'Plaats' failed to initialize correctly", dod.getRandomPlaats());
        long count = Plaats.countPlaatses();
        Assert.assertTrue("Counter for 'Plaats' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PlaatsIntegrationTest.testFindPlaats() {
        Plaats obj = dod.getRandomPlaats();
        Assert.assertNotNull("Data on demand for 'Plaats' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Plaats' failed to provide an identifier", id);
        obj = Plaats.findPlaats(id);
        Assert.assertNotNull("Find method for 'Plaats' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Plaats' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PlaatsIntegrationTest.testFindAllPlaatses() {
        Assert.assertNotNull("Data on demand for 'Plaats' failed to initialize correctly", dod.getRandomPlaats());
        long count = Plaats.countPlaatses();
        Assert.assertTrue("Too expensive to perform a find all test for 'Plaats', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Plaats> result = Plaats.findAllPlaatses();
        Assert.assertNotNull("Find all method for 'Plaats' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Plaats' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PlaatsIntegrationTest.testFindPlaatsEntries() {
        Assert.assertNotNull("Data on demand for 'Plaats' failed to initialize correctly", dod.getRandomPlaats());
        long count = Plaats.countPlaatses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Plaats> result = Plaats.findPlaatsEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Plaats' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Plaats' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PlaatsIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Plaats' failed to initialize correctly", dod.getRandomPlaats());
        Plaats obj = dod.getNewTransientPlaats(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Plaats' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Plaats' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Plaats' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PlaatsIntegrationTest.testRemove() {
        Plaats obj = dod.getRandomPlaats();
        Assert.assertNotNull("Data on demand for 'Plaats' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Plaats' failed to provide an identifier", id);
        obj = Plaats.findPlaats(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Plaats' with identifier '" + id + "'", Plaats.findPlaats(id));
    }
    
}
