// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Autvanafgiftereisdoc;
import nl.bzk.brp.model.data.kern.AutvanafgiftereisdocDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect AutvanafgiftereisdocDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AutvanafgiftereisdocDataOnDemand: @Component;
    
    private Random AutvanafgiftereisdocDataOnDemand.rnd = new SecureRandom();
    
    private List<Autvanafgiftereisdoc> AutvanafgiftereisdocDataOnDemand.data;
    
    public Autvanafgiftereisdoc AutvanafgiftereisdocDataOnDemand.getNewTransientAutvanafgiftereisdoc(int index) {
        Autvanafgiftereisdoc obj = new Autvanafgiftereisdoc();
        setCode(obj, index);
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setOms(obj, index);
        return obj;
    }
    
    public void AutvanafgiftereisdocDataOnDemand.setCode(Autvanafgiftereisdoc obj, int index) {
        String code = "code_" + index;
        if (code.length() > 6) {
            code = new Random().nextInt(10) + code.substring(1, 6);
        }
        obj.setCode(code);
    }
    
    public void AutvanafgiftereisdocDataOnDemand.setDataanvgel(Autvanafgiftereisdoc obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void AutvanafgiftereisdocDataOnDemand.setDateindegel(Autvanafgiftereisdoc obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void AutvanafgiftereisdocDataOnDemand.setOms(Autvanafgiftereisdoc obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = new Random().nextInt(10) + oms.substring(1, 250);
        }
        obj.setOms(oms);
    }
    
    public Autvanafgiftereisdoc AutvanafgiftereisdocDataOnDemand.getSpecificAutvanafgiftereisdoc(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Autvanafgiftereisdoc obj = data.get(index);
        Integer id = obj.getId();
        return Autvanafgiftereisdoc.findAutvanafgiftereisdoc(id);
    }
    
    public Autvanafgiftereisdoc AutvanafgiftereisdocDataOnDemand.getRandomAutvanafgiftereisdoc() {
        init();
        Autvanafgiftereisdoc obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Autvanafgiftereisdoc.findAutvanafgiftereisdoc(id);
    }
    
    public boolean AutvanafgiftereisdocDataOnDemand.modifyAutvanafgiftereisdoc(Autvanafgiftereisdoc obj) {
        return false;
    }
    
    public void AutvanafgiftereisdocDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Autvanafgiftereisdoc.findAutvanafgiftereisdocEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Autvanafgiftereisdoc' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Autvanafgiftereisdoc>();
        for (int i = 0; i < 10; i++) {
            Autvanafgiftereisdoc obj = getNewTransientAutvanafgiftereisdoc(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
