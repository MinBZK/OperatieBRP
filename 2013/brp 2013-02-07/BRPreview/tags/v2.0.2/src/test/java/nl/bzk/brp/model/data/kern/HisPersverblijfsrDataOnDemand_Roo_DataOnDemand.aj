// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Actie;
import nl.bzk.brp.model.data.kern.ActieDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPersverblijfsr;
import nl.bzk.brp.model.data.kern.HisPersverblijfsrDataOnDemand;
import nl.bzk.brp.model.data.kern.Pers;
import nl.bzk.brp.model.data.kern.PersDataOnDemand;
import nl.bzk.brp.model.data.kern.Verblijfsr;
import nl.bzk.brp.model.data.kern.VerblijfsrDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect HisPersverblijfsrDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HisPersverblijfsrDataOnDemand: @Component;
    
    private Random HisPersverblijfsrDataOnDemand.rnd = new SecureRandom();
    
    private List<HisPersverblijfsr> HisPersverblijfsrDataOnDemand.data;
    
    @Autowired
    private ActieDataOnDemand HisPersverblijfsrDataOnDemand.actieDataOnDemand;
    
    @Autowired
    private PersDataOnDemand HisPersverblijfsrDataOnDemand.persDataOnDemand;
    
    @Autowired
    private VerblijfsrDataOnDemand HisPersverblijfsrDataOnDemand.verblijfsrDataOnDemand;
    
    public HisPersverblijfsr HisPersverblijfsrDataOnDemand.getNewTransientHisPersverblijfsr(int index) {
        HisPersverblijfsr obj = new HisPersverblijfsr();
        setActieaanpgel(obj, index);
        setActieinh(obj, index);
        setActieverval(obj, index);
        setDataanvaaneenslverblijfsr(obj, index);
        setDataanvgel(obj, index);
        setDataanvverblijfsr(obj, index);
        setDateindegel(obj, index);
        setDatvoorzeindeverblijfsr(obj, index);
        setPers(obj, index);
        setTsreg(obj, index);
        setTsverval(obj, index);
        setVerblijfsr(obj, index);
        return obj;
    }
    
    public void HisPersverblijfsrDataOnDemand.setActieaanpgel(HisPersverblijfsr obj, int index) {
        Actie actieaanpgel = actieDataOnDemand.getRandomActie();
        obj.setActieaanpgel(actieaanpgel);
    }
    
    public void HisPersverblijfsrDataOnDemand.setActieinh(HisPersverblijfsr obj, int index) {
        Actie actieinh = actieDataOnDemand.getRandomActie();
        obj.setActieinh(actieinh);
    }
    
    public void HisPersverblijfsrDataOnDemand.setActieverval(HisPersverblijfsr obj, int index) {
        Actie actieverval = actieDataOnDemand.getRandomActie();
        obj.setActieverval(actieverval);
    }
    
    public void HisPersverblijfsrDataOnDemand.setDataanvaaneenslverblijfsr(HisPersverblijfsr obj, int index) {
        BigDecimal dataanvaaneenslverblijfsr = BigDecimal.valueOf(index);
        obj.setDataanvaaneenslverblijfsr(dataanvaaneenslverblijfsr);
    }
    
    public void HisPersverblijfsrDataOnDemand.setDataanvgel(HisPersverblijfsr obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void HisPersverblijfsrDataOnDemand.setDataanvverblijfsr(HisPersverblijfsr obj, int index) {
        BigDecimal dataanvverblijfsr = BigDecimal.valueOf(index);
        obj.setDataanvverblijfsr(dataanvverblijfsr);
    }
    
    public void HisPersverblijfsrDataOnDemand.setDateindegel(HisPersverblijfsr obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void HisPersverblijfsrDataOnDemand.setDatvoorzeindeverblijfsr(HisPersverblijfsr obj, int index) {
        BigDecimal datvoorzeindeverblijfsr = BigDecimal.valueOf(index);
        obj.setDatvoorzeindeverblijfsr(datvoorzeindeverblijfsr);
    }
    
    public void HisPersverblijfsrDataOnDemand.setPers(HisPersverblijfsr obj, int index) {
        Pers pers = persDataOnDemand.getRandomPers();
        obj.setPers(pers);
    }
    
    public void HisPersverblijfsrDataOnDemand.setTsreg(HisPersverblijfsr obj, int index) {
        Date tsreg = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsreg(tsreg);
    }
    
    public void HisPersverblijfsrDataOnDemand.setTsverval(HisPersverblijfsr obj, int index) {
        Date tsverval = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsverval(tsverval);
    }
    
    public void HisPersverblijfsrDataOnDemand.setVerblijfsr(HisPersverblijfsr obj, int index) {
        Verblijfsr verblijfsr = verblijfsrDataOnDemand.getRandomVerblijfsr();
        obj.setVerblijfsr(verblijfsr);
    }
    
    public HisPersverblijfsr HisPersverblijfsrDataOnDemand.getSpecificHisPersverblijfsr(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        HisPersverblijfsr obj = data.get(index);
        Long id = obj.getId();
        return HisPersverblijfsr.findHisPersverblijfsr(id);
    }
    
    public HisPersverblijfsr HisPersverblijfsrDataOnDemand.getRandomHisPersverblijfsr() {
        init();
        HisPersverblijfsr obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return HisPersverblijfsr.findHisPersverblijfsr(id);
    }
    
    public boolean HisPersverblijfsrDataOnDemand.modifyHisPersverblijfsr(HisPersverblijfsr obj) {
        return false;
    }
    
    public void HisPersverblijfsrDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = HisPersverblijfsr.findHisPersverblijfsrEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'HisPersverblijfsr' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<HisPersverblijfsr>();
        for (int i = 0; i < 10; i++) {
            HisPersverblijfsr obj = getNewTransientHisPersverblijfsr(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
