// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.lev;

import java.util.List;
import nl.bzk.brp.model.data.lev.HisAbonnement;
import nl.bzk.brp.model.data.lev.HisAbonnementDataOnDemand;
import nl.bzk.brp.model.data.lev.HisAbonnementIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisAbonnementIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisAbonnementIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisAbonnementIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisAbonnementIntegrationTest: @Transactional;
    
    @Autowired
    private HisAbonnementDataOnDemand HisAbonnementIntegrationTest.dod;
    
    @Test
    public void HisAbonnementIntegrationTest.testCountHisAbonnements() {
        Assert.assertNotNull("Data on demand for 'HisAbonnement' failed to initialize correctly", dod.getRandomHisAbonnement());
        long count = HisAbonnement.countHisAbonnements();
        Assert.assertTrue("Counter for 'HisAbonnement' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisAbonnementIntegrationTest.testFindHisAbonnement() {
        HisAbonnement obj = dod.getRandomHisAbonnement();
        Assert.assertNotNull("Data on demand for 'HisAbonnement' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisAbonnement' failed to provide an identifier", id);
        obj = HisAbonnement.findHisAbonnement(id);
        Assert.assertNotNull("Find method for 'HisAbonnement' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisAbonnement' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisAbonnementIntegrationTest.testFindAllHisAbonnements() {
        Assert.assertNotNull("Data on demand for 'HisAbonnement' failed to initialize correctly", dod.getRandomHisAbonnement());
        long count = HisAbonnement.countHisAbonnements();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisAbonnement', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisAbonnement> result = HisAbonnement.findAllHisAbonnements();
        Assert.assertNotNull("Find all method for 'HisAbonnement' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisAbonnement' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisAbonnementIntegrationTest.testFindHisAbonnementEntries() {
        Assert.assertNotNull("Data on demand for 'HisAbonnement' failed to initialize correctly", dod.getRandomHisAbonnement());
        long count = HisAbonnement.countHisAbonnements();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisAbonnement> result = HisAbonnement.findHisAbonnementEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisAbonnement' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisAbonnement' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisAbonnementIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisAbonnement' failed to initialize correctly", dod.getRandomHisAbonnement());
        HisAbonnement obj = dod.getNewTransientHisAbonnement(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisAbonnement' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisAbonnement' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisAbonnement' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisAbonnementIntegrationTest.testRemove() {
        HisAbonnement obj = dod.getRandomHisAbonnement();
        Assert.assertNotNull("Data on demand for 'HisAbonnement' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisAbonnement' failed to provide an identifier", id);
        obj = HisAbonnement.findHisAbonnement(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisAbonnement' with identifier '" + id + "'", HisAbonnement.findHisAbonnement(id));
    }
    
}
