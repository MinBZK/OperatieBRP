// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.lev;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.lev.Srtabonnement;
import nl.bzk.brp.model.data.lev.SrtabonnementDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect SrtabonnementDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SrtabonnementDataOnDemand: @Component;
    
    private Random SrtabonnementDataOnDemand.rnd = new SecureRandom();
    
    private List<Srtabonnement> SrtabonnementDataOnDemand.data;
    
    public Srtabonnement SrtabonnementDataOnDemand.getNewTransientSrtabonnement(int index) {
        Srtabonnement obj = new Srtabonnement();
        setNaam(obj, index);
        return obj;
    }
    
    public void SrtabonnementDataOnDemand.setNaam(Srtabonnement obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public Srtabonnement SrtabonnementDataOnDemand.getSpecificSrtabonnement(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Srtabonnement obj = data.get(index);
        Short id = obj.getId();
        return Srtabonnement.findSrtabonnement(id);
    }
    
    public Srtabonnement SrtabonnementDataOnDemand.getRandomSrtabonnement() {
        init();
        Srtabonnement obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Srtabonnement.findSrtabonnement(id);
    }
    
    public boolean SrtabonnementDataOnDemand.modifySrtabonnement(Srtabonnement obj) {
        return false;
    }
    
    public void SrtabonnementDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Srtabonnement.findSrtabonnementEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Srtabonnement' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Srtabonnement>();
        for (int i = 0; i < 10; i++) {
            Srtabonnement obj = getNewTransientSrtabonnement(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
