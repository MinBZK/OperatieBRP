// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.Functie;
import nl.bzk.brp.model.data.autaut.FunctieDataOnDemand;
import nl.bzk.brp.model.data.autaut.FunctieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect FunctieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: FunctieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: FunctieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: FunctieIntegrationTest: @Transactional;
    
    @Autowired
    private FunctieDataOnDemand FunctieIntegrationTest.dod;
    
    @Test
    public void FunctieIntegrationTest.testCountFuncties() {
        Assert.assertNotNull("Data on demand for 'Functie' failed to initialize correctly", dod.getRandomFunctie());
        long count = Functie.countFuncties();
        Assert.assertTrue("Counter for 'Functie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void FunctieIntegrationTest.testFindFunctie() {
        Functie obj = dod.getRandomFunctie();
        Assert.assertNotNull("Data on demand for 'Functie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Functie' failed to provide an identifier", id);
        obj = Functie.findFunctie(id);
        Assert.assertNotNull("Find method for 'Functie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Functie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void FunctieIntegrationTest.testFindAllFuncties() {
        Assert.assertNotNull("Data on demand for 'Functie' failed to initialize correctly", dod.getRandomFunctie());
        long count = Functie.countFuncties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Functie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Functie> result = Functie.findAllFuncties();
        Assert.assertNotNull("Find all method for 'Functie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Functie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void FunctieIntegrationTest.testFindFunctieEntries() {
        Assert.assertNotNull("Data on demand for 'Functie' failed to initialize correctly", dod.getRandomFunctie());
        long count = Functie.countFuncties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Functie> result = Functie.findFunctieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Functie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Functie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void FunctieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Functie' failed to initialize correctly", dod.getRandomFunctie());
        Functie obj = dod.getNewTransientFunctie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Functie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Functie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Functie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void FunctieIntegrationTest.testRemove() {
        Functie obj = dod.getRandomFunctie();
        Assert.assertNotNull("Data on demand for 'Functie' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Functie' failed to provide an identifier", id);
        obj = Functie.findFunctie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Functie' with identifier '" + id + "'", Functie.findFunctie(id));
    }
    
}
