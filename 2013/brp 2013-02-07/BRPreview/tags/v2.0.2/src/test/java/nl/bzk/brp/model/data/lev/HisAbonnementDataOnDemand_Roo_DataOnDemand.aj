// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.lev;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Actie;
import nl.bzk.brp.model.data.kern.ActieDataOnDemand;
import nl.bzk.brp.model.data.lev.Abonnement;
import nl.bzk.brp.model.data.lev.AbonnementDataOnDemand;
import nl.bzk.brp.model.data.lev.HisAbonnement;
import nl.bzk.brp.model.data.lev.HisAbonnementDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect HisAbonnementDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HisAbonnementDataOnDemand: @Component;
    
    private Random HisAbonnementDataOnDemand.rnd = new SecureRandom();
    
    private List<HisAbonnement> HisAbonnementDataOnDemand.data;
    
    @Autowired
    private AbonnementDataOnDemand HisAbonnementDataOnDemand.abonnementDataOnDemand;
    
    @Autowired
    private ActieDataOnDemand HisAbonnementDataOnDemand.actieDataOnDemand;
    
    public HisAbonnement HisAbonnementDataOnDemand.getNewTransientHisAbonnement(int index) {
        HisAbonnement obj = new HisAbonnement();
        setAbonnement(obj, index);
        setActieinh(obj, index);
        setActieverval(obj, index);
        setPopulatiecriterium(obj, index);
        setTsreg(obj, index);
        setTsverval(obj, index);
        return obj;
    }
    
    public void HisAbonnementDataOnDemand.setAbonnement(HisAbonnement obj, int index) {
        Abonnement abonnement = abonnementDataOnDemand.getRandomAbonnement();
        obj.setAbonnement(abonnement);
    }
    
    public void HisAbonnementDataOnDemand.setActieinh(HisAbonnement obj, int index) {
        Actie actieinh = actieDataOnDemand.getRandomActie();
        obj.setActieinh(actieinh);
    }
    
    public void HisAbonnementDataOnDemand.setActieverval(HisAbonnement obj, int index) {
        Actie actieverval = actieDataOnDemand.getRandomActie();
        obj.setActieverval(actieverval);
    }
    
    public void HisAbonnementDataOnDemand.setPopulatiecriterium(HisAbonnement obj, int index) {
        String populatiecriterium = "populatiecriterium_" + index;
        obj.setPopulatiecriterium(populatiecriterium);
    }
    
    public void HisAbonnementDataOnDemand.setTsreg(HisAbonnement obj, int index) {
        Date tsreg = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsreg(tsreg);
    }
    
    public void HisAbonnementDataOnDemand.setTsverval(HisAbonnement obj, int index) {
        Date tsverval = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsverval(tsverval);
    }
    
    public HisAbonnement HisAbonnementDataOnDemand.getSpecificHisAbonnement(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        HisAbonnement obj = data.get(index);
        Long id = obj.getId();
        return HisAbonnement.findHisAbonnement(id);
    }
    
    public HisAbonnement HisAbonnementDataOnDemand.getRandomHisAbonnement() {
        init();
        HisAbonnement obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return HisAbonnement.findHisAbonnement(id);
    }
    
    public boolean HisAbonnementDataOnDemand.modifyHisAbonnement(HisAbonnement obj) {
        return false;
    }
    
    public void HisAbonnementDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = HisAbonnement.findHisAbonnementEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'HisAbonnement' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<HisAbonnement>();
        for (int i = 0; i < 10; i++) {
            HisAbonnement obj = getNewTransientHisAbonnement(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
