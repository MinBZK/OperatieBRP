// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Srtverificatie;
import nl.bzk.brp.model.data.kern.SrtverificatieDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect SrtverificatieDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SrtverificatieDataOnDemand: @Component;
    
    private Random SrtverificatieDataOnDemand.rnd = new SecureRandom();
    
    private List<Srtverificatie> SrtverificatieDataOnDemand.data;
    
    public Srtverificatie SrtverificatieDataOnDemand.getNewTransientSrtverificatie(int index) {
        Srtverificatie obj = new Srtverificatie();
        setNaam(obj, index);
        return obj;
    }
    
    public void SrtverificatieDataOnDemand.setNaam(Srtverificatie obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public Srtverificatie SrtverificatieDataOnDemand.getSpecificSrtverificatie(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Srtverificatie obj = data.get(index);
        Integer id = obj.getId();
        return Srtverificatie.findSrtverificatie(id);
    }
    
    public Srtverificatie SrtverificatieDataOnDemand.getRandomSrtverificatie() {
        init();
        Srtverificatie obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Srtverificatie.findSrtverificatie(id);
    }
    
    public boolean SrtverificatieDataOnDemand.modifySrtverificatie(Srtverificatie obj) {
        return false;
    }
    
    public void SrtverificatieDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Srtverificatie.findSrtverificatieEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Srtverificatie' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Srtverificatie>();
        for (int i = 0; i < 10; i++) {
            Srtverificatie obj = getNewTransientSrtverificatie(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
