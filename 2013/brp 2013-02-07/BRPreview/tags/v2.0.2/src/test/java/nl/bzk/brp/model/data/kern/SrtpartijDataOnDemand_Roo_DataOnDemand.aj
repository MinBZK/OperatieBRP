// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Srtpartij;
import nl.bzk.brp.model.data.kern.SrtpartijDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect SrtpartijDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SrtpartijDataOnDemand: @Component;
    
    private Random SrtpartijDataOnDemand.rnd = new SecureRandom();
    
    private List<Srtpartij> SrtpartijDataOnDemand.data;
    
    public Srtpartij SrtpartijDataOnDemand.getNewTransientSrtpartij(int index) {
        Srtpartij obj = new Srtpartij();
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setNaam(obj, index);
        return obj;
    }
    
    public void SrtpartijDataOnDemand.setDataanvgel(Srtpartij obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void SrtpartijDataOnDemand.setDateindegel(Srtpartij obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void SrtpartijDataOnDemand.setNaam(Srtpartij obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = naam.substring(0, 40);
        }
        obj.setNaam(naam);
    }
    
    public Srtpartij SrtpartijDataOnDemand.getSpecificSrtpartij(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Srtpartij obj = data.get(index);
        Integer id = obj.getId();
        return Srtpartij.findSrtpartij(id);
    }
    
    public Srtpartij SrtpartijDataOnDemand.getRandomSrtpartij() {
        init();
        Srtpartij obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Srtpartij.findSrtpartij(id);
    }
    
    public boolean SrtpartijDataOnDemand.modifySrtpartij(Srtpartij obj) {
        return false;
    }
    
    public void SrtpartijDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Srtpartij.findSrtpartijEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Srtpartij' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Srtpartij>();
        for (int i = 0; i < 10; i++) {
            Srtpartij obj = getNewTransientSrtpartij(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
