// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Land;
import nl.bzk.brp.model.data.kern.LandDataOnDemand;
import nl.bzk.brp.model.data.kern.LandIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect LandIntegrationTest_Roo_IntegrationTest {
    
    declare @type: LandIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: LandIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: LandIntegrationTest: @Transactional;
    
    @Autowired
    private LandDataOnDemand LandIntegrationTest.dod;
    
    @Test
    public void LandIntegrationTest.testCountLands() {
        Assert.assertNotNull("Data on demand for 'Land' failed to initialize correctly", dod.getRandomLand());
        long count = Land.countLands();
        Assert.assertTrue("Counter for 'Land' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void LandIntegrationTest.testFindLand() {
        Land obj = dod.getRandomLand();
        Assert.assertNotNull("Data on demand for 'Land' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Land' failed to provide an identifier", id);
        obj = Land.findLand(id);
        Assert.assertNotNull("Find method for 'Land' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Land' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void LandIntegrationTest.testFindAllLands() {
        Assert.assertNotNull("Data on demand for 'Land' failed to initialize correctly", dod.getRandomLand());
        long count = Land.countLands();
        Assert.assertTrue("Too expensive to perform a find all test for 'Land', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Land> result = Land.findAllLands();
        Assert.assertNotNull("Find all method for 'Land' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Land' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void LandIntegrationTest.testFindLandEntries() {
        Assert.assertNotNull("Data on demand for 'Land' failed to initialize correctly", dod.getRandomLand());
        long count = Land.countLands();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Land> result = Land.findLandEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Land' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Land' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void LandIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Land' failed to initialize correctly", dod.getRandomLand());
        Land obj = dod.getNewTransientLand(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Land' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Land' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Land' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void LandIntegrationTest.testRemove() {
        Land obj = dod.getRandomLand();
        Assert.assertNotNull("Data on demand for 'Land' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Land' failed to provide an identifier", id);
        obj = Land.findLand(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Land' with identifier '" + id + "'", Land.findLand(id));
    }
    
}
