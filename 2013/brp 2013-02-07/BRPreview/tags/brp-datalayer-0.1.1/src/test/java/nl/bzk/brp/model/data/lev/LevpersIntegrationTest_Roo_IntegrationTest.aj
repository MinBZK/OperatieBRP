// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.lev;

import java.util.List;
import nl.bzk.brp.model.data.lev.Levpers;
import nl.bzk.brp.model.data.lev.LevpersDataOnDemand;
import nl.bzk.brp.model.data.lev.LevpersIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect LevpersIntegrationTest_Roo_IntegrationTest {
    
    declare @type: LevpersIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: LevpersIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: LevpersIntegrationTest: @Transactional;
    
    @Autowired
    private LevpersDataOnDemand LevpersIntegrationTest.dod;
    
    @Test
    public void LevpersIntegrationTest.testCountLevperses() {
        Assert.assertNotNull("Data on demand for 'Levpers' failed to initialize correctly", dod.getRandomLevpers());
        long count = Levpers.countLevperses();
        Assert.assertTrue("Counter for 'Levpers' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void LevpersIntegrationTest.testFindLevpers() {
        Levpers obj = dod.getRandomLevpers();
        Assert.assertNotNull("Data on demand for 'Levpers' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Levpers' failed to provide an identifier", id);
        obj = Levpers.findLevpers(id);
        Assert.assertNotNull("Find method for 'Levpers' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Levpers' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void LevpersIntegrationTest.testFindAllLevperses() {
        Assert.assertNotNull("Data on demand for 'Levpers' failed to initialize correctly", dod.getRandomLevpers());
        long count = Levpers.countLevperses();
        Assert.assertTrue("Too expensive to perform a find all test for 'Levpers', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Levpers> result = Levpers.findAllLevperses();
        Assert.assertNotNull("Find all method for 'Levpers' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Levpers' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void LevpersIntegrationTest.testFindLevpersEntries() {
        Assert.assertNotNull("Data on demand for 'Levpers' failed to initialize correctly", dod.getRandomLevpers());
        long count = Levpers.countLevperses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Levpers> result = Levpers.findLevpersEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Levpers' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Levpers' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void LevpersIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Levpers' failed to initialize correctly", dod.getRandomLevpers());
        Levpers obj = dod.getNewTransientLevpers(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Levpers' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Levpers' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Levpers' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void LevpersIntegrationTest.testRemove() {
        Levpers obj = dod.getRandomLevpers();
        Assert.assertNotNull("Data on demand for 'Levpers' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Levpers' failed to provide an identifier", id);
        obj = Levpers.findLevpers(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Levpers' with identifier '" + id + "'", Levpers.findLevpers(id));
    }
    
}
