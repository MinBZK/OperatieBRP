// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Srtdoc;
import nl.bzk.brp.model.data.kern.SrtdocDataOnDemand;
import nl.bzk.brp.model.data.kern.SrtdocIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SrtdocIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SrtdocIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SrtdocIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SrtdocIntegrationTest: @Transactional;
    
    @Autowired
    private SrtdocDataOnDemand SrtdocIntegrationTest.dod;
    
    @Test
    public void SrtdocIntegrationTest.testCountSrtdocs() {
        Assert.assertNotNull("Data on demand for 'Srtdoc' failed to initialize correctly", dod.getRandomSrtdoc());
        long count = Srtdoc.countSrtdocs();
        Assert.assertTrue("Counter for 'Srtdoc' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SrtdocIntegrationTest.testFindSrtdoc() {
        Srtdoc obj = dod.getRandomSrtdoc();
        Assert.assertNotNull("Data on demand for 'Srtdoc' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtdoc' failed to provide an identifier", id);
        obj = Srtdoc.findSrtdoc(id);
        Assert.assertNotNull("Find method for 'Srtdoc' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Srtdoc' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SrtdocIntegrationTest.testFindAllSrtdocs() {
        Assert.assertNotNull("Data on demand for 'Srtdoc' failed to initialize correctly", dod.getRandomSrtdoc());
        long count = Srtdoc.countSrtdocs();
        Assert.assertTrue("Too expensive to perform a find all test for 'Srtdoc', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Srtdoc> result = Srtdoc.findAllSrtdocs();
        Assert.assertNotNull("Find all method for 'Srtdoc' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Srtdoc' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SrtdocIntegrationTest.testFindSrtdocEntries() {
        Assert.assertNotNull("Data on demand for 'Srtdoc' failed to initialize correctly", dod.getRandomSrtdoc());
        long count = Srtdoc.countSrtdocs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Srtdoc> result = Srtdoc.findSrtdocEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Srtdoc' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Srtdoc' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SrtdocIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Srtdoc' failed to initialize correctly", dod.getRandomSrtdoc());
        Srtdoc obj = dod.getNewTransientSrtdoc(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Srtdoc' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Srtdoc' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Srtdoc' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SrtdocIntegrationTest.testRemove() {
        Srtdoc obj = dod.getRandomSrtdoc();
        Assert.assertNotNull("Data on demand for 'Srtdoc' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtdoc' failed to provide an identifier", id);
        obj = Srtdoc.findSrtdoc(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Srtdoc' with identifier '" + id + "'", Srtdoc.findSrtdoc(id));
    }
    
}
