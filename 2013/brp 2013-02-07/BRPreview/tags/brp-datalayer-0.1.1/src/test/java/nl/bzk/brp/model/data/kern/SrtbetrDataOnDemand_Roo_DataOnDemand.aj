// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Srtbetr;
import nl.bzk.brp.model.data.kern.SrtbetrDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect SrtbetrDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SrtbetrDataOnDemand: @Component;
    
    private Random SrtbetrDataOnDemand.rnd = new SecureRandom();
    
    private List<Srtbetr> SrtbetrDataOnDemand.data;
    
    public Srtbetr SrtbetrDataOnDemand.getNewTransientSrtbetr(int index) {
        Srtbetr obj = new Srtbetr();
        setCode(obj, index);
        setNaam(obj, index);
        return obj;
    }
    
    public void SrtbetrDataOnDemand.setCode(Srtbetr obj, int index) {
        String code = String.valueOf(index);
        if (code.length() > 1) {
            code = new Random().nextInt(10) + code.substring(1, 1);
        }
        obj.setCode(code);
    }
    
    public void SrtbetrDataOnDemand.setNaam(Srtbetr obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public Srtbetr SrtbetrDataOnDemand.getSpecificSrtbetr(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Srtbetr obj = data.get(index);
        Short id = obj.getId();
        return Srtbetr.findSrtbetr(id);
    }
    
    public Srtbetr SrtbetrDataOnDemand.getRandomSrtbetr() {
        init();
        Srtbetr obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Srtbetr.findSrtbetr(id);
    }
    
    public boolean SrtbetrDataOnDemand.modifySrtbetr(Srtbetr obj) {
        return false;
    }
    
    public void SrtbetrDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Srtbetr.findSrtbetrEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Srtbetr' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Srtbetr>();
        for (int i = 0; i < 10; i++) {
            Srtbetr obj = getNewTransientSrtbetr(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
