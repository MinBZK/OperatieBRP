// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.HisAuthenticatiemiddel;
import nl.bzk.brp.model.data.autaut.HisAuthenticatiemiddelDataOnDemand;
import nl.bzk.brp.model.data.autaut.HisAuthenticatiemiddelIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisAuthenticatiemiddelIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisAuthenticatiemiddelIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisAuthenticatiemiddelIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisAuthenticatiemiddelIntegrationTest: @Transactional;
    
    @Autowired
    private HisAuthenticatiemiddelDataOnDemand HisAuthenticatiemiddelIntegrationTest.dod;
    
    @Test
    public void HisAuthenticatiemiddelIntegrationTest.testCountHisAuthenticatiemiddels() {
        Assert.assertNotNull("Data on demand for 'HisAuthenticatiemiddel' failed to initialize correctly", dod.getRandomHisAuthenticatiemiddel());
        long count = HisAuthenticatiemiddel.countHisAuthenticatiemiddels();
        Assert.assertTrue("Counter for 'HisAuthenticatiemiddel' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisAuthenticatiemiddelIntegrationTest.testFindHisAuthenticatiemiddel() {
        HisAuthenticatiemiddel obj = dod.getRandomHisAuthenticatiemiddel();
        Assert.assertNotNull("Data on demand for 'HisAuthenticatiemiddel' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisAuthenticatiemiddel' failed to provide an identifier", id);
        obj = HisAuthenticatiemiddel.findHisAuthenticatiemiddel(id);
        Assert.assertNotNull("Find method for 'HisAuthenticatiemiddel' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisAuthenticatiemiddel' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisAuthenticatiemiddelIntegrationTest.testFindAllHisAuthenticatiemiddels() {
        Assert.assertNotNull("Data on demand for 'HisAuthenticatiemiddel' failed to initialize correctly", dod.getRandomHisAuthenticatiemiddel());
        long count = HisAuthenticatiemiddel.countHisAuthenticatiemiddels();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisAuthenticatiemiddel', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisAuthenticatiemiddel> result = HisAuthenticatiemiddel.findAllHisAuthenticatiemiddels();
        Assert.assertNotNull("Find all method for 'HisAuthenticatiemiddel' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisAuthenticatiemiddel' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisAuthenticatiemiddelIntegrationTest.testFindHisAuthenticatiemiddelEntries() {
        Assert.assertNotNull("Data on demand for 'HisAuthenticatiemiddel' failed to initialize correctly", dod.getRandomHisAuthenticatiemiddel());
        long count = HisAuthenticatiemiddel.countHisAuthenticatiemiddels();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisAuthenticatiemiddel> result = HisAuthenticatiemiddel.findHisAuthenticatiemiddelEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisAuthenticatiemiddel' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisAuthenticatiemiddel' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisAuthenticatiemiddelIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisAuthenticatiemiddel' failed to initialize correctly", dod.getRandomHisAuthenticatiemiddel());
        HisAuthenticatiemiddel obj = dod.getNewTransientHisAuthenticatiemiddel(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisAuthenticatiemiddel' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisAuthenticatiemiddel' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisAuthenticatiemiddel' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisAuthenticatiemiddelIntegrationTest.testRemove() {
        HisAuthenticatiemiddel obj = dod.getRandomHisAuthenticatiemiddel();
        Assert.assertNotNull("Data on demand for 'HisAuthenticatiemiddel' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisAuthenticatiemiddel' failed to provide an identifier", id);
        obj = HisAuthenticatiemiddel.findHisAuthenticatiemiddel(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisAuthenticatiemiddel' with identifier '" + id + "'", HisAuthenticatiemiddel.findHisAuthenticatiemiddel(id));
    }
    
}
