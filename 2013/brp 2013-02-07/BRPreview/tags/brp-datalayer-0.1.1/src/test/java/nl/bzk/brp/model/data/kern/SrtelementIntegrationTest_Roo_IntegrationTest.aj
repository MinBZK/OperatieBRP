// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Srtelement;
import nl.bzk.brp.model.data.kern.SrtelementDataOnDemand;
import nl.bzk.brp.model.data.kern.SrtelementIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SrtelementIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SrtelementIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SrtelementIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SrtelementIntegrationTest: @Transactional;
    
    @Autowired
    private SrtelementDataOnDemand SrtelementIntegrationTest.dod;
    
    @Test
    public void SrtelementIntegrationTest.testCountSrtelements() {
        Assert.assertNotNull("Data on demand for 'Srtelement' failed to initialize correctly", dod.getRandomSrtelement());
        long count = Srtelement.countSrtelements();
        Assert.assertTrue("Counter for 'Srtelement' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SrtelementIntegrationTest.testFindSrtelement() {
        Srtelement obj = dod.getRandomSrtelement();
        Assert.assertNotNull("Data on demand for 'Srtelement' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtelement' failed to provide an identifier", id);
        obj = Srtelement.findSrtelement(id);
        Assert.assertNotNull("Find method for 'Srtelement' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Srtelement' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SrtelementIntegrationTest.testFindAllSrtelements() {
        Assert.assertNotNull("Data on demand for 'Srtelement' failed to initialize correctly", dod.getRandomSrtelement());
        long count = Srtelement.countSrtelements();
        Assert.assertTrue("Too expensive to perform a find all test for 'Srtelement', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Srtelement> result = Srtelement.findAllSrtelements();
        Assert.assertNotNull("Find all method for 'Srtelement' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Srtelement' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SrtelementIntegrationTest.testFindSrtelementEntries() {
        Assert.assertNotNull("Data on demand for 'Srtelement' failed to initialize correctly", dod.getRandomSrtelement());
        long count = Srtelement.countSrtelements();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Srtelement> result = Srtelement.findSrtelementEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Srtelement' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Srtelement' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SrtelementIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Srtelement' failed to initialize correctly", dod.getRandomSrtelement());
        Srtelement obj = dod.getNewTransientSrtelement(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Srtelement' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Srtelement' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Srtelement' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SrtelementIntegrationTest.testRemove() {
        Srtelement obj = dod.getRandomSrtelement();
        Assert.assertNotNull("Data on demand for 'Srtelement' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtelement' failed to provide an identifier", id);
        obj = Srtelement.findSrtelement(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Srtelement' with identifier '" + id + "'", Srtelement.findSrtelement(id));
    }
    
}
