// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.brm;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.brm.Regel;
import nl.bzk.brp.model.data.brm.RegelDataOnDemand;
import nl.bzk.brp.model.data.brm.Regelimplementatie;
import nl.bzk.brp.model.data.brm.RegelimplementatieDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect RegelimplementatieDataOnDemand_Roo_DataOnDemand {
    
    declare @type: RegelimplementatieDataOnDemand: @Component;
    
    private Random RegelimplementatieDataOnDemand.rnd = new SecureRandom();
    
    private List<Regelimplementatie> RegelimplementatieDataOnDemand.data;
    
    @Autowired
    private RegelDataOnDemand RegelimplementatieDataOnDemand.regelDataOnDemand;
    
    public Regelimplementatie RegelimplementatieDataOnDemand.getNewTransientRegelimplementatie(int index) {
        Regelimplementatie obj = new Regelimplementatie();
        setRegel(obj, index);
        return obj;
    }
    
    public void RegelimplementatieDataOnDemand.setRegel(Regelimplementatie obj, int index) {
        Regel regel = regelDataOnDemand.getRandomRegel();
        obj.setRegel(regel);
    }
    
    public Regelimplementatie RegelimplementatieDataOnDemand.getSpecificRegelimplementatie(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Regelimplementatie obj = data.get(index);
        Integer id = obj.getId();
        return Regelimplementatie.findRegelimplementatie(id);
    }
    
    public Regelimplementatie RegelimplementatieDataOnDemand.getRandomRegelimplementatie() {
        init();
        Regelimplementatie obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Regelimplementatie.findRegelimplementatie(id);
    }
    
    public boolean RegelimplementatieDataOnDemand.modifyRegelimplementatie(Regelimplementatie obj) {
        return false;
    }
    
    public void RegelimplementatieDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Regelimplementatie.findRegelimplementatieEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Regelimplementatie' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Regelimplementatie>();
        for (int i = 0; i < 10; i++) {
            Regelimplementatie obj = getNewTransientRegelimplementatie(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
