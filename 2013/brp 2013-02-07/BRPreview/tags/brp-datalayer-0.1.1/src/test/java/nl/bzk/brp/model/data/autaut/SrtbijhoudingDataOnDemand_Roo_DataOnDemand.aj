// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.autaut.Srtbijhouding;
import nl.bzk.brp.model.data.autaut.SrtbijhoudingDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect SrtbijhoudingDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SrtbijhoudingDataOnDemand: @Component;
    
    private Random SrtbijhoudingDataOnDemand.rnd = new SecureRandom();
    
    private List<Srtbijhouding> SrtbijhoudingDataOnDemand.data;
    
    public Srtbijhouding SrtbijhoudingDataOnDemand.getNewTransientSrtbijhouding(int index) {
        Srtbijhouding obj = new Srtbijhouding();
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setNaam(obj, index);
        setOms(obj, index);
        return obj;
    }
    
    public void SrtbijhoudingDataOnDemand.setDataanvgel(Srtbijhouding obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void SrtbijhoudingDataOnDemand.setDateindegel(Srtbijhouding obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void SrtbijhoudingDataOnDemand.setNaam(Srtbijhouding obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public void SrtbijhoudingDataOnDemand.setOms(Srtbijhouding obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = oms.substring(0, 250);
        }
        obj.setOms(oms);
    }
    
    public Srtbijhouding SrtbijhoudingDataOnDemand.getSpecificSrtbijhouding(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Srtbijhouding obj = data.get(index);
        Short id = obj.getId();
        return Srtbijhouding.findSrtbijhouding(id);
    }
    
    public Srtbijhouding SrtbijhoudingDataOnDemand.getRandomSrtbijhouding() {
        init();
        Srtbijhouding obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Srtbijhouding.findSrtbijhouding(id);
    }
    
    public boolean SrtbijhoudingDataOnDemand.modifySrtbijhouding(Srtbijhouding obj) {
        return false;
    }
    
    public void SrtbijhoudingDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Srtbijhouding.findSrtbijhoudingEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Srtbijhouding' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Srtbijhouding>();
        for (int i = 0; i < 10; i++) {
            Srtbijhouding obj = getNewTransientSrtbijhouding(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
