// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.HisRelatie;
import nl.bzk.brp.model.data.kern.HisRelatieDataOnDemand;
import nl.bzk.brp.model.data.kern.HisRelatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisRelatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisRelatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisRelatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisRelatieIntegrationTest: @Transactional;
    
    @Autowired
    private HisRelatieDataOnDemand HisRelatieIntegrationTest.dod;
    
    @Test
    public void HisRelatieIntegrationTest.testCountHisRelaties() {
        Assert.assertNotNull("Data on demand for 'HisRelatie' failed to initialize correctly", dod.getRandomHisRelatie());
        long count = HisRelatie.countHisRelaties();
        Assert.assertTrue("Counter for 'HisRelatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisRelatieIntegrationTest.testFindHisRelatie() {
        HisRelatie obj = dod.getRandomHisRelatie();
        Assert.assertNotNull("Data on demand for 'HisRelatie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisRelatie' failed to provide an identifier", id);
        obj = HisRelatie.findHisRelatie(id);
        Assert.assertNotNull("Find method for 'HisRelatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisRelatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisRelatieIntegrationTest.testFindAllHisRelaties() {
        Assert.assertNotNull("Data on demand for 'HisRelatie' failed to initialize correctly", dod.getRandomHisRelatie());
        long count = HisRelatie.countHisRelaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisRelatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisRelatie> result = HisRelatie.findAllHisRelaties();
        Assert.assertNotNull("Find all method for 'HisRelatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisRelatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisRelatieIntegrationTest.testFindHisRelatieEntries() {
        Assert.assertNotNull("Data on demand for 'HisRelatie' failed to initialize correctly", dod.getRandomHisRelatie());
        long count = HisRelatie.countHisRelaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisRelatie> result = HisRelatie.findHisRelatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisRelatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisRelatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisRelatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisRelatie' failed to initialize correctly", dod.getRandomHisRelatie());
        HisRelatie obj = dod.getNewTransientHisRelatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisRelatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisRelatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisRelatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisRelatieIntegrationTest.testRemove() {
        HisRelatie obj = dod.getRandomHisRelatie();
        Assert.assertNotNull("Data on demand for 'HisRelatie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisRelatie' failed to provide an identifier", id);
        obj = HisRelatie.findHisRelatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisRelatie' with identifier '" + id + "'", HisRelatie.findHisRelatie(id));
    }
    
}
