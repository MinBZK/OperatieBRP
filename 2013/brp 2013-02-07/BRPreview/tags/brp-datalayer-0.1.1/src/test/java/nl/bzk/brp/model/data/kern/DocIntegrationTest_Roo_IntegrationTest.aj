// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Doc;
import nl.bzk.brp.model.data.kern.DocDataOnDemand;
import nl.bzk.brp.model.data.kern.DocIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DocIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DocIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DocIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DocIntegrationTest: @Transactional;
    
    @Autowired
    private DocDataOnDemand DocIntegrationTest.dod;
    
    @Test
    public void DocIntegrationTest.testCountDocs() {
        Assert.assertNotNull("Data on demand for 'Doc' failed to initialize correctly", dod.getRandomDoc());
        long count = Doc.countDocs();
        Assert.assertTrue("Counter for 'Doc' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DocIntegrationTest.testFindDoc() {
        Doc obj = dod.getRandomDoc();
        Assert.assertNotNull("Data on demand for 'Doc' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Doc' failed to provide an identifier", id);
        obj = Doc.findDoc(id);
        Assert.assertNotNull("Find method for 'Doc' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Doc' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DocIntegrationTest.testFindAllDocs() {
        Assert.assertNotNull("Data on demand for 'Doc' failed to initialize correctly", dod.getRandomDoc());
        long count = Doc.countDocs();
        Assert.assertTrue("Too expensive to perform a find all test for 'Doc', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Doc> result = Doc.findAllDocs();
        Assert.assertNotNull("Find all method for 'Doc' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Doc' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DocIntegrationTest.testFindDocEntries() {
        Assert.assertNotNull("Data on demand for 'Doc' failed to initialize correctly", dod.getRandomDoc());
        long count = Doc.countDocs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Doc> result = Doc.findDocEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Doc' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Doc' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DocIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Doc' failed to initialize correctly", dod.getRandomDoc());
        Doc obj = dod.getNewTransientDoc(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Doc' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Doc' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Doc' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DocIntegrationTest.testRemove() {
        Doc obj = dod.getRandomDoc();
        Assert.assertNotNull("Data on demand for 'Doc' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Doc' failed to provide an identifier", id);
        obj = Doc.findDoc(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Doc' with identifier '" + id + "'", Doc.findDoc(id));
    }
    
}
