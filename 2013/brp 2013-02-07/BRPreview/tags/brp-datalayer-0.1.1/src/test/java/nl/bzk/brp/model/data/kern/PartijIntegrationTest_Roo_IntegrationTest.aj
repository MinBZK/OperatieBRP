// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Partij;
import nl.bzk.brp.model.data.kern.PartijDataOnDemand;
import nl.bzk.brp.model.data.kern.PartijIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PartijIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PartijIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PartijIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PartijIntegrationTest: @Transactional;
    
    @Autowired
    private PartijDataOnDemand PartijIntegrationTest.dod;
    
    @Test
    public void PartijIntegrationTest.testCountPartijs() {
        Assert.assertNotNull("Data on demand for 'Partij' failed to initialize correctly", dod.getRandomPartij());
        long count = Partij.countPartijs();
        Assert.assertTrue("Counter for 'Partij' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PartijIntegrationTest.testFindPartij() {
        Partij obj = dod.getRandomPartij();
        Assert.assertNotNull("Data on demand for 'Partij' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Partij' failed to provide an identifier", id);
        obj = Partij.findPartij(id);
        Assert.assertNotNull("Find method for 'Partij' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Partij' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PartijIntegrationTest.testFindAllPartijs() {
        Assert.assertNotNull("Data on demand for 'Partij' failed to initialize correctly", dod.getRandomPartij());
        long count = Partij.countPartijs();
        Assert.assertTrue("Too expensive to perform a find all test for 'Partij', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Partij> result = Partij.findAllPartijs();
        Assert.assertNotNull("Find all method for 'Partij' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Partij' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PartijIntegrationTest.testFindPartijEntries() {
        Assert.assertNotNull("Data on demand for 'Partij' failed to initialize correctly", dod.getRandomPartij());
        long count = Partij.countPartijs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Partij> result = Partij.findPartijEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Partij' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Partij' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PartijIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Partij' failed to initialize correctly", dod.getRandomPartij());
        Partij obj = dod.getNewTransientPartij(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Partij' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Partij' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Partij' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PartijIntegrationTest.testRemove() {
        Partij obj = dod.getRandomPartij();
        Assert.assertNotNull("Data on demand for 'Partij' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Partij' failed to provide an identifier", id);
        obj = Partij.findPartij(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Partij' with identifier '" + id + "'", Partij.findPartij(id));
    }
    
}
