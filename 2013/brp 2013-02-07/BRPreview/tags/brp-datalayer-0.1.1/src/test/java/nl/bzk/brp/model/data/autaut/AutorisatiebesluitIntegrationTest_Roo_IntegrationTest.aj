// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.Autorisatiebesluit;
import nl.bzk.brp.model.data.autaut.AutorisatiebesluitDataOnDemand;
import nl.bzk.brp.model.data.autaut.AutorisatiebesluitIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AutorisatiebesluitIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AutorisatiebesluitIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AutorisatiebesluitIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AutorisatiebesluitIntegrationTest: @Transactional;
    
    @Autowired
    private AutorisatiebesluitDataOnDemand AutorisatiebesluitIntegrationTest.dod;
    
    @Test
    public void AutorisatiebesluitIntegrationTest.testCountAutorisatiebesluits() {
        Assert.assertNotNull("Data on demand for 'Autorisatiebesluit' failed to initialize correctly", dod.getRandomAutorisatiebesluit());
        long count = Autorisatiebesluit.countAutorisatiebesluits();
        Assert.assertTrue("Counter for 'Autorisatiebesluit' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AutorisatiebesluitIntegrationTest.testFindAutorisatiebesluit() {
        Autorisatiebesluit obj = dod.getRandomAutorisatiebesluit();
        Assert.assertNotNull("Data on demand for 'Autorisatiebesluit' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Autorisatiebesluit' failed to provide an identifier", id);
        obj = Autorisatiebesluit.findAutorisatiebesluit(id);
        Assert.assertNotNull("Find method for 'Autorisatiebesluit' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Autorisatiebesluit' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AutorisatiebesluitIntegrationTest.testFindAllAutorisatiebesluits() {
        Assert.assertNotNull("Data on demand for 'Autorisatiebesluit' failed to initialize correctly", dod.getRandomAutorisatiebesluit());
        long count = Autorisatiebesluit.countAutorisatiebesluits();
        Assert.assertTrue("Too expensive to perform a find all test for 'Autorisatiebesluit', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Autorisatiebesluit> result = Autorisatiebesluit.findAllAutorisatiebesluits();
        Assert.assertNotNull("Find all method for 'Autorisatiebesluit' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Autorisatiebesluit' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AutorisatiebesluitIntegrationTest.testFindAutorisatiebesluitEntries() {
        Assert.assertNotNull("Data on demand for 'Autorisatiebesluit' failed to initialize correctly", dod.getRandomAutorisatiebesluit());
        long count = Autorisatiebesluit.countAutorisatiebesluits();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Autorisatiebesluit> result = Autorisatiebesluit.findAutorisatiebesluitEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Autorisatiebesluit' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Autorisatiebesluit' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AutorisatiebesluitIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Autorisatiebesluit' failed to initialize correctly", dod.getRandomAutorisatiebesluit());
        Autorisatiebesluit obj = dod.getNewTransientAutorisatiebesluit(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Autorisatiebesluit' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Autorisatiebesluit' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Autorisatiebesluit' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AutorisatiebesluitIntegrationTest.testRemove() {
        Autorisatiebesluit obj = dod.getRandomAutorisatiebesluit();
        Assert.assertNotNull("Data on demand for 'Autorisatiebesluit' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Autorisatiebesluit' failed to provide an identifier", id);
        obj = Autorisatiebesluit.findAutorisatiebesluit(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Autorisatiebesluit' with identifier '" + id + "'", Autorisatiebesluit.findAutorisatiebesluit(id));
    }
    
}
