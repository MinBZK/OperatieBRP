// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.autaut.Bijhsituatie;
import nl.bzk.brp.model.data.autaut.BijhsituatieDataOnDemand;
import nl.bzk.brp.model.data.autaut.HisBijhsituatie;
import nl.bzk.brp.model.data.autaut.HisBijhsituatieDataOnDemand;
import nl.bzk.brp.model.data.kern.Actie;
import nl.bzk.brp.model.data.kern.ActieDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect HisBijhsituatieDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HisBijhsituatieDataOnDemand: @Component;
    
    private Random HisBijhsituatieDataOnDemand.rnd = new SecureRandom();
    
    private List<HisBijhsituatie> HisBijhsituatieDataOnDemand.data;
    
    @Autowired
    private ActieDataOnDemand HisBijhsituatieDataOnDemand.actieDataOnDemand;
    
    @Autowired
    private BijhsituatieDataOnDemand HisBijhsituatieDataOnDemand.bijhsituatieDataOnDemand;
    
    public HisBijhsituatie HisBijhsituatieDataOnDemand.getNewTransientHisBijhsituatie(int index) {
        HisBijhsituatie obj = new HisBijhsituatie();
        setActieinh(obj, index);
        setActieverval(obj, index);
        setBijhsituatie(obj, index);
        setTsreg(obj, index);
        setTsverval(obj, index);
        return obj;
    }
    
    public void HisBijhsituatieDataOnDemand.setActieinh(HisBijhsituatie obj, int index) {
        Actie actieinh = actieDataOnDemand.getRandomActie();
        obj.setActieinh(actieinh);
    }
    
    public void HisBijhsituatieDataOnDemand.setActieverval(HisBijhsituatie obj, int index) {
        Actie actieverval = actieDataOnDemand.getRandomActie();
        obj.setActieverval(actieverval);
    }
    
    public void HisBijhsituatieDataOnDemand.setBijhsituatie(HisBijhsituatie obj, int index) {
        Bijhsituatie bijhsituatie = bijhsituatieDataOnDemand.getRandomBijhsituatie();
        obj.setBijhsituatie(bijhsituatie);
    }
    
    public void HisBijhsituatieDataOnDemand.setTsreg(HisBijhsituatie obj, int index) {
        Date tsreg = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsreg(tsreg);
    }
    
    public void HisBijhsituatieDataOnDemand.setTsverval(HisBijhsituatie obj, int index) {
        Date tsverval = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsverval(tsverval);
    }
    
    public HisBijhsituatie HisBijhsituatieDataOnDemand.getSpecificHisBijhsituatie(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        HisBijhsituatie obj = data.get(index);
        Long id = obj.getId();
        return HisBijhsituatie.findHisBijhsituatie(id);
    }
    
    public HisBijhsituatie HisBijhsituatieDataOnDemand.getRandomHisBijhsituatie() {
        init();
        HisBijhsituatie obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return HisBijhsituatie.findHisBijhsituatie(id);
    }
    
    public boolean HisBijhsituatieDataOnDemand.modifyHisBijhsituatie(HisBijhsituatie obj) {
        return false;
    }
    
    public void HisBijhsituatieDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = HisBijhsituatie.findHisBijhsituatieEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'HisBijhsituatie' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<HisBijhsituatie>();
        for (int i = 0; i < 10; i++) {
            HisBijhsituatie obj = getNewTransientHisBijhsituatie(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
