// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Onderzoek;
import nl.bzk.brp.model.data.kern.OnderzoekDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect OnderzoekDataOnDemand_Roo_DataOnDemand {
    
    declare @type: OnderzoekDataOnDemand: @Component;
    
    private Random OnderzoekDataOnDemand.rnd = new SecureRandom();
    
    private List<Onderzoek> OnderzoekDataOnDemand.data;
    
    public Onderzoek OnderzoekDataOnDemand.getNewTransientOnderzoek(int index) {
        Onderzoek obj = new Onderzoek();
        setDatbegin(obj, index);
        setDateinde(obj, index);
        setOms(obj, index);
        setOnderzoekstatushis(obj, index);
        return obj;
    }
    
    public void OnderzoekDataOnDemand.setDatbegin(Onderzoek obj, int index) {
        BigDecimal datbegin = BigDecimal.valueOf(index);
        obj.setDatbegin(datbegin);
    }
    
    public void OnderzoekDataOnDemand.setDateinde(Onderzoek obj, int index) {
        BigDecimal dateinde = BigDecimal.valueOf(index);
        obj.setDateinde(dateinde);
    }
    
    public void OnderzoekDataOnDemand.setOms(Onderzoek obj, int index) {
        String oms = "oms_" + index;
        obj.setOms(oms);
    }
    
    public void OnderzoekDataOnDemand.setOnderzoekstatushis(Onderzoek obj, int index) {
        String onderzoekstatushis = String.valueOf(index);
        if (onderzoekstatushis.length() > 1) {
            onderzoekstatushis = onderzoekstatushis.substring(0, 1);
        }
        obj.setOnderzoekstatushis(onderzoekstatushis);
    }
    
    public Onderzoek OnderzoekDataOnDemand.getSpecificOnderzoek(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Onderzoek obj = data.get(index);
        Long id = obj.getId();
        return Onderzoek.findOnderzoek(id);
    }
    
    public Onderzoek OnderzoekDataOnDemand.getRandomOnderzoek() {
        init();
        Onderzoek obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Onderzoek.findOnderzoek(id);
    }
    
    public boolean OnderzoekDataOnDemand.modifyOnderzoek(Onderzoek obj) {
        return false;
    }
    
    public void OnderzoekDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Onderzoek.findOnderzoekEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Onderzoek' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Onderzoek>();
        for (int i = 0; i < 10; i++) {
            Onderzoek obj = getNewTransientOnderzoek(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
