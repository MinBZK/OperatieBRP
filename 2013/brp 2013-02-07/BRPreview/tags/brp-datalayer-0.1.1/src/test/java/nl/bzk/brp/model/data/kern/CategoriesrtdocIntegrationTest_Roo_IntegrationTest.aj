// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Categoriesrtdoc;
import nl.bzk.brp.model.data.kern.CategoriesrtdocDataOnDemand;
import nl.bzk.brp.model.data.kern.CategoriesrtdocIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategoriesrtdocIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategoriesrtdocIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategoriesrtdocIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategoriesrtdocIntegrationTest: @Transactional;
    
    @Autowired
    private CategoriesrtdocDataOnDemand CategoriesrtdocIntegrationTest.dod;
    
    @Test
    public void CategoriesrtdocIntegrationTest.testCountCategoriesrtdocs() {
        Assert.assertNotNull("Data on demand for 'Categoriesrtdoc' failed to initialize correctly", dod.getRandomCategoriesrtdoc());
        long count = Categoriesrtdoc.countCategoriesrtdocs();
        Assert.assertTrue("Counter for 'Categoriesrtdoc' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategoriesrtdocIntegrationTest.testFindCategoriesrtdoc() {
        Categoriesrtdoc obj = dod.getRandomCategoriesrtdoc();
        Assert.assertNotNull("Data on demand for 'Categoriesrtdoc' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Categoriesrtdoc' failed to provide an identifier", id);
        obj = Categoriesrtdoc.findCategoriesrtdoc(id);
        Assert.assertNotNull("Find method for 'Categoriesrtdoc' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Categoriesrtdoc' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategoriesrtdocIntegrationTest.testFindAllCategoriesrtdocs() {
        Assert.assertNotNull("Data on demand for 'Categoriesrtdoc' failed to initialize correctly", dod.getRandomCategoriesrtdoc());
        long count = Categoriesrtdoc.countCategoriesrtdocs();
        Assert.assertTrue("Too expensive to perform a find all test for 'Categoriesrtdoc', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Categoriesrtdoc> result = Categoriesrtdoc.findAllCategoriesrtdocs();
        Assert.assertNotNull("Find all method for 'Categoriesrtdoc' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Categoriesrtdoc' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategoriesrtdocIntegrationTest.testFindCategoriesrtdocEntries() {
        Assert.assertNotNull("Data on demand for 'Categoriesrtdoc' failed to initialize correctly", dod.getRandomCategoriesrtdoc());
        long count = Categoriesrtdoc.countCategoriesrtdocs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Categoriesrtdoc> result = Categoriesrtdoc.findCategoriesrtdocEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Categoriesrtdoc' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Categoriesrtdoc' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategoriesrtdocIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Categoriesrtdoc' failed to initialize correctly", dod.getRandomCategoriesrtdoc());
        Categoriesrtdoc obj = dod.getNewTransientCategoriesrtdoc(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Categoriesrtdoc' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Categoriesrtdoc' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Categoriesrtdoc' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CategoriesrtdocIntegrationTest.testRemove() {
        Categoriesrtdoc obj = dod.getRandomCategoriesrtdoc();
        Assert.assertNotNull("Data on demand for 'Categoriesrtdoc' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Categoriesrtdoc' failed to provide an identifier", id);
        obj = Categoriesrtdoc.findCategoriesrtdoc(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Categoriesrtdoc' with identifier '" + id + "'", Categoriesrtdoc.findCategoriesrtdoc(id));
    }
    
}
