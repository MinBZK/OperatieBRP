// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.HisBetrouder;
import nl.bzk.brp.model.data.kern.HisBetrouderDataOnDemand;
import nl.bzk.brp.model.data.kern.HisBetrouderIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisBetrouderIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisBetrouderIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisBetrouderIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisBetrouderIntegrationTest: @Transactional;
    
    @Autowired
    private HisBetrouderDataOnDemand HisBetrouderIntegrationTest.dod;
    
    @Test
    public void HisBetrouderIntegrationTest.testCountHisBetrouders() {
        Assert.assertNotNull("Data on demand for 'HisBetrouder' failed to initialize correctly", dod.getRandomHisBetrouder());
        long count = HisBetrouder.countHisBetrouders();
        Assert.assertTrue("Counter for 'HisBetrouder' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisBetrouderIntegrationTest.testFindHisBetrouder() {
        HisBetrouder obj = dod.getRandomHisBetrouder();
        Assert.assertNotNull("Data on demand for 'HisBetrouder' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisBetrouder' failed to provide an identifier", id);
        obj = HisBetrouder.findHisBetrouder(id);
        Assert.assertNotNull("Find method for 'HisBetrouder' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisBetrouder' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisBetrouderIntegrationTest.testFindAllHisBetrouders() {
        Assert.assertNotNull("Data on demand for 'HisBetrouder' failed to initialize correctly", dod.getRandomHisBetrouder());
        long count = HisBetrouder.countHisBetrouders();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisBetrouder', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisBetrouder> result = HisBetrouder.findAllHisBetrouders();
        Assert.assertNotNull("Find all method for 'HisBetrouder' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisBetrouder' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisBetrouderIntegrationTest.testFindHisBetrouderEntries() {
        Assert.assertNotNull("Data on demand for 'HisBetrouder' failed to initialize correctly", dod.getRandomHisBetrouder());
        long count = HisBetrouder.countHisBetrouders();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisBetrouder> result = HisBetrouder.findHisBetrouderEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisBetrouder' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisBetrouder' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisBetrouderIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisBetrouder' failed to initialize correctly", dod.getRandomHisBetrouder());
        HisBetrouder obj = dod.getNewTransientHisBetrouder(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisBetrouder' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisBetrouder' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisBetrouder' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisBetrouderIntegrationTest.testRemove() {
        HisBetrouder obj = dod.getRandomHisBetrouder();
        Assert.assertNotNull("Data on demand for 'HisBetrouder' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisBetrouder' failed to provide an identifier", id);
        obj = HisBetrouder.findHisBetrouder(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisBetrouder' with identifier '" + id + "'", HisBetrouder.findHisBetrouder(id));
    }
    
}
