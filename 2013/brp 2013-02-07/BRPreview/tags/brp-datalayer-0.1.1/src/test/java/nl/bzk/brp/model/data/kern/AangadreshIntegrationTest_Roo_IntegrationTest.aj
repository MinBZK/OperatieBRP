// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Aangadresh;
import nl.bzk.brp.model.data.kern.AangadreshDataOnDemand;
import nl.bzk.brp.model.data.kern.AangadreshIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AangadreshIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AangadreshIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AangadreshIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AangadreshIntegrationTest: @Transactional;
    
    @Autowired
    private AangadreshDataOnDemand AangadreshIntegrationTest.dod;
    
    @Test
    public void AangadreshIntegrationTest.testCountAangadreshes() {
        Assert.assertNotNull("Data on demand for 'Aangadresh' failed to initialize correctly", dod.getRandomAangadresh());
        long count = Aangadresh.countAangadreshes();
        Assert.assertTrue("Counter for 'Aangadresh' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AangadreshIntegrationTest.testFindAangadresh() {
        Aangadresh obj = dod.getRandomAangadresh();
        Assert.assertNotNull("Data on demand for 'Aangadresh' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Aangadresh' failed to provide an identifier", id);
        obj = Aangadresh.findAangadresh(id);
        Assert.assertNotNull("Find method for 'Aangadresh' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Aangadresh' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AangadreshIntegrationTest.testFindAllAangadreshes() {
        Assert.assertNotNull("Data on demand for 'Aangadresh' failed to initialize correctly", dod.getRandomAangadresh());
        long count = Aangadresh.countAangadreshes();
        Assert.assertTrue("Too expensive to perform a find all test for 'Aangadresh', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Aangadresh> result = Aangadresh.findAllAangadreshes();
        Assert.assertNotNull("Find all method for 'Aangadresh' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Aangadresh' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AangadreshIntegrationTest.testFindAangadreshEntries() {
        Assert.assertNotNull("Data on demand for 'Aangadresh' failed to initialize correctly", dod.getRandomAangadresh());
        long count = Aangadresh.countAangadreshes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Aangadresh> result = Aangadresh.findAangadreshEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Aangadresh' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Aangadresh' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AangadreshIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Aangadresh' failed to initialize correctly", dod.getRandomAangadresh());
        Aangadresh obj = dod.getNewTransientAangadresh(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Aangadresh' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Aangadresh' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Aangadresh' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AangadreshIntegrationTest.testRemove() {
        Aangadresh obj = dod.getRandomAangadresh();
        Assert.assertNotNull("Data on demand for 'Aangadresh' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Aangadresh' failed to provide an identifier", id);
        obj = Aangadresh.findAangadresh(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Aangadresh' with identifier '" + id + "'", Aangadresh.findAangadresh(id));
    }
    
}
