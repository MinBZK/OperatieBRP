// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Persindicatie;
import nl.bzk.brp.model.data.kern.PersindicatieDataOnDemand;
import nl.bzk.brp.model.data.kern.PersindicatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PersindicatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersindicatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersindicatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersindicatieIntegrationTest: @Transactional;
    
    @Autowired
    private PersindicatieDataOnDemand PersindicatieIntegrationTest.dod;
    
    @Test
    public void PersindicatieIntegrationTest.testCountPersindicaties() {
        Assert.assertNotNull("Data on demand for 'Persindicatie' failed to initialize correctly", dod.getRandomPersindicatie());
        long count = Persindicatie.countPersindicaties();
        Assert.assertTrue("Counter for 'Persindicatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersindicatieIntegrationTest.testFindPersindicatie() {
        Persindicatie obj = dod.getRandomPersindicatie();
        Assert.assertNotNull("Data on demand for 'Persindicatie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persindicatie' failed to provide an identifier", id);
        obj = Persindicatie.findPersindicatie(id);
        Assert.assertNotNull("Find method for 'Persindicatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Persindicatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersindicatieIntegrationTest.testFindAllPersindicaties() {
        Assert.assertNotNull("Data on demand for 'Persindicatie' failed to initialize correctly", dod.getRandomPersindicatie());
        long count = Persindicatie.countPersindicaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Persindicatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Persindicatie> result = Persindicatie.findAllPersindicaties();
        Assert.assertNotNull("Find all method for 'Persindicatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Persindicatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersindicatieIntegrationTest.testFindPersindicatieEntries() {
        Assert.assertNotNull("Data on demand for 'Persindicatie' failed to initialize correctly", dod.getRandomPersindicatie());
        long count = Persindicatie.countPersindicaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Persindicatie> result = Persindicatie.findPersindicatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Persindicatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Persindicatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersindicatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Persindicatie' failed to initialize correctly", dod.getRandomPersindicatie());
        Persindicatie obj = dod.getNewTransientPersindicatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Persindicatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Persindicatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Persindicatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersindicatieIntegrationTest.testRemove() {
        Persindicatie obj = dod.getRandomPersindicatie();
        Assert.assertNotNull("Data on demand for 'Persindicatie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persindicatie' failed to provide an identifier", id);
        obj = Persindicatie.findPersindicatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Persindicatie' with identifier '" + id + "'", Persindicatie.findPersindicatie(id));
    }
    
}
