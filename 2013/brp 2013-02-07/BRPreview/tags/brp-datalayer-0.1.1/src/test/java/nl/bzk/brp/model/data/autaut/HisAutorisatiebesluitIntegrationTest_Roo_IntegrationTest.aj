// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.HisAutorisatiebesluit;
import nl.bzk.brp.model.data.autaut.HisAutorisatiebesluitDataOnDemand;
import nl.bzk.brp.model.data.autaut.HisAutorisatiebesluitIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisAutorisatiebesluitIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisAutorisatiebesluitIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisAutorisatiebesluitIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisAutorisatiebesluitIntegrationTest: @Transactional;
    
    @Autowired
    private HisAutorisatiebesluitDataOnDemand HisAutorisatiebesluitIntegrationTest.dod;
    
    @Test
    public void HisAutorisatiebesluitIntegrationTest.testCountHisAutorisatiebesluits() {
        Assert.assertNotNull("Data on demand for 'HisAutorisatiebesluit' failed to initialize correctly", dod.getRandomHisAutorisatiebesluit());
        long count = HisAutorisatiebesluit.countHisAutorisatiebesluits();
        Assert.assertTrue("Counter for 'HisAutorisatiebesluit' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisAutorisatiebesluitIntegrationTest.testFindHisAutorisatiebesluit() {
        HisAutorisatiebesluit obj = dod.getRandomHisAutorisatiebesluit();
        Assert.assertNotNull("Data on demand for 'HisAutorisatiebesluit' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisAutorisatiebesluit' failed to provide an identifier", id);
        obj = HisAutorisatiebesluit.findHisAutorisatiebesluit(id);
        Assert.assertNotNull("Find method for 'HisAutorisatiebesluit' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisAutorisatiebesluit' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisAutorisatiebesluitIntegrationTest.testFindAllHisAutorisatiebesluits() {
        Assert.assertNotNull("Data on demand for 'HisAutorisatiebesluit' failed to initialize correctly", dod.getRandomHisAutorisatiebesluit());
        long count = HisAutorisatiebesluit.countHisAutorisatiebesluits();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisAutorisatiebesluit', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisAutorisatiebesluit> result = HisAutorisatiebesluit.findAllHisAutorisatiebesluits();
        Assert.assertNotNull("Find all method for 'HisAutorisatiebesluit' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisAutorisatiebesluit' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisAutorisatiebesluitIntegrationTest.testFindHisAutorisatiebesluitEntries() {
        Assert.assertNotNull("Data on demand for 'HisAutorisatiebesluit' failed to initialize correctly", dod.getRandomHisAutorisatiebesluit());
        long count = HisAutorisatiebesluit.countHisAutorisatiebesluits();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisAutorisatiebesluit> result = HisAutorisatiebesluit.findHisAutorisatiebesluitEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisAutorisatiebesluit' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisAutorisatiebesluit' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisAutorisatiebesluitIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisAutorisatiebesluit' failed to initialize correctly", dod.getRandomHisAutorisatiebesluit());
        HisAutorisatiebesluit obj = dod.getNewTransientHisAutorisatiebesluit(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisAutorisatiebesluit' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisAutorisatiebesluit' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisAutorisatiebesluit' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisAutorisatiebesluitIntegrationTest.testRemove() {
        HisAutorisatiebesluit obj = dod.getRandomHisAutorisatiebesluit();
        Assert.assertNotNull("Data on demand for 'HisAutorisatiebesluit' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisAutorisatiebesluit' failed to provide an identifier", id);
        obj = HisAutorisatiebesluit.findHisAutorisatiebesluit(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisAutorisatiebesluit' with identifier '" + id + "'", HisAutorisatiebesluit.findHisAutorisatiebesluit(id));
    }
    
}
