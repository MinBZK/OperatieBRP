// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.util.List;
import nl.bzk.brp.model.data.autaut.HisBijhsituatie;
import nl.bzk.brp.model.data.autaut.HisBijhsituatieDataOnDemand;
import nl.bzk.brp.model.data.autaut.HisBijhsituatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisBijhsituatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisBijhsituatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisBijhsituatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisBijhsituatieIntegrationTest: @Transactional;
    
    @Autowired
    private HisBijhsituatieDataOnDemand HisBijhsituatieIntegrationTest.dod;
    
    @Test
    public void HisBijhsituatieIntegrationTest.testCountHisBijhsituaties() {
        Assert.assertNotNull("Data on demand for 'HisBijhsituatie' failed to initialize correctly", dod.getRandomHisBijhsituatie());
        long count = HisBijhsituatie.countHisBijhsituaties();
        Assert.assertTrue("Counter for 'HisBijhsituatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisBijhsituatieIntegrationTest.testFindHisBijhsituatie() {
        HisBijhsituatie obj = dod.getRandomHisBijhsituatie();
        Assert.assertNotNull("Data on demand for 'HisBijhsituatie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisBijhsituatie' failed to provide an identifier", id);
        obj = HisBijhsituatie.findHisBijhsituatie(id);
        Assert.assertNotNull("Find method for 'HisBijhsituatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisBijhsituatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisBijhsituatieIntegrationTest.testFindAllHisBijhsituaties() {
        Assert.assertNotNull("Data on demand for 'HisBijhsituatie' failed to initialize correctly", dod.getRandomHisBijhsituatie());
        long count = HisBijhsituatie.countHisBijhsituaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisBijhsituatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisBijhsituatie> result = HisBijhsituatie.findAllHisBijhsituaties();
        Assert.assertNotNull("Find all method for 'HisBijhsituatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisBijhsituatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisBijhsituatieIntegrationTest.testFindHisBijhsituatieEntries() {
        Assert.assertNotNull("Data on demand for 'HisBijhsituatie' failed to initialize correctly", dod.getRandomHisBijhsituatie());
        long count = HisBijhsituatie.countHisBijhsituaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisBijhsituatie> result = HisBijhsituatie.findHisBijhsituatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisBijhsituatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisBijhsituatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisBijhsituatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisBijhsituatie' failed to initialize correctly", dod.getRandomHisBijhsituatie());
        HisBijhsituatie obj = dod.getNewTransientHisBijhsituatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisBijhsituatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisBijhsituatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisBijhsituatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisBijhsituatieIntegrationTest.testRemove() {
        HisBijhsituatie obj = dod.getRandomHisBijhsituatie();
        Assert.assertNotNull("Data on demand for 'HisBijhsituatie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisBijhsituatie' failed to provide an identifier", id);
        obj = HisBijhsituatie.findHisBijhsituatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisBijhsituatie' with identifier '" + id + "'", HisBijhsituatie.findHisBijhsituatie(id));
    }
    
}
