// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.autaut;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.autaut.Beperkingpopulatie;
import nl.bzk.brp.model.data.autaut.BeperkingpopulatieDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect BeperkingpopulatieDataOnDemand_Roo_DataOnDemand {
    
    declare @type: BeperkingpopulatieDataOnDemand: @Component;
    
    private Random BeperkingpopulatieDataOnDemand.rnd = new SecureRandom();
    
    private List<Beperkingpopulatie> BeperkingpopulatieDataOnDemand.data;
    
    public Beperkingpopulatie BeperkingpopulatieDataOnDemand.getNewTransientBeperkingpopulatie(int index) {
        Beperkingpopulatie obj = new Beperkingpopulatie();
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setNaam(obj, index);
        setOms(obj, index);
        return obj;
    }
    
    public void BeperkingpopulatieDataOnDemand.setDataanvgel(Beperkingpopulatie obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void BeperkingpopulatieDataOnDemand.setDateindegel(Beperkingpopulatie obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void BeperkingpopulatieDataOnDemand.setNaam(Beperkingpopulatie obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = new Random().nextInt(10) + naam.substring(1, 40);
        }
        obj.setNaam(naam);
    }
    
    public void BeperkingpopulatieDataOnDemand.setOms(Beperkingpopulatie obj, int index) {
        String oms = "oms_" + index;
        if (oms.length() > 250) {
            oms = oms.substring(0, 250);
        }
        obj.setOms(oms);
    }
    
    public Beperkingpopulatie BeperkingpopulatieDataOnDemand.getSpecificBeperkingpopulatie(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Beperkingpopulatie obj = data.get(index);
        Short id = obj.getId();
        return Beperkingpopulatie.findBeperkingpopulatie(id);
    }
    
    public Beperkingpopulatie BeperkingpopulatieDataOnDemand.getRandomBeperkingpopulatie() {
        init();
        Beperkingpopulatie obj = data.get(rnd.nextInt(data.size()));
        Short id = obj.getId();
        return Beperkingpopulatie.findBeperkingpopulatie(id);
    }
    
    public boolean BeperkingpopulatieDataOnDemand.modifyBeperkingpopulatie(Beperkingpopulatie obj) {
        return false;
    }
    
    public void BeperkingpopulatieDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Beperkingpopulatie.findBeperkingpopulatieEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Beperkingpopulatie' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Beperkingpopulatie>();
        for (int i = 0; i < 10; i++) {
            Beperkingpopulatie obj = getNewTransientBeperkingpopulatie(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
