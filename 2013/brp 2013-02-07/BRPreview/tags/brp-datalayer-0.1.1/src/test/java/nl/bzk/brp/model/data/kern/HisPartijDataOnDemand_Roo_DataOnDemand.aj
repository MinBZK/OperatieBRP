// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Actie;
import nl.bzk.brp.model.data.kern.ActieDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPartij;
import nl.bzk.brp.model.data.kern.HisPartijDataOnDemand;
import nl.bzk.brp.model.data.kern.Partij;
import nl.bzk.brp.model.data.kern.PartijDataOnDemand;
import nl.bzk.brp.model.data.kern.Sector;
import nl.bzk.brp.model.data.kern.SectorDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect HisPartijDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HisPartijDataOnDemand: @Component;
    
    private Random HisPartijDataOnDemand.rnd = new SecureRandom();
    
    private List<HisPartij> HisPartijDataOnDemand.data;
    
    @Autowired
    private ActieDataOnDemand HisPartijDataOnDemand.actieDataOnDemand;
    
    @Autowired
    private PartijDataOnDemand HisPartijDataOnDemand.partijDataOnDemand;
    
    @Autowired
    private SectorDataOnDemand HisPartijDataOnDemand.sectorDataOnDemand;
    
    public HisPartij HisPartijDataOnDemand.getNewTransientHisPartij(int index) {
        HisPartij obj = new HisPartij();
        setActieinh(obj, index);
        setActieverval(obj, index);
        setDataanv(obj, index);
        setDateinde(obj, index);
        setPartij(obj, index);
        setSector(obj, index);
        setTsreg(obj, index);
        setTsverval(obj, index);
        return obj;
    }
    
    public void HisPartijDataOnDemand.setActieinh(HisPartij obj, int index) {
        Actie actieinh = actieDataOnDemand.getRandomActie();
        obj.setActieinh(actieinh);
    }
    
    public void HisPartijDataOnDemand.setActieverval(HisPartij obj, int index) {
        Actie actieverval = actieDataOnDemand.getRandomActie();
        obj.setActieverval(actieverval);
    }
    
    public void HisPartijDataOnDemand.setDataanv(HisPartij obj, int index) {
        BigDecimal dataanv = BigDecimal.valueOf(index);
        obj.setDataanv(dataanv);
    }
    
    public void HisPartijDataOnDemand.setDateinde(HisPartij obj, int index) {
        BigDecimal dateinde = BigDecimal.valueOf(index);
        obj.setDateinde(dateinde);
    }
    
    public void HisPartijDataOnDemand.setPartij(HisPartij obj, int index) {
        Partij partij = partijDataOnDemand.getRandomPartij();
        obj.setPartij(partij);
    }
    
    public void HisPartijDataOnDemand.setSector(HisPartij obj, int index) {
        Sector sector = sectorDataOnDemand.getRandomSector();
        obj.setSector(sector);
    }
    
    public void HisPartijDataOnDemand.setTsreg(HisPartij obj, int index) {
        Date tsreg = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsreg(tsreg);
    }
    
    public void HisPartijDataOnDemand.setTsverval(HisPartij obj, int index) {
        Date tsverval = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTsverval(tsverval);
    }
    
    public HisPartij HisPartijDataOnDemand.getSpecificHisPartij(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        HisPartij obj = data.get(index);
        Long id = obj.getId();
        return HisPartij.findHisPartij(id);
    }
    
    public HisPartij HisPartijDataOnDemand.getRandomHisPartij() {
        init();
        HisPartij obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return HisPartij.findHisPartij(id);
    }
    
    public boolean HisPartijDataOnDemand.modifyHisPartij(HisPartij obj) {
        return false;
    }
    
    public void HisPartijDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = HisPartij.findHisPartijEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'HisPartij' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<HisPartij>();
        for (int i = 0; i < 10; i++) {
            HisPartij obj = getNewTransientHisPartij(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
