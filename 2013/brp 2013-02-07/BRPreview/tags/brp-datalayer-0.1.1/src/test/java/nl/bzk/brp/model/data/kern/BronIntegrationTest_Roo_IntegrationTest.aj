// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Bron;
import nl.bzk.brp.model.data.kern.BronDataOnDemand;
import nl.bzk.brp.model.data.kern.BronIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect BronIntegrationTest_Roo_IntegrationTest {
    
    declare @type: BronIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: BronIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: BronIntegrationTest: @Transactional;
    
    @Autowired
    private BronDataOnDemand BronIntegrationTest.dod;
    
    @Test
    public void BronIntegrationTest.testCountBrons() {
        Assert.assertNotNull("Data on demand for 'Bron' failed to initialize correctly", dod.getRandomBron());
        long count = Bron.countBrons();
        Assert.assertTrue("Counter for 'Bron' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void BronIntegrationTest.testFindBron() {
        Bron obj = dod.getRandomBron();
        Assert.assertNotNull("Data on demand for 'Bron' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Bron' failed to provide an identifier", id);
        obj = Bron.findBron(id);
        Assert.assertNotNull("Find method for 'Bron' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Bron' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void BronIntegrationTest.testFindAllBrons() {
        Assert.assertNotNull("Data on demand for 'Bron' failed to initialize correctly", dod.getRandomBron());
        long count = Bron.countBrons();
        Assert.assertTrue("Too expensive to perform a find all test for 'Bron', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Bron> result = Bron.findAllBrons();
        Assert.assertNotNull("Find all method for 'Bron' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Bron' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void BronIntegrationTest.testFindBronEntries() {
        Assert.assertNotNull("Data on demand for 'Bron' failed to initialize correctly", dod.getRandomBron());
        long count = Bron.countBrons();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Bron> result = Bron.findBronEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Bron' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Bron' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void BronIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Bron' failed to initialize correctly", dod.getRandomBron());
        Bron obj = dod.getNewTransientBron(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Bron' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Bron' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Bron' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void BronIntegrationTest.testRemove() {
        Bron obj = dod.getRandomBron();
        Assert.assertNotNull("Data on demand for 'Bron' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Bron' failed to provide an identifier", id);
        obj = Bron.findBron(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Bron' with identifier '" + id + "'", Bron.findBron(id));
    }
    
}
