// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Pers;
import nl.bzk.brp.model.data.kern.PersDataOnDemand;
import nl.bzk.brp.model.data.kern.Persvoornaam;
import nl.bzk.brp.model.data.kern.PersvoornaamDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect PersvoornaamDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PersvoornaamDataOnDemand: @Component;
    
    private Random PersvoornaamDataOnDemand.rnd = new SecureRandom();
    
    private List<Persvoornaam> PersvoornaamDataOnDemand.data;
    
    @Autowired
    private PersDataOnDemand PersvoornaamDataOnDemand.persDataOnDemand;
    
    public Persvoornaam PersvoornaamDataOnDemand.getNewTransientPersvoornaam(int index) {
        Persvoornaam obj = new Persvoornaam();
        setNaam(obj, index);
        setPers(obj, index);
        setPersvoornaamstatushis(obj, index);
        setVolgnr(obj, index);
        return obj;
    }
    
    public void PersvoornaamDataOnDemand.setNaam(Persvoornaam obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = naam.substring(0, 40);
        }
        obj.setNaam(naam);
    }
    
    public void PersvoornaamDataOnDemand.setPers(Persvoornaam obj, int index) {
        Pers pers = persDataOnDemand.getRandomPers();
        obj.setPers(pers);
    }
    
    public void PersvoornaamDataOnDemand.setPersvoornaamstatushis(Persvoornaam obj, int index) {
        String persvoornaamstatushis = String.valueOf(index);
        if (persvoornaamstatushis.length() > 1) {
            persvoornaamstatushis = persvoornaamstatushis.substring(0, 1);
        }
        obj.setPersvoornaamstatushis(persvoornaamstatushis);
    }
    
    public void PersvoornaamDataOnDemand.setVolgnr(Persvoornaam obj, int index) {
        Integer volgnr = new Integer(index);
        obj.setVolgnr(volgnr);
    }
    
    public Persvoornaam PersvoornaamDataOnDemand.getSpecificPersvoornaam(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Persvoornaam obj = data.get(index);
        Long id = obj.getId();
        return Persvoornaam.findPersvoornaam(id);
    }
    
    public Persvoornaam PersvoornaamDataOnDemand.getRandomPersvoornaam() {
        init();
        Persvoornaam obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Persvoornaam.findPersvoornaam(id);
    }
    
    public boolean PersvoornaamDataOnDemand.modifyPersvoornaam(Persvoornaam obj) {
        return false;
    }
    
    public void PersvoornaamDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Persvoornaam.findPersvoornaamEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Persvoornaam' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Persvoornaam>();
        for (int i = 0; i < 10; i++) {
            Persvoornaam obj = getNewTransientPersvoornaam(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
