// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.HisPersverblijfsr;
import nl.bzk.brp.model.data.kern.HisPersverblijfsrDataOnDemand;
import nl.bzk.brp.model.data.kern.HisPersverblijfsrIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect HisPersverblijfsrIntegrationTest_Roo_IntegrationTest {
    
    declare @type: HisPersverblijfsrIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: HisPersverblijfsrIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: HisPersverblijfsrIntegrationTest: @Transactional;
    
    @Autowired
    private HisPersverblijfsrDataOnDemand HisPersverblijfsrIntegrationTest.dod;
    
    @Test
    public void HisPersverblijfsrIntegrationTest.testCountHisPersverblijfsrs() {
        Assert.assertNotNull("Data on demand for 'HisPersverblijfsr' failed to initialize correctly", dod.getRandomHisPersverblijfsr());
        long count = HisPersverblijfsr.countHisPersverblijfsrs();
        Assert.assertTrue("Counter for 'HisPersverblijfsr' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void HisPersverblijfsrIntegrationTest.testFindHisPersverblijfsr() {
        HisPersverblijfsr obj = dod.getRandomHisPersverblijfsr();
        Assert.assertNotNull("Data on demand for 'HisPersverblijfsr' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisPersverblijfsr' failed to provide an identifier", id);
        obj = HisPersverblijfsr.findHisPersverblijfsr(id);
        Assert.assertNotNull("Find method for 'HisPersverblijfsr' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'HisPersverblijfsr' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void HisPersverblijfsrIntegrationTest.testFindAllHisPersverblijfsrs() {
        Assert.assertNotNull("Data on demand for 'HisPersverblijfsr' failed to initialize correctly", dod.getRandomHisPersverblijfsr());
        long count = HisPersverblijfsr.countHisPersverblijfsrs();
        Assert.assertTrue("Too expensive to perform a find all test for 'HisPersverblijfsr', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<HisPersverblijfsr> result = HisPersverblijfsr.findAllHisPersverblijfsrs();
        Assert.assertNotNull("Find all method for 'HisPersverblijfsr' illegally returned null", result);
        Assert.assertTrue("Find all method for 'HisPersverblijfsr' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void HisPersverblijfsrIntegrationTest.testFindHisPersverblijfsrEntries() {
        Assert.assertNotNull("Data on demand for 'HisPersverblijfsr' failed to initialize correctly", dod.getRandomHisPersverblijfsr());
        long count = HisPersverblijfsr.countHisPersverblijfsrs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<HisPersverblijfsr> result = HisPersverblijfsr.findHisPersverblijfsrEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'HisPersverblijfsr' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'HisPersverblijfsr' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void HisPersverblijfsrIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'HisPersverblijfsr' failed to initialize correctly", dod.getRandomHisPersverblijfsr());
        HisPersverblijfsr obj = dod.getNewTransientHisPersverblijfsr(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'HisPersverblijfsr' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'HisPersverblijfsr' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'HisPersverblijfsr' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void HisPersverblijfsrIntegrationTest.testRemove() {
        HisPersverblijfsr obj = dod.getRandomHisPersverblijfsr();
        Assert.assertNotNull("Data on demand for 'HisPersverblijfsr' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'HisPersverblijfsr' failed to provide an identifier", id);
        obj = HisPersverblijfsr.findHisPersverblijfsr(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'HisPersverblijfsr' with identifier '" + id + "'", HisPersverblijfsr.findHisPersverblijfsr(id));
    }
    
}
