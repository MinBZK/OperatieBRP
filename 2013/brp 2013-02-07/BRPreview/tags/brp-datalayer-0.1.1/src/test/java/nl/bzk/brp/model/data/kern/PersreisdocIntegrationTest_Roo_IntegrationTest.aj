// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Persreisdoc;
import nl.bzk.brp.model.data.kern.PersreisdocDataOnDemand;
import nl.bzk.brp.model.data.kern.PersreisdocIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PersreisdocIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersreisdocIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersreisdocIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersreisdocIntegrationTest: @Transactional;
    
    @Autowired
    private PersreisdocDataOnDemand PersreisdocIntegrationTest.dod;
    
    @Test
    public void PersreisdocIntegrationTest.testCountPersreisdocs() {
        Assert.assertNotNull("Data on demand for 'Persreisdoc' failed to initialize correctly", dod.getRandomPersreisdoc());
        long count = Persreisdoc.countPersreisdocs();
        Assert.assertTrue("Counter for 'Persreisdoc' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersreisdocIntegrationTest.testFindPersreisdoc() {
        Persreisdoc obj = dod.getRandomPersreisdoc();
        Assert.assertNotNull("Data on demand for 'Persreisdoc' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persreisdoc' failed to provide an identifier", id);
        obj = Persreisdoc.findPersreisdoc(id);
        Assert.assertNotNull("Find method for 'Persreisdoc' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Persreisdoc' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersreisdocIntegrationTest.testFindAllPersreisdocs() {
        Assert.assertNotNull("Data on demand for 'Persreisdoc' failed to initialize correctly", dod.getRandomPersreisdoc());
        long count = Persreisdoc.countPersreisdocs();
        Assert.assertTrue("Too expensive to perform a find all test for 'Persreisdoc', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Persreisdoc> result = Persreisdoc.findAllPersreisdocs();
        Assert.assertNotNull("Find all method for 'Persreisdoc' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Persreisdoc' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersreisdocIntegrationTest.testFindPersreisdocEntries() {
        Assert.assertNotNull("Data on demand for 'Persreisdoc' failed to initialize correctly", dod.getRandomPersreisdoc());
        long count = Persreisdoc.countPersreisdocs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Persreisdoc> result = Persreisdoc.findPersreisdocEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Persreisdoc' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Persreisdoc' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersreisdocIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Persreisdoc' failed to initialize correctly", dod.getRandomPersreisdoc());
        Persreisdoc obj = dod.getNewTransientPersreisdoc(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Persreisdoc' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Persreisdoc' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Persreisdoc' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersreisdocIntegrationTest.testRemove() {
        Persreisdoc obj = dod.getRandomPersreisdoc();
        Assert.assertNotNull("Data on demand for 'Persreisdoc' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persreisdoc' failed to provide an identifier", id);
        obj = Persreisdoc.findPersreisdoc(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Persreisdoc' with identifier '" + id + "'", Persreisdoc.findPersreisdoc(id));
    }
    
}
