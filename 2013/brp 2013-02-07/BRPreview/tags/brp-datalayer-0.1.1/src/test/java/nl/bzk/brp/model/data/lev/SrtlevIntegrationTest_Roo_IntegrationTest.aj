// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.lev;

import java.util.List;
import nl.bzk.brp.model.data.lev.Srtlev;
import nl.bzk.brp.model.data.lev.SrtlevDataOnDemand;
import nl.bzk.brp.model.data.lev.SrtlevIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SrtlevIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SrtlevIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SrtlevIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SrtlevIntegrationTest: @Transactional;
    
    @Autowired
    private SrtlevDataOnDemand SrtlevIntegrationTest.dod;
    
    @Test
    public void SrtlevIntegrationTest.testCountSrtlevs() {
        Assert.assertNotNull("Data on demand for 'Srtlev' failed to initialize correctly", dod.getRandomSrtlev());
        long count = Srtlev.countSrtlevs();
        Assert.assertTrue("Counter for 'Srtlev' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SrtlevIntegrationTest.testFindSrtlev() {
        Srtlev obj = dod.getRandomSrtlev();
        Assert.assertNotNull("Data on demand for 'Srtlev' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtlev' failed to provide an identifier", id);
        obj = Srtlev.findSrtlev(id);
        Assert.assertNotNull("Find method for 'Srtlev' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Srtlev' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SrtlevIntegrationTest.testFindAllSrtlevs() {
        Assert.assertNotNull("Data on demand for 'Srtlev' failed to initialize correctly", dod.getRandomSrtlev());
        long count = Srtlev.countSrtlevs();
        Assert.assertTrue("Too expensive to perform a find all test for 'Srtlev', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Srtlev> result = Srtlev.findAllSrtlevs();
        Assert.assertNotNull("Find all method for 'Srtlev' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Srtlev' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SrtlevIntegrationTest.testFindSrtlevEntries() {
        Assert.assertNotNull("Data on demand for 'Srtlev' failed to initialize correctly", dod.getRandomSrtlev());
        long count = Srtlev.countSrtlevs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Srtlev> result = Srtlev.findSrtlevEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Srtlev' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Srtlev' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SrtlevIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Srtlev' failed to initialize correctly", dod.getRandomSrtlev());
        Srtlev obj = dod.getNewTransientSrtlev(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Srtlev' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Srtlev' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Srtlev' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SrtlevIntegrationTest.testRemove() {
        Srtlev obj = dod.getRandomSrtlev();
        Assert.assertNotNull("Data on demand for 'Srtlev' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Srtlev' failed to provide an identifier", id);
        obj = Srtlev.findSrtlev(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Srtlev' with identifier '" + id + "'", Srtlev.findSrtlev(id));
    }
    
}
