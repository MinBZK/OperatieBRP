// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Sector;
import nl.bzk.brp.model.data.kern.SectorDataOnDemand;
import nl.bzk.brp.model.data.kern.SectorIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SectorIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SectorIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SectorIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SectorIntegrationTest: @Transactional;
    
    @Autowired
    private SectorDataOnDemand SectorIntegrationTest.dod;
    
    @Test
    public void SectorIntegrationTest.testCountSectors() {
        Assert.assertNotNull("Data on demand for 'Sector' failed to initialize correctly", dod.getRandomSector());
        long count = Sector.countSectors();
        Assert.assertTrue("Counter for 'Sector' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SectorIntegrationTest.testFindSector() {
        Sector obj = dod.getRandomSector();
        Assert.assertNotNull("Data on demand for 'Sector' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Sector' failed to provide an identifier", id);
        obj = Sector.findSector(id);
        Assert.assertNotNull("Find method for 'Sector' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Sector' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SectorIntegrationTest.testFindAllSectors() {
        Assert.assertNotNull("Data on demand for 'Sector' failed to initialize correctly", dod.getRandomSector());
        long count = Sector.countSectors();
        Assert.assertTrue("Too expensive to perform a find all test for 'Sector', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Sector> result = Sector.findAllSectors();
        Assert.assertNotNull("Find all method for 'Sector' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Sector' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SectorIntegrationTest.testFindSectorEntries() {
        Assert.assertNotNull("Data on demand for 'Sector' failed to initialize correctly", dod.getRandomSector());
        long count = Sector.countSectors();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Sector> result = Sector.findSectorEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Sector' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Sector' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SectorIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Sector' failed to initialize correctly", dod.getRandomSector());
        Sector obj = dod.getNewTransientSector(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Sector' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Sector' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Sector' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SectorIntegrationTest.testRemove() {
        Sector obj = dod.getRandomSector();
        Assert.assertNotNull("Data on demand for 'Sector' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Sector' failed to provide an identifier", id);
        obj = Sector.findSector(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Sector' with identifier '" + id + "'", Sector.findSector(id));
    }
    
}
