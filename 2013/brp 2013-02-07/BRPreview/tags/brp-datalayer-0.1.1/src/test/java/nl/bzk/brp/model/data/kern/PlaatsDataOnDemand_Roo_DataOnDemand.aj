// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.math.BigDecimal;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import nl.bzk.brp.model.data.kern.Plaats;
import nl.bzk.brp.model.data.kern.PlaatsDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect PlaatsDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PlaatsDataOnDemand: @Component;
    
    private Random PlaatsDataOnDemand.rnd = new SecureRandom();
    
    private List<Plaats> PlaatsDataOnDemand.data;
    
    public Plaats PlaatsDataOnDemand.getNewTransientPlaats(int index) {
        Plaats obj = new Plaats();
        setDataanvgel(obj, index);
        setDateindegel(obj, index);
        setNaam(obj, index);
        setWplcode(obj, index);
        return obj;
    }
    
    public void PlaatsDataOnDemand.setDataanvgel(Plaats obj, int index) {
        BigDecimal dataanvgel = BigDecimal.valueOf(index);
        obj.setDataanvgel(dataanvgel);
    }
    
    public void PlaatsDataOnDemand.setDateindegel(Plaats obj, int index) {
        BigDecimal dateindegel = BigDecimal.valueOf(index);
        obj.setDateindegel(dateindegel);
    }
    
    public void PlaatsDataOnDemand.setNaam(Plaats obj, int index) {
        String naam = "naam_" + index;
        if (naam.length() > 40) {
            naam = naam.substring(0, 40);
        }
        obj.setNaam(naam);
    }
    
    public void PlaatsDataOnDemand.setWplcode(Plaats obj, int index) {
        String wplcode = "wp_" + index;
        if (wplcode.length() > 4) {
            wplcode = new Random().nextInt(10) + wplcode.substring(1, 4);
        }
        obj.setWplcode(wplcode);
    }
    
    public Plaats PlaatsDataOnDemand.getSpecificPlaats(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Plaats obj = data.get(index);
        Integer id = obj.getId();
        return Plaats.findPlaats(id);
    }
    
    public Plaats PlaatsDataOnDemand.getRandomPlaats() {
        init();
        Plaats obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return Plaats.findPlaats(id);
    }
    
    public boolean PlaatsDataOnDemand.modifyPlaats(Plaats obj) {
        return false;
    }
    
    public void PlaatsDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Plaats.findPlaatsEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Plaats' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Plaats>();
        for (int i = 0; i < 10; i++) {
            Plaats obj = getNewTransientPlaats(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
