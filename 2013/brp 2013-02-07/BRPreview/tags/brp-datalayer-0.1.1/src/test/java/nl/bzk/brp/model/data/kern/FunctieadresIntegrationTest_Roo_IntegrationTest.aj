// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Functieadres;
import nl.bzk.brp.model.data.kern.FunctieadresDataOnDemand;
import nl.bzk.brp.model.data.kern.FunctieadresIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect FunctieadresIntegrationTest_Roo_IntegrationTest {
    
    declare @type: FunctieadresIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: FunctieadresIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: FunctieadresIntegrationTest: @Transactional;
    
    @Autowired
    private FunctieadresDataOnDemand FunctieadresIntegrationTest.dod;
    
    @Test
    public void FunctieadresIntegrationTest.testCountFunctieadreses() {
        Assert.assertNotNull("Data on demand for 'Functieadres' failed to initialize correctly", dod.getRandomFunctieadres());
        long count = Functieadres.countFunctieadreses();
        Assert.assertTrue("Counter for 'Functieadres' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void FunctieadresIntegrationTest.testFindFunctieadres() {
        Functieadres obj = dod.getRandomFunctieadres();
        Assert.assertNotNull("Data on demand for 'Functieadres' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Functieadres' failed to provide an identifier", id);
        obj = Functieadres.findFunctieadres(id);
        Assert.assertNotNull("Find method for 'Functieadres' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Functieadres' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void FunctieadresIntegrationTest.testFindAllFunctieadreses() {
        Assert.assertNotNull("Data on demand for 'Functieadres' failed to initialize correctly", dod.getRandomFunctieadres());
        long count = Functieadres.countFunctieadreses();
        Assert.assertTrue("Too expensive to perform a find all test for 'Functieadres', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Functieadres> result = Functieadres.findAllFunctieadreses();
        Assert.assertNotNull("Find all method for 'Functieadres' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Functieadres' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void FunctieadresIntegrationTest.testFindFunctieadresEntries() {
        Assert.assertNotNull("Data on demand for 'Functieadres' failed to initialize correctly", dod.getRandomFunctieadres());
        long count = Functieadres.countFunctieadreses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Functieadres> result = Functieadres.findFunctieadresEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Functieadres' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Functieadres' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void FunctieadresIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Functieadres' failed to initialize correctly", dod.getRandomFunctieadres());
        Functieadres obj = dod.getNewTransientFunctieadres(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Functieadres' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Functieadres' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Functieadres' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void FunctieadresIntegrationTest.testRemove() {
        Functieadres obj = dod.getRandomFunctieadres();
        Assert.assertNotNull("Data on demand for 'Functieadres' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Functieadres' failed to provide an identifier", id);
        obj = Functieadres.findFunctieadres(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Functieadres' with identifier '" + id + "'", Functieadres.findFunctieadres(id));
    }
    
}
