// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Persgeslnaamcomp;
import nl.bzk.brp.model.data.kern.PersgeslnaamcompDataOnDemand;
import nl.bzk.brp.model.data.kern.PersgeslnaamcompIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PersgeslnaamcompIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersgeslnaamcompIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersgeslnaamcompIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersgeslnaamcompIntegrationTest: @Transactional;
    
    @Autowired
    private PersgeslnaamcompDataOnDemand PersgeslnaamcompIntegrationTest.dod;
    
    @Test
    public void PersgeslnaamcompIntegrationTest.testCountPersgeslnaamcomps() {
        Assert.assertNotNull("Data on demand for 'Persgeslnaamcomp' failed to initialize correctly", dod.getRandomPersgeslnaamcomp());
        long count = Persgeslnaamcomp.countPersgeslnaamcomps();
        Assert.assertTrue("Counter for 'Persgeslnaamcomp' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersgeslnaamcompIntegrationTest.testFindPersgeslnaamcomp() {
        Persgeslnaamcomp obj = dod.getRandomPersgeslnaamcomp();
        Assert.assertNotNull("Data on demand for 'Persgeslnaamcomp' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persgeslnaamcomp' failed to provide an identifier", id);
        obj = Persgeslnaamcomp.findPersgeslnaamcomp(id);
        Assert.assertNotNull("Find method for 'Persgeslnaamcomp' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Persgeslnaamcomp' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersgeslnaamcompIntegrationTest.testFindAllPersgeslnaamcomps() {
        Assert.assertNotNull("Data on demand for 'Persgeslnaamcomp' failed to initialize correctly", dod.getRandomPersgeslnaamcomp());
        long count = Persgeslnaamcomp.countPersgeslnaamcomps();
        Assert.assertTrue("Too expensive to perform a find all test for 'Persgeslnaamcomp', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Persgeslnaamcomp> result = Persgeslnaamcomp.findAllPersgeslnaamcomps();
        Assert.assertNotNull("Find all method for 'Persgeslnaamcomp' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Persgeslnaamcomp' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersgeslnaamcompIntegrationTest.testFindPersgeslnaamcompEntries() {
        Assert.assertNotNull("Data on demand for 'Persgeslnaamcomp' failed to initialize correctly", dod.getRandomPersgeslnaamcomp());
        long count = Persgeslnaamcomp.countPersgeslnaamcomps();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Persgeslnaamcomp> result = Persgeslnaamcomp.findPersgeslnaamcompEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Persgeslnaamcomp' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Persgeslnaamcomp' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersgeslnaamcompIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Persgeslnaamcomp' failed to initialize correctly", dod.getRandomPersgeslnaamcomp());
        Persgeslnaamcomp obj = dod.getNewTransientPersgeslnaamcomp(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Persgeslnaamcomp' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Persgeslnaamcomp' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Persgeslnaamcomp' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersgeslnaamcompIntegrationTest.testRemove() {
        Persgeslnaamcomp obj = dod.getRandomPersgeslnaamcomp();
        Assert.assertNotNull("Data on demand for 'Persgeslnaamcomp' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persgeslnaamcomp' failed to provide an identifier", id);
        obj = Persgeslnaamcomp.findPersgeslnaamcomp(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Persgeslnaamcomp' with identifier '" + id + "'", Persgeslnaamcomp.findPersgeslnaamcomp(id));
    }
    
}
