// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Persverificatie;
import nl.bzk.brp.model.data.kern.PersverificatieDataOnDemand;
import nl.bzk.brp.model.data.kern.PersverificatieIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PersverificatieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersverificatieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersverificatieIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersverificatieIntegrationTest: @Transactional;
    
    @Autowired
    private PersverificatieDataOnDemand PersverificatieIntegrationTest.dod;
    
    @Test
    public void PersverificatieIntegrationTest.testCountPersverificaties() {
        Assert.assertNotNull("Data on demand for 'Persverificatie' failed to initialize correctly", dod.getRandomPersverificatie());
        long count = Persverificatie.countPersverificaties();
        Assert.assertTrue("Counter for 'Persverificatie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersverificatieIntegrationTest.testFindPersverificatie() {
        Persverificatie obj = dod.getRandomPersverificatie();
        Assert.assertNotNull("Data on demand for 'Persverificatie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persverificatie' failed to provide an identifier", id);
        obj = Persverificatie.findPersverificatie(id);
        Assert.assertNotNull("Find method for 'Persverificatie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Persverificatie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersverificatieIntegrationTest.testFindAllPersverificaties() {
        Assert.assertNotNull("Data on demand for 'Persverificatie' failed to initialize correctly", dod.getRandomPersverificatie());
        long count = Persverificatie.countPersverificaties();
        Assert.assertTrue("Too expensive to perform a find all test for 'Persverificatie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Persverificatie> result = Persverificatie.findAllPersverificaties();
        Assert.assertNotNull("Find all method for 'Persverificatie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Persverificatie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersverificatieIntegrationTest.testFindPersverificatieEntries() {
        Assert.assertNotNull("Data on demand for 'Persverificatie' failed to initialize correctly", dod.getRandomPersverificatie());
        long count = Persverificatie.countPersverificaties();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Persverificatie> result = Persverificatie.findPersverificatieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Persverificatie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Persverificatie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersverificatieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Persverificatie' failed to initialize correctly", dod.getRandomPersverificatie());
        Persverificatie obj = dod.getNewTransientPersverificatie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Persverificatie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Persverificatie' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Persverificatie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersverificatieIntegrationTest.testRemove() {
        Persverificatie obj = dod.getRandomPersverificatie();
        Assert.assertNotNull("Data on demand for 'Persverificatie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Persverificatie' failed to provide an identifier", id);
        obj = Persverificatie.findPersverificatie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Persverificatie' with identifier '" + id + "'", Persverificatie.findPersverificatie(id));
    }
    
}
