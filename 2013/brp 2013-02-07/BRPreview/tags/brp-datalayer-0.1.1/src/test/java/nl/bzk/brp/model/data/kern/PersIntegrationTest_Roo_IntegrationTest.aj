// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.PersDataOnDemand;
import nl.bzk.brp.model.data.kern.PersIntegrationTest;
import nl.bzk.brp.model.service.PersonService;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PersIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersIntegrationTest: @Transactional;
    
    @Autowired
    private PersDataOnDemand PersIntegrationTest.dod;
    
    @Autowired
    PersonService PersIntegrationTest.personService;
    
    @Test
    public void PersIntegrationTest.testCountAllPerses() {
        Assert.assertNotNull("Data on demand for 'Pers' failed to initialize correctly", dod.getRandomPers());
        long count = personService.countAllPerses();
        Assert.assertTrue("Counter for 'Pers' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersIntegrationTest.testFindPers() {
        Pers obj = dod.getRandomPers();
        Assert.assertNotNull("Data on demand for 'Pers' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Pers' failed to provide an identifier", id);
        obj = personService.findPers(id);
        Assert.assertNotNull("Find method for 'Pers' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Pers' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersIntegrationTest.testFindAllPerses() {
        Assert.assertNotNull("Data on demand for 'Pers' failed to initialize correctly", dod.getRandomPers());
        long count = personService.countAllPerses();
        Assert.assertTrue("Too expensive to perform a find all test for 'Pers', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Pers> result = personService.findAllPerses();
        Assert.assertNotNull("Find all method for 'Pers' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Pers' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersIntegrationTest.testFindPersEntries() {
        Assert.assertNotNull("Data on demand for 'Pers' failed to initialize correctly", dod.getRandomPers());
        long count = personService.countAllPerses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Pers> result = personService.findPersEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Pers' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Pers' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersIntegrationTest.testSavePers() {
        Assert.assertNotNull("Data on demand for 'Pers' failed to initialize correctly", dod.getRandomPers());
        Pers obj = dod.getNewTransientPers(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Pers' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Pers' identifier to be null", obj.getId());
        personService.savePers(obj);
        obj.flush();
        Assert.assertNotNull("Expected 'Pers' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersIntegrationTest.testDeletePers() {
        Pers obj = dod.getRandomPers();
        Assert.assertNotNull("Data on demand for 'Pers' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Pers' failed to provide an identifier", id);
        obj = personService.findPers(id);
        personService.deletePers(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'Pers' with identifier '" + id + "'", personService.findPers(id));
    }
    
}
