// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package nl.bzk.brp.model.data.kern;

import java.util.List;
import nl.bzk.brp.model.data.kern.Adellijketitel;
import nl.bzk.brp.model.data.kern.AdellijketitelDataOnDemand;
import nl.bzk.brp.model.data.kern.AdellijketitelIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AdellijketitelIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AdellijketitelIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AdellijketitelIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AdellijketitelIntegrationTest: @Transactional;
    
    @Autowired
    private AdellijketitelDataOnDemand AdellijketitelIntegrationTest.dod;
    
    @Test
    public void AdellijketitelIntegrationTest.testCountAdellijketitels() {
        Assert.assertNotNull("Data on demand for 'Adellijketitel' failed to initialize correctly", dod.getRandomAdellijketitel());
        long count = Adellijketitel.countAdellijketitels();
        Assert.assertTrue("Counter for 'Adellijketitel' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AdellijketitelIntegrationTest.testFindAdellijketitel() {
        Adellijketitel obj = dod.getRandomAdellijketitel();
        Assert.assertNotNull("Data on demand for 'Adellijketitel' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Adellijketitel' failed to provide an identifier", id);
        obj = Adellijketitel.findAdellijketitel(id);
        Assert.assertNotNull("Find method for 'Adellijketitel' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Adellijketitel' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AdellijketitelIntegrationTest.testFindAllAdellijketitels() {
        Assert.assertNotNull("Data on demand for 'Adellijketitel' failed to initialize correctly", dod.getRandomAdellijketitel());
        long count = Adellijketitel.countAdellijketitels();
        Assert.assertTrue("Too expensive to perform a find all test for 'Adellijketitel', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Adellijketitel> result = Adellijketitel.findAllAdellijketitels();
        Assert.assertNotNull("Find all method for 'Adellijketitel' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Adellijketitel' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AdellijketitelIntegrationTest.testFindAdellijketitelEntries() {
        Assert.assertNotNull("Data on demand for 'Adellijketitel' failed to initialize correctly", dod.getRandomAdellijketitel());
        long count = Adellijketitel.countAdellijketitels();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Adellijketitel> result = Adellijketitel.findAdellijketitelEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Adellijketitel' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Adellijketitel' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AdellijketitelIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Adellijketitel' failed to initialize correctly", dod.getRandomAdellijketitel());
        Adellijketitel obj = dod.getNewTransientAdellijketitel(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Adellijketitel' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Adellijketitel' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Adellijketitel' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AdellijketitelIntegrationTest.testRemove() {
        Adellijketitel obj = dod.getRandomAdellijketitel();
        Assert.assertNotNull("Data on demand for 'Adellijketitel' failed to initialize correctly", obj);
        Short id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Adellijketitel' failed to provide an identifier", id);
        obj = Adellijketitel.findAdellijketitel(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Adellijketitel' with identifier '" + id + "'", Adellijketitel.findAdellijketitel(id));
    }
    
}
