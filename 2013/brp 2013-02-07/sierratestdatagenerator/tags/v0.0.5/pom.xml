<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>nl.bzk.brp.test</groupId>
	<artifactId>sierratestdatagenerator</artifactId>
	<version>0.0.5</version>
	<packaging>jar</packaging>
	<name>sierratestdatagenerator</name>
	<parent>
		<groupId>nl.bzk.brp</groupId>
		<artifactId>root-pom</artifactId>
		<version>0.1.17</version>
	</parent>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>1.6</maven.compiler.source>
		<maven.compiler.target>1.6</maven.compiler.target>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<maven.build.timestamp.format>dd-MM-yyyy HH:mm.ss</maven.build.timestamp.format>
		<buildnumber>${env.BUILD_NUMBER}</buildnumber>
		<svnrevision>${env.SVN_REVISION}</svnrevision>
		<timestamp>${maven.build.timestamp}</timestamp>
	</properties>

	<developers>
		<developer>
			<id>bash</id>
			<name>Bas Huisman</name>
			<email>bashuisman@lostlemon.nl</email>
			<organization>Lost Lemon</organization>
			<organizationUrl>http://www.lostlemon.nl/</organizationUrl>
			<roles>
				<role>Developer</role>
			</roles>
			<timezone>+1</timezone>
		</developer>
		<developer>
			<id>anjaw</id>
			<name>Antek Jaworski</name>
			<email>antekjaworski@lostlemon.nl</email>
			<organization>Lost Lemon</organization>
			<organizationUrl>http://www.lostlemon.nl/</organizationUrl>
			<roles>
				<role>Developer</role>
			</roles>
			<timezone>+1</timezone>
		</developer>
	</developers>

	<dependencies>
		<dependency>
			<groupId>commons-cli</groupId>
			<artifactId>commons-cli</artifactId>
			<version>1.2</version>
		</dependency>
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
			<version>2.4</version>
		</dependency>

		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>1.6.6</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>1.0.6</version>
		</dependency>
		<dependency>
			<groupId>net.sourceforge.jexcelapi</groupId>
			<artifactId>jxl</artifactId>
			<version>2.6.12</version>
		</dependency>
	</dependencies>
	<profiles>
		<profile>
			<id>create-test-sql</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>exec-maven-plugin</artifactId>
						<version>1.2.1</version>
						<executions>
							<execution>
								<id>1</id>
								<phase>test</phase>
								<goals>
									<goal>java</goal>
								</goals>
								<configuration>
									<arguments>
										<argument>-f</argument>
										<argument>src/test/resources/SierraTestdataGenerator.xls</argument>
										<argument>-s</argument>
										<argument>target/sierratestdatagenerator.sql</argument>
									</arguments>
									<mainClass>nl.bzk.brp.sierratestdatagenerator.SierraTestdataGenerator</mainClass>
								</configuration>
							</execution>
						</executions>
					</plugin>
					<!-- zip de sql statements -->
					<plugin>
						<artifactId>maven-assembly-plugin</artifactId>
						<executions>
							<execution>
								<phase>package</phase>
								<goals>
									<goal>single</goal>
								</goals>
							</execution>
						</executions>
						<configuration>
							<descriptors>
								<descriptor>src/assemble/sql.xml</descriptor>
							</descriptors>
						</configuration>
					</plugin>

				</plugins>
			</build>
		</profile>
	</profiles>

	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<includes>
					<include>**/*</include>
				</includes>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>2.4</version>
				<configuration>
				  <archive>
					<index>true</index>
					<manifest>
						<addDefaultSpecificationEntries />
						<classpathPrefix>lib/</classpathPrefix>
						<addClasspath>true</addClasspath>
						<mainClass>nl.bzk.brp.sierratestdatagenerator.SierraTestdataGenerator</mainClass>
					</manifest>
					<manifestEntries>
						<mode>development</mode>
						<url>${project.url}</url>
						<version>${version}</version>
						<project-name>${name}</project-name>
						<artifactId>${artifactId}</artifactId>
					</manifestEntries>
				  </archive>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>2.3.2</version>
				<configuration>
					<source>${maven.compiler.source}</source>
					<target>${maven.compiler.target}</target>
					<optimize>true</optimize>
					<showWarnings>false</showWarnings>
					<encoding>${project.build.sourceEncoding}</encoding>
					<debug>true</debug>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>2.5</version>
				<configuration>
					<encoding>${project.build.sourceEncoding}</encoding>
				</configuration>
			</plugin>
            <!--
             | shade-plugin relocate the required classes for this artifact INTO the jar file 'finalName'
             | as result that you have a single jar file that you can run from command line without any external dependency
             | Note: all dependendency with scope 'test', 'provided' ... are ignored.
             +-->
            <plugin>
                 <groupId>org.apache.maven.plugins</groupId>
                 <artifactId>maven-shade-plugin</artifactId>
                 <version>1.3.1</version>
                    <executions>
                        <execution>
                            <phase>package</phase>
                            <goals>
                                <goal>shade</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
<!--
                        <finalName>${name}</finalName>
-->
                    </configuration>
            </plugin>
		</plugins>
	</build>
	<scm>
		<connection>scm:svn:https://www.modernodam.nl/svn/brp-code/sierratestdatagenerator/tags/v0.0.5</connection>
	</scm>
</project>
