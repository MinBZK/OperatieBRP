group groepenLogisch;

import "generieke-templates.stg"

groepLogisch(groep) ::= <<
package <groep.packageName>;

import nl.bzk.brp.model.groep.logisch.basis.<first(groep.interfaces)>;


/**
 * <groep.javaDoc>.
 */
public interface <groep.name> extends <first(groep.interfaces)> {

}

>>
