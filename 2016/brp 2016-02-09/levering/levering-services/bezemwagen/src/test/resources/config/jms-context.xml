<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:jms="http://www.springframework.org/schema/jms"
       xmlns:amq="http://activemq.apache.org/schema/core"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/jms http://www.springframework.org/schema/jms/spring-jms.xsd
        http://activemq.apache.org/schema/core http://activemq.apache.org/schema/core/activemq-core.xsd
    ">

    <bean id="connectionFactory" class="org.apache.activemq.ActiveMQConnectionFactory">
        <property name="brokerURL" value="${jms.broker.ah.url}" />
    </bean>

    <jms:listener-container container-type="default"
        connection-factory="connectionFactory" acknowledge="auto"
        destination-type="queue" concurrency="1">
        <jms:listener destination="${jms.broker.ah.queue}"
            ref="administratieveHandelingListener" />
    </jms:listener-container>

    <amq:broker id="broker" persistent="false" useJmx="false"
        brokerName="localhost" >
        <amq:transportConnectors>
            <amq:transportConnector uri="${jms.broker.ah.url}" />
        </amq:transportConnectors>
        <amq:systemUsage>
            <amq:systemUsage>
                <amq:tempUsage>
                    <amq:tempUsage limit="32mb" />
                </amq:tempUsage>
            </amq:systemUsage>
        </amq:systemUsage>
    </amq:broker>

    <amq:queue id="AHQueue" physicalName="${jms.broker.ah.queue}" />
    <amq:connectionFactory id="testConnectionFactory"
        brokerURL="${jms.broker.ah.url}" />

    <bean id="mutatieAdministratieveHandelingTemplate" class="org.springframework.jms.core.JmsTemplate">
        <property name="connectionFactory" ref="testConnectionFactory" />
        <property name="defaultDestination" ref="AHQueue" />
    </bean>

    <bean id="administratieveHandelingListener" class="nl.bzk.brp.levering.bezemwagen.jms.impl.AdministratieveHandelingListenerImpl"/>
</beans>
