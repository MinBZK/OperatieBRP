<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:p="http://www.springframework.org/schema/p"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:aop="http://www.springframework.org/schema/aop"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:jdbc="http://www.springframework.org/schema/jdbc"
    xmlns:jpa="http://www.springframework.org/schema/data/jpa"
    xmlns:amq="http://activemq.apache.org/schema/core"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
        http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc.xsd
        http://www.springframework.org/schema/data/jpa http://www.springframework.org/schema/data/jpa/spring-jpa-1.0.xsd
        http://activemq.apache.org/schema/core http://activemq.apache.org/schema/core/activemq-core.xsd">

 	<!-- JMS XA ConnectionFactory -->
  	<amq:xaConnectionFactory id="activeMqXaConnectionFactory" brokerURL="${routering.activemq.url:tcp://localhost:61616}"/>

  	<bean name="gemeenteConnectionFactory" class="nl.bzk.migratiebrp.register.client.util.RefBeanFactory">
  		<property name="bean" ref="activeMqXaConnectionFactory"/>
  	</bean>

    <!-- Atomikos JTA-enabled ConnectionFactory, configured with the vendor's XA factory. -->
    <bean name="voiscJmsConnectionFactory" class="com.atomikos.jms.AtomikosConnectionFactoryBean" init-method="init" destroy-method="close">
        <property name="uniqueResourceName">
            <!--
             | Tijdens jUnit testen kan de ApplicationContext meerdere keren worden geinstantieerd.
             | Elke keer dient een unique naam gebruikt te worden, anders gaat Atomikos fout.
              -->
            <bean class="nl.bzk.migratiebrp.util.common.UniqueName" >
            	<property name="baseName" value="voiscQueues" />
            </bean>
        </property>        
        <property name="xaConnectionFactory" ref="activeMqXaConnectionFactory" />
        <property name="minPoolSize" value="${voisc.queue.minpool:3}" />
        <property name="maxPoolSize" value="${voisc.queue.maxpool:20}" />
	</bean>

    <!-- Destinations -->

  	<amq:queue id="vospgOntvangstQueue" physicalName="${routering.queue.voisc.ontvangst:voisc.ontvangst}" />
  	<amq:queue id="vospgVerzendenQueue" physicalName="${routering.queue.voisc.verzenden:voisc.verzenden}" />

  	<amq:queue id="archiveringQueue" physicalName="${routering.queue.archivering:archivering}" />

  	<amq:topic id="gemeenteRegisterTopic" physicalName="${routering.topic.gemeente.register:gemeente.register}" />
  	<amq:queue id="gemeenteVerzoekQueue" physicalName="${routering.queue.gemeente.register:gemeente.verzoek}" />

</beans>