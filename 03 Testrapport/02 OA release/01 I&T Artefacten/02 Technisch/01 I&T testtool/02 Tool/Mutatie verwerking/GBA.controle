VOISC;select cast(query1.result as integer) + cast(query2.result as integer) from (select case when (current_timestamp - max(tijdstip_verzonden)) > interval '60 seconds' then 1 else 0 end as result from voisc.bericht) query1, (select case when (select count (*) from voisc.bericht) > 0 then 1 else 2 end as result) query2;2;10;30;300
#Welke DB;query;gewenste resultaat van de query;na hoeveel seconde de query opnieuw uitvoeren;Maximaal hoe vaak moet er gecontroleerd worden;na hoeveel seconde de query voor het eerst uitgevoerd wordt na het starten van een M-moment
