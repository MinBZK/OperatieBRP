#ARCHIVERING;select cast(query1.result as integer) + cast(query2.result as integer) from (select case when (current_timestamp - max(tsverzending)) > interval '60 seconds' then 1 else 0 end as result from ber.ber b where b.zendendesysteem = 'BRP' and b.ontvangendesysteem = 'Leveringsysteem' and b.data like '%?xml%' and b.data not like '%Plaatsing afnemerindicatie%') query1, (select case when (select count (*) from ber.ber b where b.zendendesysteem = 'BRP' and b.ontvangendesysteem = 'Leveringsysteem' and b.data like '%?xml%' and b.data not like '%Plaatsing afnemerindicatie%') > 0 then 1 else 2 end as result) query2;2;10;60
BESTAND;30;;5;30
#Welke DB;query;gewenste resultaat van de query;na hoeveel seconde de query opnieuw uitvoeren;na hoeveel seconde de query voor het eerst uitgevoerd wordt na het starten van een M-moment
